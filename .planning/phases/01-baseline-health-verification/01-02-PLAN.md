---
phase: 01-baseline-health-verification
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/
  - plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml
  - plugins/sec-overlay/skills/sec-overlay/helpers/README.md
  - plugins/sec-overlay/.claude-plugin/plugin.json
  - plugins/sec-overlay/CHANGELOG.md
  - .claude-plugin/marketplace.json
  - .pre-commit-config.yaml
  - scripts/hooks/pre-commit-check.sh
  - scripts/README.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every triage row dispositioned `code defect`, `stale test`, or `config` in 01-VERIFICATION.md has a corresponding fix commit; rows dispositioned `environmental` have none (D-01)"
    - "`uv run pytest` against the helpers package reports zero failures other than the ones recorded as environmental with their root cause named"
    - "`uv run ruff check` and `uv run ty check` against the helpers package exit 0 with zero unaddressed warnings (D-14)"
    - "`claude plugin validate .` exits 0 at the repo root and inside plugins/sec-overlay/ (D-15)"
    - "`prek run --all-files` exits 0 from the repo root (D-12)"
    - "Every fix commit touching a sec-overlay shipping file bumps the plugin patch version and adds a CHANGELOG entry in that same commit — no batching (D-04)"
    - "Every commit that changes a test rather than production code states in its message why the test asserted stale or wrong behavior (D-03)"
    - "The two frozen JSON-contract modules are byte-identical to their state at phase start; a fix that would require editing one halts and escalates instead (D-02)"
    - "No gate was made green by weakening it: no test deleted or skipped, no unjustified suppression added, no exclude list widened, no gitignored corpus data committed"
  prohibitions:
    - statement: "Must not manufacture a green gate by weakening the gate itself — deleting or skipping tests, adding an unjustified noqa or type-ignore, widening the ruff/ty exclude lists, or committing gitignored corpus data — instead of fixing the defect"
      status: unverified
    - statement: "Must not record a gate result that is not backed by quoted output from a real run of the stated command in the stated directory during this phase"
      status: unverified
    - statement: "Must not bypass the repo's own governance hooks to land a fix: no --no-verify, no wildcard staging, no direct commit to main"
      status: unverified
    - statement: "Must not edit the frozen JSON-contract modules; a fix that appears to require one halts and escalates to the maintainer instead of proceeding"
      status: unverified
  artifacts:
    - path: "plugins/sec-overlay/.claude-plugin/plugin.json"
      provides: "Patch-bumped plugin version accompanying each shipping-file fix"
      contains: "version"
    - path: "plugins/sec-overlay/CHANGELOG.md"
      provides: "A Fixed entry per fix commit touching a sec-overlay shipping file"
      contains: "### Fixed"
  key_links:
    - from: "plugins/sec-overlay/.claude-plugin/plugin.json"
      to: "plugins/sec-overlay/CHANGELOG.md"
      via: "each version bump has a matching changelog heading in the same commit (D-04)"
      pattern: "version"
    - from: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      to: "git history"
      via: "each non-environmental triage row maps to one fix commit SHA"
      pattern: "Triage Ledger"
---

<objective>
Turn the triaged baseline red into green. Execute the approved triage ledger from Plan 01:
fix each real defect at its root, under the repo's own governance rules, and leave the
frozen JSON contract untouched.

Purpose: VAL-01/02/03 are not "we looked at the gates" — they are "the gates pass".
Phase 2 builds on this package, so a defect left here becomes a defect Phase 2 inherits.
Output: fix commits whose re-run of each gate exits 0, each carrying its version bump and
changelog entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-health-verification/01-CONTEXT.md
@.planning/phases/01-baseline-health-verification/01-PATTERNS.md
@.planning/phases/01-baseline-health-verification/01-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the VAL-02 quality-gate defects in the helpers package</name>
  <files>plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/, plugins/sec-overlay/skills/sec-overlay/helpers/tests/, plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml, plugins/sec-overlay/skills/sec-overlay/helpers/README.md, plugins/sec-overlay/.claude-plugin/plugin.json, plugins/sec-overlay/CHANGELOG.md</files>
  <precondition>01-VERIFICATION.md contains a `## Remediation Route` heading recording the option id the maintainer selected at the Plan 01 checkpoint. If that heading is absent, or if it names `escalate-frozen`, halt.</precondition>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md (the triage ledger and the remediation route — this is the work order)
    - .planning/phases/01-baseline-health-verification/01-CONTEXT.md (D-01 through D-04, D-13, D-14 govern every fix here)
    - .planning/phases/01-baseline-health-verification/01-PATTERNS.md (sections "sec_overlay/*.py fix" and "tests/test_*.py fix" — the style to copy)
    - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/normalize.py (32 lines — the house style analog: module docstring, future annotations, absolute sec_overlay imports, pure typed transform)
    - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_normalize.py (28 lines — the test style analog: plain functions, private builder helper, one behavior per test)
    - plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml (ruff line-length 100, target py312, and the existing narrow exclude lists)
    - plugins/sec-overlay/CHANGELOG.md (the Common Changelog entry shape to append to)
    - plugins/sec-overlay/.claude-plugin/plugin.json (the version field to bump)
    - scripts/hooks/pre-commit-check.sh (the staging rules each fix commit must satisfy)
  </read_first>
  <behavior>
    - After the fixes, `uv run --locked --directory <helpers> pytest -q` reports zero failures except those the ledger dispositioned `environmental`
    - After the fixes, `uv run --locked --directory <helpers> ruff check sec_overlay/ bench/ tests/` exits 0
    - After the fixes, `uv run --locked --directory <helpers> ty check` exits 0
    - A test whose assertion is changed still fails against the pre-fix production code for the reason its commit message states
    - Running the suite twice in a row produces the same result — no fix depends on test ordering or leftover state
  </behavior>
  <action>
Work only the ledger rows whose gate is VAL-02 and whose disposition is `code defect`,
`stale test`, or `config`. Rows dispositioned `environmental` are not touched — in
particular the two known missing-corpus failures stay red and stay documented (D-01 fixes
defects, not the absence of gitignored data).

If the VAL-02 ledger has no actionable rows, do nothing to the source tree: append a
`## VAL-02 Remediation` section to 01-VERIFICATION.md reading `No fix required — gate green
at baseline.` and make no code commit. Both branches end with the same verify.

For each actionable row, work root cause first. Grep every caller of a function before
changing it — a guard in the one shared function is a smaller and more correct diff than a
guard in each caller, and patching only the path the failure named leaves the sibling
callers broken.

Choosing between fixing the test and fixing the production code follows D-03: change the
test only when it asserts stale or wrong behavior, change production code when the test
asserts intended behavior. State the reason in the commit message body — that sentence is
the audit trail for the call.

Match the file you are editing. A module fix keeps the house shape from the analog: a
one-line module docstring, `from __future__ import annotations`, absolute `sec_overlay.`
imports (never a relative path), Google-style docstrings on public functions, and pure
typed transforms with no new abstraction layer. A test fix keeps the analog's shape: a
plain pytest function, a private builder helper, one behavior asserted per function, and a
name that describes the behavior rather than the method under test.

The green must be earned, not manufactured. Do not delete or skip a failing test, do not
add a bare suppression, do not widen the ruff or ty exclude lists to step around a finding,
and do not commit corpus or seed data. A suppression is acceptable only when the finding is
genuinely a false positive and the suppression carries a justification comment on the same
line saying why (D-14).

The frozen JSON-contract modules named in D-02 are out of bounds. Before every commit,
inspect `git diff --cached --name-only` and confirm neither appears. If a fix genuinely
requires one, stop: write the failing receipt and the required change into
01-VERIFICATION.md and escalate to the maintainer rather than proceeding.
<!-- planner-discipline-allow: models.py -->
<!-- planner-discipline-allow: evidence.py -->

Commit each fix separately as `fix(sec-overlay): <imperative summary, under 50 chars>`.
Every commit touching a sec-overlay shipping file bumps the patch version in the plugin
manifest and adds a `### Fixed` bullet to the plugin changelog in that same commit — there
is no batching exception (D-04). A commit touching a file under `helpers/` also stages
`helpers/README.md`, because the hook requires the immediate folder's README. Stage explicit
paths only; never wildcard-stage, never `--no-verify`, never commit on main.
  </action>
  <verify>
    <automated>H=plugins/sec-overlay/skills/sec-overlay/helpers; uv run --locked --directory "$H" ruff check sec_overlay/ bench/ tests/ && uv run --locked --directory "$H" ty check</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ruff check sec_overlay/ bench/ tests/` exits 0
    - `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ty check` exits 0
    - `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers pytest -q` reports zero failures, or reports only failures whose node ids appear as `environmental` rows in the 01-VERIFICATION.md triage ledger
    - `git diff --name-only <phase-start-SHA>..HEAD` lists neither frozen contract module
    - The count of test functions collected by pytest is greater than or equal to the count recorded in the Plan 01 baseline receipt — no test was deleted or skipped to reach green
    - `git diff <phase-start-SHA>..HEAD -- plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml` shows no addition to `extend-exclude` or `[tool.ty.src] exclude`
    - Every newly added `noqa` or `type: ignore` in the diff has a justification comment on the same line
    - `git status --porcelain --ignored | grep bench/corpus_seed` shows the corpus still untracked — no seed data was committed
    - For each fix commit touching a sec-overlay shipping file, `git show --name-only` for that commit lists both `plugins/sec-overlay/.claude-plugin/plugin.json` and `plugins/sec-overlay/CHANGELOG.md`
    - Each commit that changed a test assertion rather than production code has a body sentence stating why the test asserted stale or wrong behavior
  </acceptance_criteria>
  <reversibility rating="one-way">
    Editing a frozen JSON-contract module would break the published contract the Go port
    mirrors byte for byte; undoing it requires a coordinated migration across both
    implementations, not a revert. The task therefore halts at that boundary rather than
    crossing it. The maintainer already confirmed the concrete fix set against this boundary
    at the Plan 01 checkpoint.
  </reversibility>
  <done>The three VAL-02 commands exit 0 (modulo documented environmental failures), and every fix commit carries its version bump, changelog entry, and rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the VAL-01 and VAL-03 governance-surface defects</name>
  <files>.claude-plugin/marketplace.json, plugins/sec-overlay/.claude-plugin/plugin.json, plugins/sec-overlay/CHANGELOG.md, .pre-commit-config.yaml, scripts/hooks/pre-commit-check.sh, scripts/README.md, README.md, CHANGELOG.md</files>
  <precondition>01-VERIFICATION.md contains a `## Remediation Route` heading recording the option id the maintainer selected at the Plan 01 checkpoint. If that heading is absent, or if it names `escalate-frozen`, halt.</precondition>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md (the VAL-01 and VAL-03 triage rows)
    - .planning/phases/01-baseline-health-verification/01-PATTERNS.md (section ".pre-commit-config.yaml / hook script fix" — the error-message shape to match)
    - .claude-plugin/marketplace.json (the manifest the root validation reads)
    - plugins/sec-overlay/.claude-plugin/plugin.json (the manifest the plugin validation reads)
    - .pre-commit-config.yaml (the two configured hook ids)
    - scripts/hooks/pre-commit-check.sh (91 lines — set -euo pipefail, the safe_grep wrapper distinguishing no-match from real failure, and the two-line stderr message pairs)
    - CLAUDE.md (repo root — the governance rules the hooks encode, so a hook fix does not drift from the written policy)
  </read_first>
  <action>
Work the ledger rows whose gate is VAL-01 or VAL-03 and whose disposition is `code defect`
or `config`. If neither gate has an actionable row, append
`## VAL-01 / VAL-03 Remediation — No fix required; both gates green at baseline.` to
01-VERIFICATION.md and make no code commit.

A VAL-01 failure lands in one of two manifests: the marketplace manifest at the repo root or
the sec-overlay plugin manifest. Fix the manifest the validator's own error message names —
read that message, do not guess the schema. If the root validation fails because a
registered plugin entry does not resolve, fix the entry or the path, not the validator's
expectations.

A VAL-03 failure lands either in the hook configuration or in a hook script. A hook-script
fix matches the existing script's shape: `set -euo pipefail`, the `safe_grep` wrapper that
treats "no match" as an expected exit 1 while a real grep failure stays fatal, and errors
written to stderr as an `error:` line followed by a `fix:` line telling the developer what to
do. Keep any hook change aligned with the governance rules written in the root CLAUDE.md —
if the hook and the written policy disagree, that disagreement is the finding, and the fix
brings them back into agreement rather than loosening the hook.

Do not make VAL-03 pass by removing a hook, relaxing a check, or narrowing what the hook
inspects. A hook that no longer enforces the rule is not a passing gate.

A commit here that touches only repo-level files is subject to the hook's repo-level rule:
root `README.md` and root `CHANGELOG.md` must be staged in the same commit. A commit
touching `scripts/` also stages `scripts/README.md`. A commit touching the plugin manifest
is a sec-overlay shipping-file change and therefore bumps the plugin patch version and adds
a plugin changelog entry (D-04).

Use `fix(...)` for a defect and `chore(...)` for a configuration correction, keeping the
subject under 50 characters. Stage explicit paths only; never `--no-verify`, never main.

Before each commit, inspect `git diff --cached --name-only` and confirm the frozen
JSON-contract modules named in D-02 do not appear.
<!-- planner-discipline-allow: models.py -->
<!-- planner-discipline-allow: evidence.py -->
  </action>
  <verify>
    <automated>claude plugin validate . && (cd plugins/sec-overlay && claude plugin validate .) && prek run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `claude plugin validate .` at the repo root exits 0
    - `claude plugin validate .` inside `plugins/sec-overlay/` exits 0
    - `prek run --all-files` from the repo root exits 0
    - `.pre-commit-config.yaml` still declares both hook ids it declared at phase start — no hook was removed to reach green
    - `git diff <phase-start-SHA>..HEAD -- scripts/hooks/` contains no deletion of a check condition without a matching CLAUDE.md governance update in the same commit
    - `.claude-plugin/marketplace.json` still registers every plugin entry it registered at phase start
    - Any commit touching `scripts/` also lists `scripts/README.md`; any commit touching only repo-level files also lists root `README.md` and root `CHANGELOG.md`
    - `git diff --name-only <phase-start-SHA>..HEAD` lists neither frozen contract module
  </acceptance_criteria>
  <reversibility rating="one-way">
    Same frozen-contract boundary as Task 1. No fix on the VAL-01 or VAL-03 surface should
    reach those modules; if one appears to, the task halts and escalates.
  </reversibility>
  <done>Both plugin validation invocations and the repo-wide prek run exit 0, with no hook or manifest weakened to get there.</done>
</task>

</tasks>

<flagged_assumptions>
Carried forward unresolved from Plan 01; neither is auto-dismissed:

| Requirement | Probe | Status | Assumption being made |
|-------------|-------|--------|-----------------------|
| VAL-02 | unclassified — review manually | unresolved | Assumed a maintainer-documented environmental failure (missing gitignored corpus) does not block the gate, while every other failure does. |
| VAL-03 | unclassified — review manually | unresolved | Assumed `prek run --all-files` exiting 0 is the whole of VAL-03, with "hooks are installed" proven by prek executing both configured hook ids. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → published plugin payload | fixes land in files users receive on install |
| agent → governance hook layer | fixes may touch the scripts that enforce the repo's own rules |
| agent → frozen JSON contract | the Go port reads these modules' output byte for byte |
| PyPI / uv resolver → helpers virtualenv | gate re-runs resolve the dev dependency group |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | VAL-02 fix surface | high | mitigate | Acceptance criteria assert the collected test count did not drop, the exclude lists did not grow, every new suppression carries a same-line justification, and the gitignored corpus stayed untracked — so a green reached by weakening the gate fails the task |
| T-01-03 | Tampering | frozen JSON-contract modules | critical | mitigate | Both tasks inspect `git diff --cached --name-only` before every commit, both carry a `one-way` reversibility rating, and the automated verify asserts the phase-start diff lists neither module; a fix requiring one halts and escalates |
| T-01-04 | Elevation of Privilege | governance hooks and hook scripts | high | mitigate | Task 2 forbids removing a hook or relaxing a check to reach green, asserts both hook ids survive, and requires any check removal to ship with a matching CLAUDE.md governance update; commits use explicit paths, never `--no-verify`, never main |
| T-01-02 | Repudiation | fix commit history | high | mitigate | Every shipping-file fix commit must list both the plugin manifest and the plugin changelog in `git show --name-only`, and every test-assertion change must carry a rationale sentence, so the reason for each call survives in history (D-03, D-04) |
| T-01-05 | Tampering | uv dependency resolution | medium | mitigate | Gate re-runs pass `--locked`, so no fix can silently re-resolve or upgrade a dependency; D-09 forbids adding pins this phase |
| T-01-SC | Tampering | package-manager installs | medium | mitigate | This plan installs no packages. Any `uv add`, `uv lock --upgrade`, `npm install`, or equivalent halts and escalates for a package-legitimacy review rather than proceeding |
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Kind | Created by |
|----------|------|-----------|
| Fix commits against the helpers package | modified source and/or test modules under `sec_overlay/` and `tests/`, contingent on the triage ledger | Task 1 |
| `plugins/sec-overlay/.claude-plugin/plugin.json` | patch version bump, one per shipping-file fix commit (D-04) | Tasks 1-2 |
| `plugins/sec-overlay/CHANGELOG.md` | a `### Fixed` entry per shipping-file fix commit | Tasks 1-2 |
| Fix commits against the governance surface | modified `.claude-plugin/marketplace.json`, plugin manifest, `.pre-commit-config.yaml`, or `scripts/hooks/*.sh`, contingent on the triage ledger | Task 2 |
| `## VAL-02 Remediation` / `## VAL-01 / VAL-03 Remediation` sections in 01-VERIFICATION.md | explicit no-fix-required records when a gate was green at baseline | Tasks 1-2 |

The frozen JSON-contract modules are produced by nothing in this plan and must remain
byte-identical to their phase-start state.
</artifacts_this_phase_produces>

<verification>
- All three VAL-02 commands exit 0 against the helpers package, modulo documented environmental failures
- Both VAL-01 plugin validation invocations exit 0
- `prek run --all-files` exits 0
- The frozen contract modules are unchanged since phase start
- No test was deleted or skipped, no exclude list widened, no unjustified suppression added, no corpus data committed
- Every shipping-file fix commit carries its version bump and changelog entry
- Every test-assertion change carries a rationale in its commit message
</verification>

<success_criteria>
Every gate that was red at baseline is green because the defect behind it was fixed at its
root — and a reader of the git history can tell, for each fix, what was wrong and why that
fix was the right call.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-health-verification/01-02-SUMMARY.md` when done
</output>
