---
phase: 04.1-close-gap-diff-04-review-sidecar-workspace-isolation
verified: 2026-08-19T00:00:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 04.1: Close gap: DIFF-04 — review sidecar workspace isolation Verification Report

**Phase Goal:** Reviewing a repo writes nothing into that repo's tracked working tree — `run_review` resolves its workspace through `RepoMemory.for_target(root)` to the `<target>/.sec-overlay/<slug>/` sidecar, exactly as `scan` and `audit` already do, with a regression test pinning the convention.

**Verified:** 2026-08-19
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `run_review` resolves its workspace through `RepoMemory.for_target(root)` so every review artifact lands under `<root>/.sec-overlay/<slug>/`, not the reviewed repo's tracked tree | ✓ VERIFIED | `cli.py:172-176`: `r = runner or subprocess.run`; `memory = RepoMemory.for_target(root, runner=r)`; `memory.ensure(target=root)`; `ws = memory.workspace` — this is the exact `scan`/`audit` analog (`cli.py:411-413`, `440-442`), replacing the prior `Workspace(args.root)` construction |
| 2 | A regression test in `tests/test_cli.py` fails if `run_review`'s workspace root ever reverts to the bare target root | ✓ VERIFIED | `test_review_defaults_to_repo_memory` (test_cli.py:250) asserts `not (target / "artifacts").exists()` and `not (target / "report.md").exists()` plus a positive assertion the manifest exists under the resolved sidecar slug. Ran standalone: `1 passed in 0.78s` |
| 3 | `review_ledger.json` and `report.md` are written under the sidecar, making the receipt ledger a durable non-tracked artifact | ✓ VERIFIED | `test_review_writes_ledger_and_report_with_zero_drops_and_declines` and `test_review_ledger_drop_count_matches_markdown_drop_rows` (test_cli.py) both read `ws = RepoMemory.for_target(str(tmp_path), runner=runner).workspace` then assert on `ws.artifacts / "review_ledger.json"` and `ws.report_path` — no bare-root read remains |
| 4 | `--prepare` mode's `runs/review_prompts/` and `runs/review_plan.json` land under the sidecar | ✓ VERIFIED | `cli.py` prepare branch (`ws.runs / "review_prompts"`) reads through the same `ws` now resolved via `RepoMemory`; `test_review_live.py`'s `_sidecar_ws` helper (line 16-23) rejoins every prepare-mode assertion onto the resolved workspace instead of `tmp_path` |
| 5 | Rule resolution and prompt rendering still address the reviewed repo root, not the sidecar | ✓ VERIFIED | `cli.py:187`: `build_resolution(rule_path, excludes or [], Path(root))` unchanged; `cli.py:252`: `render_review_prompt(..., repo_root=root, ...)` unchanged — confirmed via `rg` and by reading both call sites directly |
| 6 | The full pytest suite shows no new failures against the recorded baseline of 1175 passed / 2 documented environmental failures | ✓ VERIFIED | Ran independently: `2 failed, 1176 passed in 40.25s` — the 2 failures are exactly `test_bench.py::test_seed_corpus_is_valid` and `test_preflight.py::test_report_finds_vendored_rules_regardless_of_cwd`, the documented environmental pair. 1176 = 1175 baseline + 1 new regression test, zero new failures |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `sec_overlay/cli.py` | `run_review` sidecar resolution + corrected `root:` docstring | ✓ VERIFIED | Lines 143-148 docstring now states artifacts live in the resolved per-repo sidecar beneath root, not at root; lines 172-176 hold the sidecar resolution |
| `tests/test_cli.py` | new `test_review_defaults_to_repo_memory` regression test | ✓ VERIFIED | Present at line 250, passes standalone and in full-file run |
| `tests/test_review_live.py`, `test_review_tracer.py`, `test_rule_glob.py` | assertions resolved through the sidecar | ✓ VERIFIED | `rg` for `tmp_path / "artifacts"`, `tmp_path / "runs"`, `tmp_path / "report.md"` across `tests/` returns zero matches; all three files import `RepoMemory` and use a `_sidecar_ws` helper or direct `RepoMemory.for_target(...)` call |
| `plugins/sec-overlay/CHANGELOG.md` entry + `plugin.json` patch bump | version and changelog updated | ✓ VERIFIED | `1.61.5` → `1.61.6` in `plugin.json`; CHANGELOG `1.61.6` entry names DIFF-04 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `run_review` | `RepoMemory.for_target(root, runner=r)` → `.workspace` | direct call at cli.py:172-176 | ✓ WIRED | Single choke point; `scan`/`audit`/`review` and all direct test callers of `run_review` route through it |
| `r = runner or subprocess.run` | `RepoMemory.for_target(..., runner=r)` | keyword threading | ✓ WIRED | `repo_slug`'s `runner` default binds `subprocess.run` at import time; `r` is resolved at call time inside `run_review`, so `monkeypatch.setattr(subprocess, "run", ...)` in `test_review_defaults_to_repo_memory` is actually observed — confirmed by the test passing |
| `build_resolution(rule_path, excludes, Path(root))` / `render_review_prompt(repo_root=root, ...)` | bare target root (unchanged) | direct call | ✓ WIRED (unchanged, correctly) | `rg -n 'build_resolution'` still shows `Path(root)`; `rg -n 'repo_root=root'` still matches — RULE-02 layering and prompt path fidelity preserved |
| Test-side sidecar resolution | same fake runner passed to `run_review`/`main` | `_sidecar_ws(root)` reading `subprocess.run` at call time, or `RepoMemory.for_target(str(tmp_path), runner=runner)` using the test's own explicit runner | ✓ WIRED | Verified by reading `test_review_live.py:16-23`, `test_review_tracer.py:18-25`, `test_rule_glob.py:255,344` — each resolves through the identical runner object the surrounding test hands to production code |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| DIFF-04 | 04.1-01-PLAN.md | Coverage manifest / review artifacts must not leak into the reviewed repo's tracked tree; regression test required | ✓ SATISFIED | Sidecar resolution verified in cli.py; `test_review_defaults_to_repo_memory` pins it; REQUIREMENTS.md already marks DIFF-04 complete (this phase closes the milestone-audit-identified gap in that requirement, not a net-new requirement) |

No orphaned requirements — the plan declares only `[DIFF-04]` and that is the only requirement ID this close-gap phase targets per ROADMAP.md.

### Anti-Patterns Found

None. Grepped `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` across all five code/test files touched by commit `f76432a` — zero matches.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Named regression test passes in isolation | `uv run pytest tests/test_cli.py::test_review_defaults_to_repo_memory -q` | `1 passed in 0.78s` | ✓ PASS |
| Review-affected test files pass together | `uv run pytest tests/test_cli.py tests/test_review_live.py tests/test_review_tracer.py tests/test_rule_glob.py tests/test_diffscope.py -q` | `94 passed in 1.48s` | ✓ PASS |
| Full helpers suite at baseline | `uv run pytest -q` (run once) | `2 failed, 1176 passed in 40.25s` — failures are the documented environmental pair | ✓ PASS |
| Lint/type clean | `uv run ruff check sec_overlay/ bench/ tests/` and `uv run ty check` | `All checks passed!` (both) | ✓ PASS |
| Plugin manifest valid | `claude plugin validate .` | `✔ Validation passed` | ✓ PASS |
| Single governed commit carries all 12 files | `git show f76432a --stat --name-only` | 12 files, one commit, `fix(sec-overlay): review writes to sidecar` | ✓ PASS |
| No `--workspace` flag added to review subparser | read `cli.py:382-398` | `review` subparser has `--base`, `--head`, `--root`, `--profile`, `--rule`, `--exclude`, `--prepare` only | ✓ PASS |
| `test_diffscope.py` unaffected | `git show f76432a --stat --name-only` | file absent from the diff | ✓ PASS |

### Requirements / Doc Verification

- `SKILL.md` diff (commit `f76432a`) adds the sidecar-resolution paragraph and the identical-`--root` discipline note for prepare/dispatch/consume, matching the plan's Task 3 instruction word-for-word in substance.
- `CHANGELOG.md` `1.61.6` entry names the fix and DIFF-04; `plugin.json` version bumped `1.61.5` → `1.61.6` (patch, fix-type — correct per repo semver policy).

## Gaps Summary

None. All six must-have truths, all four required artifacts, and all four key links verified directly against the codebase (not SUMMARY.md claims) at the exact line numbers the plan specified. The full test suite, run independently by the verifier, reproduces the documented baseline exactly (1176 passed / 2 pre-existing environmental failures, zero new failures). `ruff`, `ty`, and `claude plugin validate .` are clean. The single commit `f76432a` carries the production fix, all test rewrites, the doc updates, the CHANGELOG entry, and the version bump together, matching the plan's "one governed commit" requirement.

---

*Verified: 2026-08-19*
*Verifier: Claude (gsd-verifier)*
