---
phase: 05-end-to-end-verification-audit-review
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .gitignore
  - README.md
  - CHANGELOG.md
  - .planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md
  - .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md
autonomous: true
requirements: [AUD-01]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`uv run python -m sec_overlay.preflight` reports the vendored semgrep ruleset present and exits 0 before the audit is driven (RESEARCH Pitfall 2)."
    - "A full `/sec-overlay:audit` run reaches the last entry of `phases.py`'s 22-entry `PHASE_TABLE` on the pinned target HEAD, with a recorded stage for every entry (AUD-01, D-01, D-02, D-03)."
    - "A per-phase receipt exists under the sidecar's `kb/receipts/` for every deterministic stage that ran, written by the `on_complete` closure's `fence()` then `receipt()` sequence."
    - "The working-tree fence holds throughout: the target repo's tracked tree is byte-identical before and after the run (AUD-01)."
    - "The audit runs against the full repository with the tool's default excludes, not a narrowed subtree, so the AUD-05 coverage denominator stays whole (D-03)."
    - "If the run halts mid-pipeline before every `PHASE_TABLE` entry records a stage, the halting phase name and the error are recorded, the halt is triaged as run-blocker or deferred under D-10, and any run-blocker fix ships under full governance (edge E-01)."
  artifacts:
    - .planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md
    - .gitignore
  key_links:
    - "`run.py`'s `drive()` writes `run.env` once and pins the pass SHA; every later stage fences against that pass baseline, so a mid-run re-pin would silently reset the fence."
    - "`preflight` resolves the vendored ruleset at `skills/sec-overlay/helpers/rules/semgrep`; the `scan` verb's `run_semgrep` is the only consumer, so this gap blocks the audit path and not the Plan 01 review path."
    - "`RepoMemory.for_target` routes the audit workspace to the target's `.sec-overlay/<slug>/` sidecar, the same sidecar Plan 01 wrote into."
  prohibitions:
    - "Contents of the target work repository must never enter this repository's git history. No target-repo file path below the repo root, no code snippet, and no finding body may appear in any committed Phase 5 artifact (D-07, one-way)."
    - "A failing success criterion must never be made to disappear by re-running the pipeline, re-scoping the audit, or restating a predicted result as an observed one (D-12)."
    - "Run evidence in the target repo's sidecar must not be deleted, overwritten, or pruned before v5.0 ships through Phase 6 and the milestone audit (D-09)."
    - "Nothing may be committed to, staged in, or deleted from the target work repository's tracked tree. The run is read-only against tracked files."
  flagged_assumptions:
    - "E-02 (unclassified, unresolved): the deterministic edge report did not classify what the fence should do when it detects a working-tree mutation mid-run. Planner assumption: a fence trip is a run-blocker under D-10 because it stops the pass from being trustworthy, so the run halts and the trip is recorded before anything else. Surface for confirmation during execution; do not silently continue past a tripped fence."
    - "The audit writes its sidecar into the target repo as an untracked directory, and the target's `.gitignore` does not list `.sec-overlay` (verified at plan time). The fence checks tracked files, so an untracked sidecar should not trip it — but this is unverified against a real cross-repo run. Confirm during execution and record the observed behaviour."
---

<objective>
Drive a full `/sec-overlay:audit` run end to end against the pinned head of the target
work repository, with per-phase receipts written and the working-tree fence intact.

This is the horizontal expansion of Plan 01's tracer: the same layers — environment,
cross-repo execution, sidecar production, sanitized receipt, defect ledger — applied
to the longer 22-stage audit pipeline. Plans 03 and 04 read this run's output back.

Purpose: satisfies AUD-01 and produces the sidecar every later plan in this phase
inspects.
Output: a sanitized audit run receipt, the vendored ruleset gap closed, and ledger
rows for every gap observed.
</objective>

## Artifacts this phase produces

Files created or modified by this plan:

- `05-02-audit-run-receipt.md` — sanitized receipt for the audit run
- `05-DEFECTS.md` — appended (created by Plan 01)
- `.gitignore` — one ignore entry for the vendored semgrep ruleset
- `README.md`, `CHANGELOG.md` at the repository root — governance updates required by
  a commit touching a path outside `plugins/`

Directories created outside this repository (never copied in, per D-07 and D-08):

- The target repo's `.sec-overlay/<slug>/` sidecar: `run.env`, `kb/receipts/<phase>.json`
  for every deterministic stage, `kb/scan-profile.json`, `findings/`, `artifacts/`

Cloned but never committed:

- `plugins/sec-overlay/skills/sec-overlay/helpers/rules/semgrep/` — a shallow clone of
  the upstream semgrep ruleset, held out of version control by the new ignore entry

No plugin shipping file changes, so no plugin version bump applies. No `tdd="true"`
tasks: this plan runs an existing shipped pipeline and writes Markdown evidence, both
on the documented TDD exception list.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-end-to-end-verification-audit-review/05-CONTEXT.md
@.planning/phases/05-end-to-end-verification-audit-review/05-PATTERNS.md
@.planning/phases/05-end-to-end-verification-audit-review/05-01-SUMMARY.md
@plugins/sec-overlay/commands/audit.md
@plugins/sec-overlay/skills/sec-overlay/SKILL.md
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/run.py
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/phases.py
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/preflight.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the vendored semgrep ruleset gap as a governed run-blocker fix</name>
  <files>.gitignore, README.md, CHANGELOG.md, .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <precondition>Network access to `github.com` is available for a shallow clone. The clone is roughly one hundred megabytes of upstream rule files.</precondition>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/preflight.py` — the rules-directory resolution and the exit-1 condition
    - `.gitignore` at the repository root — the current ignore set
    - `CLAUDE.md` at the repository root, Governance section — the changelog routing and staging rules
  </read_first>
  <reversibility rating="reversible">An ignore entry and an untracked clone are both removable. The rejected alternative — registering the ruleset as a real git submodule — is a larger, more permanent change and belongs in Phase 6.</reversibility>
  <action>
`preflight` exits 1 because the vendored semgrep ruleset directory does not exist.
Verified at plan time: `plugins/sec-overlay/skills/sec-overlay/helpers/rules/` holds
only `README.md` and `smoke.yaml`, and the repository has no `.gitmodules` file. This
blocks the audit's semgrep stage, which makes it a run-blocker under D-10.

Clone the upstream ruleset shallowly into the path `preflight.py` resolves, using
`git clone --depth 1 https://github.com/semgrep/semgrep-rules` targeted at
`plugins/sec-overlay/skills/sec-overlay/helpers/rules/semgrep`.

Before or immediately after the clone, add an ignore entry to the repository root
`.gitignore` covering that directory. Without it, several thousand upstream rule files
become stageable, and the repository's staging rule forbids bulk adds precisely to
prevent that kind of accident. Verify the ignore works by confirming
`git status --porcelain` lists nothing under the rules directory after the clone.

Governance for this commit: the change touches a path outside `plugins/`, so update
the root `CHANGELOG.md` and the root `README.md` in the same commit. No plugin
shipping file changes, so no plugin version bump and no plugin changelog entry apply.
Stage the three paths explicitly; never use a bulk add, and never bypass the hooks.

Record one documentation defect in `05-DEFECTS.md` as `non-blocker` / `deferred`: the
plugin maintainer manual states the semgrep rules are a git submodule, which is false
for this repository — there is no `.gitmodules` and the directory was absent. Phase 6
owns either registering the submodule or correcting the manual.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -m sec_overlay.preflight; echo "exit=$?"</automated>
    <automated>git status --porcelain -- plugins/sec-overlay/skills/sec-overlay/helpers/rules/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m sec_overlay.preflight` exits 0 and its output line for the vendored ruleset reads `OK` rather than `MISSING`.
    - `git status --porcelain -- plugins/sec-overlay/skills/sec-overlay/helpers/rules/` prints `0` lines.
    - `git check-ignore -q plugins/sec-overlay/skills/sec-overlay/helpers/rules/semgrep` exits 0.
    - The commit that lands the ignore entry also stages the root `README.md` and the root `CHANGELOG.md`.
    - `05-DEFECTS.md` gains a row whose Disposition cell reads `deferred` for the submodule documentation defect.
  </acceptance_criteria>
  <done>Preflight reports every tool present and the ruleset vendored, the clone is invisible to git, and the governance files moved with the change.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the audit through every PHASE_TABLE stage on the pinned target head</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <precondition>`git -C /Users/christopher/Documents/Development/_hy/mando status --porcelain` is empty and `git -C /Users/christopher/Documents/Development/_hy/mando rev-parse HEAD` resolves to the head recorded in the Plan 01 receipt. Halt and report if the tracked tree is dirty or the head has moved.</precondition>
  <read_first>
    - `plugins/sec-overlay/commands/audit.md` — the single-repo driver loop and the exact `drive` and `advance` invocations
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/phases.py` — the authoritative 22-entry `PHASE_TABLE`
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/run.py` — `drive()`, `advance()`, and the `on_complete` closure
    - `plugins/sec-overlay/skills/sec-overlay/SKILL.md` — the audit driver and the agent-phase dispatch contract
  </read_first>
  <reversibility rating="costly">The run writes a full sidecar into a repository we do not own, and D-09 forbids removing it until v5.0 ships. Removable in principle, but not before the milestone closes.</reversibility>
  <action>
Drive the single-repo audit to completion against the full target repository at its
pinned head, per D-01, D-02, and D-03.

Record the pinned pass SHA first: run `git -C /Users/christopher/Documents/Development/_hy/mando rev-parse HEAD`
and capture the full SHA before starting. The driver writes `run.env` once and pins
the pass SHA itself; do not re-pin mid-run, because every later stage fences against
that original baseline.

Run the driver from `plugins/sec-overlay/skills/sec-overlay/helpers` exactly as
`audit.md` specifies: `uv run python -c "from sec_overlay.run import drive; print(drive('/Users/christopher/Documents/Development/_hy/mando', config='rules/smoke.yaml'))"`.
Audit the whole repository with the tool's default excludes. Do not pass a narrowing
scope argument — D-03 requires the full AUD-05 coverage denominator, and a narrowed
run would make Plan 04's denominator check meaningless.

The driver alternates between deterministic stages it completes itself and agent
stages it announces. When it prints a `NEXT AGENT PHASE` block, run that agent prompt
so it writes its declared outputs, then close the stage with
`uv run python -c "from sec_overlay.run import advance; advance('/Users/christopher/Documents/Development/_hy/mando', '<phase>')"`,
then re-invoke `drive` to continue. Resume is stage-based, so `drive` picks up at the
first phase with no recorded stage. Repeat until the last `PHASE_TABLE` entry has a
recorded stage.

Use `phases.py`'s `PHASE_TABLE` as the completeness reference, not `repo_memory.py`'s
shorter `PHASES` tuple — the two disagree, and citing the shorter one would understate
the pipeline and produce a false complete.

Two expected non-defects, so neither becomes a ledger row: `factcheck` is a deliberate
soft no-op when the verdicts knowledge-base file is absent, so a missing factcheck
receipt is correct behaviour; and the sidecar directory itself is untracked in the
target, which is not a fence trip.

If the run halts mid-pipeline, record the halting phase name and the error, then
triage under D-10. A crash, hang, or gate false-halt is a run-blocker and gets fixed
under full governance in this phase. Anything else is a `deferred` ledger row. Per
D-12, do not restart the pipeline from scratch to escape a recorded halt.
  </action>
  <verify>
    <automated>python3 -c "import glob,json,os,sys; sd=glob.glob('/Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/kb/receipts'); print('receipt dirs:', sd); print('receipts:', sorted(os.path.basename(p) for p in glob.glob(sd[0]+'/*.json')) if sd else 'NONE')"</automated>
    <automated>test -z "$(git -C /Users/christopher/Documents/Development/_hy/mando status --porcelain --untracked-files=no)" && echo "fence held: tracked tree unchanged"</automated>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -c "from sec_overlay.phases import PHASE_TABLE; print(len(PHASE_TABLE)); print([p[0] if isinstance(p,(list,tuple)) else p for p in PHASE_TABLE])"</automated>
  </verify>
  <acceptance_criteria>
    - Every entry in `phases.py`'s `PHASE_TABLE` has a recorded stage in the target sidecar, or each missing entry is named in `05-DEFECTS.md` with a severity and disposition.
    - `kb/receipts/` in the sidecar contains a JSON receipt for every deterministic stage that ran.
    - `git -C /Users/christopher/Documents/Development/_hy/mando status --porcelain --untracked-files=no` returns empty output after the run.
    - The full pinned pass SHA captured before the run equals the SHA recorded in the sidecar's `run.env`.
    - No narrowing scope argument was passed to `drive`; the invocation matches `audit.md` apart from the target path.
  </acceptance_criteria>
  <done>The audit reached the final `PHASE_TABLE` stage on the pinned head, per-phase receipts exist in the sidecar, and the target's tracked tree is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Author the sanitized audit run receipt and close the ledger rows</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md, .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <read_first>
    - `.planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md` — the receipt format established by the tracer
    - `.planning/phases/05-end-to-end-verification-audit-review/05-PATTERNS.md` — the receipt shape and the sanitization gate
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` — the ledger to append to
  </read_first>
  <reversibility rating="costly">Committing a receipt is one-way for git history (D-07), but the door was opened once under Plan 01's blocking human checkpoint, which approved this exact sanitization format and gate against real content. This task reuses both, so the residual exposure is a costly-to-correct leak rather than an unreviewed one-way step. Flagged, not gated: re-asking would gate a settled decision.</reversibility>
  <action>
Write `05-02-audit-run-receipt.md` in the same Phase 1 evidence format Plan 01
established: the exact copy-pasteable `drive` and `advance` commands, the directory
they ran from, the exit code of each, the decisive output tail rather than a full
dump, and a version block produced by running `uv --version`, `git --version`,
`python --version`, `semgrep --version`, `codeql version`, `ast-grep --version`,
`osv-scanner --version`, and `gitleaks version` at run time. Do not restate the
versions documented in the research file — the receipt follows the actual run.

Record, per D-09, the sidecar path and the statement that its artifacts are retained
untouched until v5.0 ships. Record the full pinned pass SHA, the count of stages with
a recorded stage against the `PHASE_TABLE` total, the list of per-phase receipt file
names present under `kb/receipts/`, the fence result before and after, and the
headline finding counts from the report.

Apply the D-07 sanitization pass before saving: keep commands, exit codes, seal
states, headline counts, gate verdicts, and SHAs; drop every target-repo path below
the repo root, every code snippet, and every finding body. The `.sec-overlay` sidecar
path is permitted and required by D-09.

Append a ledger row to `05-DEFECTS.md` for every gap this run surfaced, each with a
severity, a repro command, a disposition, and a sentence stating why that disposition
was chosen. A run-blocker fixed in Task 1 or Task 2 is `fixed-here` and names its
commit; everything else is `deferred` to Phase 6.
  </action>
  <verify>
    <automated>rg -N '_hy/mando/' .planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md | rg -v '_hy/mando/\.sec-overlay' | wc -l</automated>
    <automated>rg -c 'Exit code' .planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md</automated>
    <automated>rg -c '^\| .* \| (blocker|non-blocker) \| .* \| (fixed-here|deferred) \|' .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The sanitization command above prints `0`. <!-- planner-discipline-allow: _hy/mando/ -->
    - `05-02-audit-run-receipt.md` contains an exit code line, a version block naming at least eight tool versions, the full pinned pass SHA, and the sidecar path.
    - The stage count stated in the receipt is compared against the `PHASE_TABLE` length rather than against a documented figure.
    - `05-DEFECTS.md` has at least one more row than after Plan 01, and every row's Disposition cell reads `fixed-here` or `deferred`.
    - No row in `05-DEFECTS.md` describes a re-run that converted an observed failure into a pass.
  </acceptance_criteria>
  <done>The audit run is evidenced by a sanitized receipt following the Phase 1 format, and every gap it surfaced has a dispositioned ledger row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target work repo → this marketplace repo | Third-party employer source crosses into a repository whose history is permanent |
| this repo's tooling → target work repo tree | A 22-stage pipeline writes a sidecar into a repository we do not own |
| upstream semgrep ruleset → this repo's working tree | Several thousand third-party files land inside a tracked directory |
| shipped audit pipeline → recorded evidence | Real pipeline output crosses into a claim of correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | receipt and ledger authoring in Task 3 | high | mitigate | D-07 sanitization pass before save; automated `rg` gate in Task 3 verify allowing only the `.sec-overlay` sidecar path; format inherited from the gate Plan 01's blocking checkpoint validated |
| T-05-08 | Tampering | the audit run against the target tree | high | mitigate | Task 2 precondition asserts a clean tracked tree; Task 2 verify re-asserts the fence held; the driver's own `fence()` runs before every deterministic stage; prohibition forbids staging or deleting anything in the target |
| T-05-09 | Tampering | the vendored semgrep clone inside a tracked directory | high | mitigate | Root `.gitignore` entry added in the same task as the clone; acceptance criteria assert `git status --porcelain` on that path prints zero lines and `git check-ignore` exits 0; explicit-path staging only, never a bulk add |
| T-05-10 | Repudiation | stage-completeness claim in Task 3 | high | mitigate | The receipt compares against `phases.py`'s `PHASE_TABLE` length computed at run time, not against a documented count and not against `repo_memory.py`'s shorter tuple |
| T-05-11 | Repudiation | a mid-pipeline halt | medium | mitigate | D-12 forbids restarting to escape a recorded halt; the halting phase and error are recorded first, then triaged under D-10; E-02 flagged assumption governs a tripped fence |
| T-05-12 | Elevation of Privilege | a run-blocker fix landing without governance | medium | mitigate | D-10 requires branch, Conventional Commit, changelog, and tests for any fix; Task 1 acceptance criteria assert the governance files moved with the change; hooks are never bypassed |
| T-05-13 | Denial of Service | a long agent-driven pipeline hanging | low | accept | A hang is a run-blocker under D-10 and is visible as a stage that never records; Phase 6 owns any durability work |
</threat_model>

<verification>
- Preflight exits 0 with the vendored ruleset reported present.
- The vendored clone is invisible to `git status` and confirmed by `git check-ignore`.
- Every `PHASE_TABLE` entry has a recorded stage, or each exception is a ledger row.
- Per-phase receipts exist under the sidecar's `kb/receipts/`.
- The target repo's tracked tree is byte-identical before and after the run.
- The sanitization gate reports zero target-repo paths below the repo root in the produced files.
</verification>

<success_criteria>
AUD-01 has real run evidence: a full audit run completed end to end on a real target
repo at a pinned SHA, per-phase receipts were written, the working-tree fence held
throughout, and the one environment run-blocker was closed under full governance.
</success_criteria>

<output>
Create `.planning/phases/05-end-to-end-verification-audit-review/05-02-SUMMARY.md` when done
</output>
