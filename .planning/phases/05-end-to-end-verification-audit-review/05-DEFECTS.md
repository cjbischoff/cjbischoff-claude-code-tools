# Phase 5 Defect Ledger

D-11 format: four columns, each row with a rationale sentence. `Severity` is `blocker` or
`non-blocker`; `Disposition` is `fixed-here` or `deferred`. Sanitized per D-07 — repro
commands carry no target-repo paths below repo root, no code snippets, no finding bodies.

| Defect | Severity | Repro Command | Disposition |
|---|---|---|---|
| D-05 mixing-criterion unsatisfiable: no diff range in the target repo mixes `app/` and `functions/` while also staying within the 5–30 allowlisted-file bound. Rationale: `functions/` has only 5 commits in the repo's entire history (all early infra-migration/router-upgrade commits, none co-occurring with a recent, bounded `app/` change), so no candidate range can satisfy both D-04/D-05's file-count and mixing sub-criteria at once — the mixing sub-criterion is deferred, not the review itself. | non-blocker | `git -C <target-repo-root> log --oneline -- functions/` (5 results total, all pre-dating any candidate diff range) | deferred |
| Real-run reviewer dispatch not exercised: the CLI-only `review` invocation in Task 1/2 never spawns the per-file `review-file` subagent (D-13 assigns that dispatch to `SKILL.md`, not the `review` CLI verb), so both profile runs sealed with 0 live findings and all reviewable files landed in `review_source_skipped` (D-15: a missing agent return is ledgered identically to a stale ref pair or unparseable response — "a reviewer failure, never a coverage failure"). Rationale: AUD-06's completion criteria (`05-RESEARCH.md`) ground the run's done-ness in `CoverageManifest.seal()` and `review_findings.apply_profile()`, not in genuine LLM-authored findings, so this is by design for a tracer proving the pipeline's mechanics, not a coverage gap — full agent-dispatched review is Phase 6 scope. | non-blocker | `uv run python -m sec_overlay.cli review --base <sha> --head <sha> --root <target-repo-root> --profile security` (exit 0, manifest seals `complete`, `review_ledger.json` shows `review_source_skipped: 14`) | deferred |
| `run_review`'s production git-subprocess runner had no `cwd`/`-C` binding to `--root` — every `diffscope.py` git call (which itself issues no `-C <path>`) silently ran against the CLI process's own working directory instead of the target repo, producing an empty changed-file set and a zero-file sealed manifest with no error when invoked from any directory other than `--root`. Discovered running the real pipeline end to end against a live target repo — every existing test mocked the runner and never exercised a real subprocess/cwd mismatch. Rationale: a silent false-empty result on a security review run is a run-blocker (D-10) — it would make every finding vanish without any error signal, not just fail loudly. | blocker | `uv run python -m sec_overlay.cli review --base <sha> --head <sha> --root <target-repo-root> --profile security` run from a directory other than `<target-repo-root>` (pre-fix: 0 changed files found; post-fix: correct changed-file set found) | fixed-here |
| `review`'s resume-identity guard (SCALE-03) rejects a second profile run against the same target's default sidecar, and the `review` subcommand exposes no `--workspace` override (only `audit` does) — running the general profile against the identical SHA range with the same invocation shape as the security run fails with exit `2`, `resume rejected: profile changed from 'security' to 'general'`. Rationale: this is the identity guard working as designed (a resumed run must not silently switch profile mid-review) — it is not itself a defect, but it is a real gap in `review`'s CLI surface for the documented "run every profile against one target" workflow AUD-06 requires; Task 2 worked around it with a `SEC_OVERLAY_HOME` override rather than a code change, so the gap remains for Phase 6 to decide whether `review` should gain a `--workspace` flag like `audit`'s. | non-blocker | `uv run python -m sec_overlay.cli review --base <sha> --head <sha> --root <target-repo-root> --profile general` run against a target whose sidecar already holds a sealed `security`-profile manifest, no `SEC_OVERLAY_HOME`/`--workspace` override supplied (exit `2`) | deferred |
| E-12 flagged assumption: the D-06/D-10 profile-superset contract (security-kept ⊆ general-kept) was only exercised against the vacuous case (both profiles kept 0 findings), so ∅ ⊆ ∅ is a vacuous pass, not a substantive confirmation the contract holds when either profile has live findings. Rationale: Task 1/2's tracer run never dispatched the per-file reviewer (see the row above), so no run in this phase can produce a non-empty finding set to test the contract against a real superset/subset relationship — this must be re-verified in Phase 6 once full agent-dispatched review is exercised. | non-blocker | inspect `review_ledger.json`'s `review_findings` list under both profiles on a diff range where the reviewer actually returns findings (none available in this phase's tracer run) | deferred |
| `plugins/sec-overlay/CLAUDE.md` documents the vendored semgrep ruleset as a git submodule ("Semgrep rules are a git submodule (`helpers/rules/semgrep/`). Clone with `--recurse-submodules`."), but no `.gitmodules` file exists anywhere in this repo's history — the ruleset is a plain gitignored directory populated by a shallow `git clone`, not a submodule. Rationale: this is a documentation-only defect (the fix in this task closes the real gap — vendoring the ruleset content and gitignoring it — without touching the plugin's shipping files), so it is a non-blocker deferred for a maintainer to correct the stale prose in a future plugin doc pass. | non-blocker | `test -f .gitmodules \|\| echo "no .gitmodules — doc claim is false"` (prints the message; no submodule file exists) | deferred |
| `skills/sec-overlay/CLAUDE.md`'s documented "Phase order" list names step 13.5 (Red Team) and step C2 (Postflight) as part of the audit pipeline, but neither is an entry in `phases.py`'s mechanical `PHASE_TABLE`, so `run.drive()`/`run.advance()` never invoke either — they must be run as separate `python -m sec_overlay.redteam` / `python -m sec_overlay.postflight` module calls exactly as the plugin's own maintainer manual documents. The gap surfaces only as a `PhaseHalt` at `artifact-gate` (the deterministic check that hard-requires a red-team plan), not at the point the documented step is actually skipped, which makes the failure harder to diagnose than the missing wiring itself. Rationale: real design/documentation mismatch in a shipped module, not an error in this run's invocation; fixing the wiring (adding `redteam`/`postflight` as real `PHASE_TABLE`/`DETERMINISTIC_ACTIONS` entries, or updating the doc to state they are always manual) is a plugin-code change outside this phase's remit, so it is deferred to Phase 6. | non-blocker | `uv run python -c "from sec_overlay.phases import PHASE_TABLE; print([p[0] if isinstance(p,(list,tuple)) else p for p in PHASE_TABLE])"` (prints 22 names, no `redteam`, no `postflight`) run from `plugins/sec-overlay/skills/sec-overlay/helpers` | deferred |
| `agents/redteam.md`'s prose describes a 3-way discriminator per finding (`static-settled` / `needs-runtime` / a third "neither" case handled via `open_questions` alone), but `redteam.py`'s `wants_runtime(f)` implements only a 2-way mechanical split: `f.runtime_disposition == "needs-runtime" OR f.status is FindingStatus.NEEDS_DEPLOYMENT_TESTING`. Setting `runtime_disposition = "unassessed"` on a finding whose `status` is already `NEEDS_DEPLOYMENT_TESTING` does not exempt it — the status alone forces inclusion, so the module cannot represent the prose's "neither" case for any finding already carrying that status. Discovered live: a finding marked `unassessed` still entered the `needs_runtime` bucket and would have rendered an empty/placeholder directive had the producer role not corrected the disposition and written a genuine directive instead. Rationale: this is a real prose-vs-mechanics gap in the shipped redteam module, not an authoring error in this run once corrected; reconciling the prose with the mechanical predicate (or vice versa) is a plugin-code change outside this phase's remit. | non-blocker | inspect `redteam.py`'s `wants_runtime()` against `agents/redteam.md`'s "static-settled / needs-runtime / neither" description for a finding whose `status` is `needs-deployment-testing` and whose `runtime_disposition` is set to `unassessed` (module still routes it into `needs_runtime`) | deferred |
| A shipping finding's rendered `findings/<ID>.md` detail file has a cosmetic template-rendering defect on its deps-class "Fix." line: the package-name interpolation renders empty (an empty pair of backticks where the package name belongs), rather than naming the affected package. Surfaced by the `artifact-review` role's claim-to-evidence pass, which correctly recognised this is not a severity/evidence mismatch (the underlying finding JSON is accurate) and therefore not actionable under the safety contract's three permitted actions (demote, force-rerender, open_questions) — carried here instead. Rationale: a template/rendering bug in the deps-finding-detail template, not a false claim; fixing the template is a plugin-code change outside this phase's remit. | non-blocker | render any deps-class finding's detail file via the `report` phase and inspect its "Fix." line for an empty backtick-delimited package name | deferred |
| 05-REVIEW.md WR-01: `run_review` raises an unhandled `FileNotFoundError` when `--root` names a nonexistent directory, a regression against the CLI's convention of a clean exit-2 error message. Rationale: maintainer decision (2026-08-21 UAT session) records that this rides with Phase 6's REL-01 defect-disposition sweep rather than a Phase 5 fix — it is an error-message quality defect, not a correctness or coverage defect in any verified run. | non-blocker | `uv run python -m sec_overlay.cli review --base <sha> --head <sha> --root /nonexistent-path --profile security` (raises `FileNotFoundError` traceback instead of a clean exit-2 message) | deferred |
| 05-REVIEW.md WR-02: `helpers/tests/README.md` claims the other tests missed the cwd-scoping bug because they "bypass `main()`", but the actual cause is that they mock the git runner, so no real subprocess ever ran against the wrong working directory. Rationale: maintainer decision (2026-08-21 UAT session) records that this documentation-accuracy defect rides with Phase 6's REL-01 defect-disposition sweep. | non-blocker | read `helpers/tests/README.md`'s explanation of the cwd bug against the mocked-runner test fixtures it describes | deferred |
