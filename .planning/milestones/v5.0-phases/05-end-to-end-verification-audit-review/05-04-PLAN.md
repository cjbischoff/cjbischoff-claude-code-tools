---
phase: 05-end-to-end-verification-audit-review
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - .planning/phases/05-end-to-end-verification-audit-review/05-04-artifact-coverage-receipt.md
  - .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md
autonomous: true
requirements: [AUD-04, AUD-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The architecture gate and the threat-model gate both recorded a verdict for this run, and each verdict object's `passed` field is readable from the sidecar (AUD-04)."
    - "The architecture and threat-model artifacts satisfy the deterministic gates: Mermaid node and edge caps, derivation headers, and STE lint (AUD-04)."
    - "Every CVSS vector in the threat-model and finding artifacts starts with `CVSS:4.0/`; `cvss.py::_parse()` raises `ValueError` on a `CVSS:3` prefix, so a surviving v3.1 vector means the artifact bypassed scoring (AUD-04, edge E-08)."
    - "The audit report states its coverage denominator as an explicit number, not as a qualitative claim (AUD-05)."
    - "Every attack-surface class with zero findings has a logged entry in the coverage ledger built by `coverage_ledger.build_coverage_ledger()`, and `validate_coverage_ledger()` accepts the result (AUD-05)."
    - "The ledger's `completeness` field is read as observed; a value other than `complete` is recorded with the surfaces still marked `needs_follow_up`, since `complete` is impossible while any surface awaits follow-up (edge E-09)."
    - "If a gate artifact is absent from the sidecar, its absence is recorded as an unproven criterion rather than an implied pass (edge E-07)."
    - statement: "If an attack-surface class has zero findings and no coverage-ledger entry, the class name and the empty-entry condition are stated in the receipt and the gap becomes a deferred ledger row."
      verification: backstop
  artifacts:
    - .planning/phases/05-end-to-end-verification-audit-review/05-04-artifact-coverage-receipt.md
  key_links:
    - "`coverage_ledger.validate_coverage_ledger()` is the authoritative acceptance check; a hand-rolled ledger walk would duplicate and eventually disagree with it."
    - "`cvss.py::_parse()` is the only place the v4.0-only rule is enforced, so scanning artifacts for a `CVSS:3` prefix tests whether anything reached an artifact without passing through it."
    - "`factcheck` is a deliberate soft no-op when the verdicts knowledge-base file is absent, so a missing factcheck receipt is not a gate failure."
  prohibitions:
    - "Contents of the target work repository must never enter this repository's git history. No target-repo file path below the repo root, no code snippet, and no finding body may appear in any committed Phase 5 artifact (D-07, one-way)."
    - "A failing success criterion must never be made to disappear by re-running the pipeline, re-scoping the audit, or restating a predicted result as an observed one (D-12)."
    - "Run evidence in the target repo's sidecar must not be deleted, overwritten, or pruned before v5.0 ships through Phase 6 and the milestone audit (D-09)."
---

<objective>
Read the audit run's architecture, threat-model, and coverage artifacts back from the
live sidecar and prove they pass the deterministic gates, score with CVSS v4.0 only,
state an explicit coverage denominator, and log a ledger entry for every
attack-surface class that produced no finding.

This is the last expansion from Plan 02's audit run. It closes the two remaining
requirements and leaves the phase ready for verification against all six criteria.

Purpose: satisfies AUD-04 and AUD-05 against real artifacts rather than fixtures.
Output: a sanitized artifact-and-coverage receipt and ledger rows for every gap.
</objective>

## Artifacts this phase produces

Files created or modified by this plan:

- `05-04-artifact-coverage-receipt.md` — sanitized receipt for the AUD-04 and AUD-05 readbacks
- `05-DEFECTS.md` — appended and finalized (created by Plan 01)

Artifacts read but never copied in (D-07, D-08):

- The target repo's sidecar `kb/receipts/arch-gate.json` and `kb/receipts/tm-gate.json`
  verdict objects
- The target repo's sidecar `artifacts/ARCHITECTURE.md` and `artifacts/THREAT_MODEL.md`
- The target repo's sidecar `kb/coverage-ledger.json` and the report's coverage
  denominator statement

Downstream artifact this plan prepares inputs for, produced by `/gsd-verify-work` and
not by this plan:

- `05-VERIFICATION.md` — the six-criteria verdict table, whose Evidence cells cite the
  sidecar paths and fields this plan's receipt names

No plugin source symbols are created. No `tdd="true"` tasks: this plan reads existing
artifacts and writes Markdown evidence, both on the documented TDD exception list.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-end-to-end-verification-audit-review/05-CONTEXT.md
@.planning/phases/05-end-to-end-verification-audit-review/05-PATTERNS.md
@.planning/phases/05-end-to-end-verification-audit-review/05-03-SUMMARY.md
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/coverage_ledger.py
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cvss.py
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/artifact_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the deterministic artifact gates and the CVSS v4.0-only rule (AUD-04)</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/artifact_gate.py` — the gate entry point and its verdict shape
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cvss.py` — `_parse()` and the `CVSS:4.0/` prefix requirement
    - `.planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md` — the sidecar path and the recorded stage list
  </read_first>
  <action>
Read both gate verdicts and scan every scored artifact for a non-v4.0 vector.

Locate `kb/receipts/arch-gate.json` and `kb/receipts/tm-gate.json` in the sidecar
recorded in the Plan 02 receipt, and read each verdict object's `passed` field. Record
both values as observed. If either file is absent, record the criterion as unproven
and name the missing file — an absent gate artifact is not an implied pass, and
treating it as one is the exact failure this check exists to catch. Distinguish this
from the known soft no-op: `factcheck` deliberately writes no receipt when the
verdicts knowledge-base file is absent, so a missing factcheck receipt is correct
behaviour and not a gate failure.

For the deterministic checks themselves — Mermaid node and edge caps, derivation
headers, and STE lint — re-run the shipped gate module against the produced artifacts
rather than eyeballing them, so the receipt carries a reproducible command and an exit
code. `artifact_gate` and `ste_lint` are both CLI-callable as
`python -m sec_overlay.<module>`.

For the CVSS rule, scan the threat-model artifact and the finding JSON files for any
vector string and confirm every one starts with `CVSS:4.0/`. `cvss.py::_parse()` raises
on a `CVSS:3` prefix and on anything not starting with the v4.0 prefix, so a surviving
v3.1 vector in an artifact means that artifact reached its file without passing through
scoring. Report the total vector count and the count of non-v4.0 vectors; the second
must be zero.

Record only counts, verdict booleans, file names, and exit codes. Do not quote
artifact prose, diagram bodies, or threat descriptions — those are target-repo derived
content under D-07.

Per D-12, a failing gate verdict or a surviving v3.1 vector is a `deferred` ledger row
under D-10 unless it halted the run, and is never a reason to re-run.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -c "
import glob, json, os
for name in ('arch-gate', 'tm-gate'):
    hits = glob.glob(f'/Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/kb/receipts/{name}.json')
    if not hits:
        print(name, 'MISSING'); continue
    d = json.load(open(hits[0]))
    print(name, 'passed=', d.get('passed'))
"</automated>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -c "
import glob, re
pat = re.compile(r'CVSS:[0-9]\.[0-9]/')
files = glob.glob('/Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/artifacts/*.md') + glob.glob('/Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/findings/*.json')
tot = bad = 0
for f in files:
    for m in pat.findall(open(f, errors='ignore').read()):
        tot += 1
        if not m.startswith('CVSS:4.0/'): bad += 1
print('vectors', tot); print('non_v4_vectors', bad)
"</automated>
  </verify>
  <acceptance_criteria>
    - The first command prints a `passed=` value for both gate names, or prints `MISSING` and that name has a matching row in `05-DEFECTS.md`.
    - The second command prints `non_v4_vectors 0`, or the non-zero count has a matching `deferred` row in `05-DEFECTS.md`.
    - The receipt records both gate `passed` booleans as observed, never inferred from the absence of an error.
    - The receipt states the total vector count alongside the non-v4.0 count, so a zero total is distinguishable from a clean pass.
    - The receipt contains no artifact prose, diagram body, or threat description text; only counts, booleans, file names, and exit codes.
  </acceptance_criteria>
  <done>Both gate verdicts are recorded as observed values, the deterministic checks were re-run with a reproducible command and exit code, and every CVSS vector in the run's artifacts has been counted and checked against the v4.0-only rule.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the coverage denominator and the coverage ledger (AUD-05)</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/coverage_ledger.py` — `build_coverage_ledger()`, `validate_coverage_ledger()`, the `completeness` semantics
    - `.planning/phases/05-end-to-end-verification-audit-review/05-02-audit-run-receipt.md` — the sidecar path and the confirmation that no narrowing scope was passed
  </read_first>
  <action>
Check that the report names an explicit denominator and that every empty attack-surface
class is logged.

Read the audit report in the sidecar's `artifacts/` directory and locate its coverage
denominator statement. AUD-05 requires an explicit number — the count of files, or
surfaces, the audit considered. A qualitative claim such as a description of breadth
does not satisfy it. Record the exact denominator figure and the label the report gives
it. Confirm it reflects a whole-repository audit under default excludes, consistent
with D-03 and with the Plan 02 receipt's statement that no narrowing scope argument was
passed; a denominator smaller than the repository would mean the run silently narrowed.

Read `kb/coverage-ledger.json` from the sidecar and run the shipped
`validate_coverage_ledger()` against it. Do not hand-roll a ledger walk —
`coverage_ledger.py` owns the acceptance rules, and a parallel implementation would
eventually disagree with the pipeline it audits. Record the validator's result and the
ledger's `completeness` field exactly as observed. `completeness` cannot be `complete`
while any surface is `needs_follow_up`, so if the observed value is `complete` also
record the count of surfaces still marked `needs_follow_up` — a `complete` value
alongside a non-zero follow-up count is a contradiction worth a ledger row.

Cross-check the two sides: for every attack-surface class the run enumerated that
produced zero findings, confirm a ledger entry exists. Report the count of
zero-finding classes and the count of those without an entry; the second must be zero.
Name any class in the second group by its class label, which is tool-side taxonomy and
safe under D-07.

Per D-12, a missing denominator, a failed validator, a contradictory `completeness`
value, or an unlogged empty class is a `deferred` ledger row under D-10, and never a
reason to re-run.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -c "
import glob, json
from sec_overlay.coverage_ledger import validate_coverage_ledger
hits = glob.glob('/Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/kb/coverage-ledger.json')
if not hits:
    print('coverage-ledger MISSING')
else:
    d = json.load(open(hits[0]))
    print('completeness', d.get('completeness'))
    entries = d.get('entries') or d.get('surfaces') or []
    print('entries', len(entries))
    print('needs_follow_up', sum(1 for e in entries if isinstance(e, dict) and e.get('status') == 'needs_follow_up'))
    try:
        print('validate', validate_coverage_ledger(d))
    except Exception as exc:
        print('validate RAISED', type(exc).__name__, exc)
"</automated>
    <automated>rg -n -i 'denominator|files considered|surfaces considered' /Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/artifacts/report.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - The first command prints a `completeness` value and a `validate` result, or prints `MISSING` and that absence has a matching row in `05-DEFECTS.md`.
    - The second command returns at least one line containing an explicit numeric denominator, or its absence is a `deferred` ledger row.
    - The receipt states the denominator figure, the entry count, the `needs_follow_up` count, and the validator result, each read from the sidecar rather than from the research file.
    - If `completeness` reads `complete` while the `needs_follow_up` count is non-zero, the contradiction is stated in the receipt and ledgered.
    - The count of zero-finding attack-surface classes without a ledger entry is stated, and any such class is named by its class label only.
  </acceptance_criteria>
  <done>The report's coverage denominator is recorded as an explicit number, the ledger has been validated with the shipped validator, and every zero-finding attack-surface class either has an entry or a ledger row naming it.</done>
</task>

<task type="auto">
  <name>Task 3: Author the sanitized artifact-and-coverage receipt and finalize the ledger</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-04-artifact-coverage-receipt.md, .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <read_first>
    - `.planning/phases/05-end-to-end-verification-audit-review/05-03-finding-integrity-receipt.md` — the established receipt format
    - `.planning/phases/05-end-to-end-verification-audit-review/05-PATTERNS.md` — the receipt shape, the sanitization gate, and the downstream verification-report structure
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` — the ledger to finalize
  </read_first>
  <reversibility rating="costly">Committing a receipt is one-way for git history (D-07), but the door was opened once under Plan 01's blocking human checkpoint, which approved this exact sanitization format and gate against real content. This task reuses both, so the residual exposure is a costly-to-correct leak rather than an unreviewed one-way step. Flagged, not gated: re-asking would gate a settled decision.</reversibility>
  <action>
Write `05-04-artifact-coverage-receipt.md` in the Phase 1 evidence format the earlier
receipts established: the exact copy-pasteable readback commands from Tasks 1 and 2,
the directory they ran from, their exit codes, the decisive output tail rather than a
full dump, and a version block captured at run time.

State the AUD-04 result as both gate `passed` booleans, the deterministic gate command
exit code, the total vector count, and the non-v4.0 vector count. State the AUD-05
result as the denominator figure, the ledger entry count, the `needs_follow_up` count,
the validator result, and the count of unlogged zero-finding classes.

Per D-08, cite what a verifier should check in the live sidecar and where, for all six
phase criteria and not only this plan's two: the sidecar path, the gate receipt files
and their `passed` fields, the coverage ledger file and its `completeness` field, the
report headline counts block, the per-finding `evidence_sources` field, and the review
coverage manifest `seal` field. The downstream verification report consumes these
citations, so each Evidence cell it writes must be traceable to a path and a field named
here. Do not copy sidecar content in.

Apply the D-07 sanitization pass before saving: keep commands, exit codes, seal states,
headline counts, gate verdicts, and SHAs; drop every target-repo path below the repo
root, every code snippet, and every finding body. Class labels, gate names, and field
names are tool-side taxonomy and are safe to keep. The `.sec-overlay` sidecar path is
permitted and required by D-09.

Finalize `05-DEFECTS.md`: append a row for every gap Tasks 1 and 2 surfaced, then read
the whole ledger and confirm every row carries a severity, a repro command, a
disposition, and a rationale sentence. Confirm no row describes a re-run that converted
an observed failure into a pass. Record in the receipt that the sidecar is retained
untouched until v5.0 ships, per D-09.
  </action>
  <verify>
    <automated>rg -N '_hy/mando/' .planning/phases/05-end-to-end-verification-audit-review/05-04-artifact-coverage-receipt.md .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md | rg -v '_hy/mando/\.sec-overlay' | wc -l</automated>
    <automated>rg -N '_hy/mando/' .planning/phases/05-end-to-end-verification-audit-review/05-*-receipt.md .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md .planning/phases/05-end-to-end-verification-audit-review/05-0*-PLAN.md | rg -v '_hy/mando/\.sec-overlay' | wc -l</automated>
    <automated>rg -c '^\| .* \| (blocker|non-blocker) \| .* \| (fixed-here|deferred) \|' .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Both sanitization commands print `0`. <!-- planner-discipline-allow: _hy/mando/ -->
    - The receipt contains an exit code line, a version block, both AUD-04 gate booleans, the two vector counts, and the five AUD-05 figures.
    - The receipt names a sidecar path and an exact field for each of the six phase success criteria, so every downstream Evidence cell is traceable.
    - Every row in `05-DEFECTS.md` has a severity, a repro command, a disposition, and a rationale sentence.
    - `05-DEFECTS.md` contains no row describing a re-run that converted an observed failure into a pass.
  </acceptance_criteria>
  <done>AUD-04 and AUD-05 are evidenced by a sanitized receipt with reproducible readback commands, the defect ledger is complete and internally consistent, and every one of the six phase criteria has a named sidecar path and field for the downstream verifier to check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target work repo sidecar → this marketplace repo | Architecture, threat-model, and coverage data from a third-party codebase crosses into a permanent history |
| real artifact output → a correctness claim | Gate verdicts and coverage figures cross into a requirement verdict |
| this phase's receipts → the downstream verification report | Citations cross into the record that closes the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Information Disclosure | architecture and threat-model artifact readback in Task 1 | high | mitigate | Only counts, booleans, file names, and exit codes leave the sidecar; an acceptance criterion forbids artifact prose, diagram bodies, and threat descriptions in the receipt |
| T-05-22 | Information Disclosure | receipt and ledger authoring in Task 3 | high | mitigate | D-07 sanitization pass before save; two automated `rg` gates in Task 3 verify, the second covering every Phase 5 file including the plans themselves, allowing only the `.sec-overlay` sidecar path |
| T-05-23 | Repudiation | an absent gate artifact read as a pass | high | mitigate | Edge E-07 truth requires recording an absent gate artifact as unproven; the Task 1 verification prints `MISSING` explicitly rather than failing silently |
| T-05-24 | Tampering | a CVSS v3.1 vector reaching an artifact without passing through scoring | high | mitigate | Task 1 scans every artifact and finding file for any vector prefix and reports the non-v4.0 count separately from the total, so a zero total cannot be mistaken for a clean pass |
| T-05-25 | Repudiation | a `complete` coverage ledger hiding surfaces awaiting follow-up | high | mitigate | Task 2 reads `completeness` and the `needs_follow_up` count together and requires the contradiction be stated and ledgered; the shipped `validate_coverage_ledger()` is the acceptance authority |
| T-05-26 | Spoofing | a narrowed audit producing a flattering denominator | medium | mitigate | Task 2 cross-checks the denominator against the Plan 02 receipt's statement that no narrowing scope argument was passed, per D-03 |
| T-05-27 | Spoofing | a hand-rolled ledger walk disagreeing with the pipeline | medium | mitigate | The verification command imports `coverage_ledger.validate_coverage_ledger` directly; the task action forbids a parallel implementation |
| T-05-28 | Tampering | sidecar artifacts modified or pruned during readback | medium | mitigate | D-09 retention prohibition; all readback commands are read-only, opening files without writing |
</threat_model>

<verification>
- Both gate verdicts are read as observed values, or their absence is recorded as unproven.
- The deterministic gate command ran with a recorded exit code rather than a visual inspection.
- The total and non-v4.0 CVSS vector counts are both stated.
- The coverage denominator is an explicit number and is consistent with a whole-repository audit.
- The shipped coverage-ledger validator ran and its result is recorded alongside the `completeness` and `needs_follow_up` figures.
- The sanitization gate reports zero target-repo paths below the repo root across every Phase 5 file, plans included.
- Every ledger row carries a severity, a repro command, a disposition, and a rationale sentence.
</verification>

<success_criteria>
AUD-04 and AUD-05 have real artifact evidence: the architecture and threat-model gates
recorded verdicts, the deterministic checks were re-run reproducibly, every CVSS vector
is v4.0, the report states an explicit coverage denominator over the whole repository,
and the coverage ledger validates with every zero-finding attack-surface class either
logged or ledgered as a gap. The defect ledger is complete and the six criteria each
have a named sidecar path and field for the downstream verifier.
</success_criteria>

<output>
Create `.planning/phases/05-end-to-end-verification-audit-review/05-04-SUMMARY.md` when done
</output>
