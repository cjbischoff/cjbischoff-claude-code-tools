---
phase: 05-end-to-end-verification-audit-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md
  - .planning/phases/05-end-to-end-verification-audit-review/05-01-review-general-receipt.md
  - .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md
autonomous: false
requirements: [AUD-06]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `review --profile security` run against the pinned mando SHA range exits 0 and writes a coverage manifest whose `seal` field is present in `artifacts/coverage_manifest.json` (AUD-06, D-04, D-05)."
    - "A `review --profile general` run on the identical `base..head` range exits 0 and produces its own sealed manifest (AUD-06, D-06)."
    - "Every reported line in both runs is positioning-confirmed: no reported finding carries a declined `PositionResult` (AUD-06)."
    - "The security profile's kept-finding set is a subset of the general profile's kept set on the same range, evidencing the Phase 3 D-10 profile-superset contract on real code (D-06)."
    - "If `CoverageManifest.seal()` raises `CoverageTransitionError` because an entry is not `done`, the raised error and the offending entry state are recorded as a ledger row, not retried away (edge E-11, D-12)."
    - statement: "If either run produces zero reviewable files after exclusions, the receipt records the zero-reviewable early-return path and the unsealed manifest state rather than reporting a vacuous pass."
      verification: backstop
  artifacts:
    - .planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md
    - .planning/phases/05-end-to-end-verification-audit-review/05-01-review-general-receipt.md
    - .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md
  key_links:
    - "`cli.py review --root` resolves the mando workspace through `RepoMemory.for_target`, so both runs must land in the same `.sec-overlay/<slug>/` sidecar (DIFF-04, Phase 04.1)."
    - "Both profile runs must pin the identical `base..head` SHA pair; a drifting head silently invalidates the superset comparison."
  prohibitions:
    - "Contents of the target work repository must never enter this repository's git history. No target-repo file path below the repo root, no code snippet, and no finding body may appear in any committed Phase 5 artifact (D-07, one-way)."
    - "A failing success criterion must never be made to disappear by re-running the pipeline, re-scoping the diff, or restating a predicted result as an observed one (D-12)."
    - "Nothing may be committed to, staged in, or deleted from the target work repository's tracked tree. The run is read-only against tracked files."
  flagged_assumptions:
    - "E-12 (unclassified, unresolved): the deterministic edge report did not classify what happens when the general profile returns a finding the security profile dropped — a direct superset violation. Planner assumption: this is a real-output defect and therefore a `deferred` ledger row under D-12, not a run-blocker under D-10. Surface for confirmation during execution; do not silently treat it as a pass."
---

<objective>
Prove the diff-review pipeline end to end on a real historical diff from the target
work repository, in both profiles, with the coverage manifest sealed and every
reported line positioning-confirmed.

This is the phase tracer. It is the thinnest path that touches every layer Phase 5
uses — environment, cross-repo pipeline execution, sidecar artifact production,
sanitized receipt authoring, and the defect ledger — in one runnable slice. The
audit pipeline (Plan 02) and the audit-output readbacks (Plans 03, 04) expand
outward from the receipt format and sanitization gate this plan establishes.

Purpose: satisfies AUD-06 and lands the evidence-capture discipline every later
plan reuses.
Output: two sanitized run receipts and the phase defect ledger.
</objective>

## Artifacts this phase produces

Files created by this plan:

- `05-01-review-security-receipt.md` — sanitized receipt for the security-profile run
- `05-01-review-general-receipt.md` — sanitized receipt for the general-profile run
- `05-DEFECTS.md` — the phase defect ledger (D-11), created here and appended by
  Plans 02, 03, and 04

Files created by later plans in this phase:

- `05-02-audit-run-receipt.md` (Plan 02)
- `05-03-finding-integrity-receipt.md` (Plan 03)
- `05-04-artifact-coverage-receipt.md` (Plan 04)

Artifacts written outside this repository (never copied in, per D-07 and D-08):

- The target repo's `.sec-overlay/<slug>/` sidecar: `artifacts/coverage_manifest.json`,
  `review_ledger.json`, `findings/`, `kb/`

No plugin source symbols are created by this plan. No `tdd="true"` tasks appear here:
every task produces Markdown evidence or runs an existing shipped CLI, both of which
are on the documented TDD exception list.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-end-to-end-verification-audit-review/05-CONTEXT.md
@.planning/phases/05-end-to-end-verification-audit-review/05-PATTERNS.md
@.planning/phases/01-baseline-health-verification/01-CONTEXT.md
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/review_coverage.py
@plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/review_findings.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end security-profile review on a real diff — one path only</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md, .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <precondition>The target repository is readable at `/Users/christopher/Documents/Development/_hy/mando`, `git -C /Users/christopher/Documents/Development/_hy/mando status --porcelain` is empty, and `git -C /Users/christopher/Documents/Development/_hy/mando rev-parse --short HEAD` returns `80e2abc`. Halt and report if the tracked tree is dirty or the head has moved.</precondition>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py` lines 600-660 — the `review` subparser flags
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/review_coverage.py` — `CoverageManifest`, `seal()`, `check_resume_identity()`
    - `.planning/phases/01-baseline-health-verification/01-CONTEXT.md` D-05..D-08 — the receipt format this file must follow
    - `.planning/phases/05-end-to-end-verification-audit-review/05-PATTERNS.md` — receipt shape and the sanitization gate
  </read_first>
  <reversibility rating="reversible">This task writes working-tree files and runs a read-only pipeline; nothing enters git history here. The one-way step is the commit, and Task 3's blocking checkpoint gates it. No decision checkpoint is inserted before this task: D-07 already locked sanitized-receipts-only, so re-asking would gate a settled decision.</reversibility>
  <action>
Resolve the diff range, run one security-profile review end to end, and record it.

Diff selection (D-05, Claude's Discretion, resolved at plan time): base `5f477d8`,
head `d06ce30` — a merged pull request touching 15 files, 13 of them TS/TSX, with a
largest single-file change of 402 changed lines, well inside `file_select.py`'s
`DEFAULT_MAX_DIFF_LINES = 5000`. Re-resolve both short SHAs to full SHAs with
`git -C <target> rev-parse` at run time and record both full SHAs in the receipt.

Record one deviation from D-05 explicitly. D-05 asks for a range that mixes the
application directory and the serverless-functions directory. No such range exists:
`git -C <target> log --oneline -50 -- functions/` returns exactly five commits, each
either roughly 111 files or predating the current layout, so no candidate satisfies
both the 5-30 file bound and the mixing sub-criterion. Per D-12, log the
mixing-criterion deviation as a ledger row rather than loosening the file-count bound
or silently exceeding the cap. Fallback candidates if `5f477d8..d06ce30` fails to
produce reviewable files: `9d4042e..0e98a2b` (6 TS files) or `30dc08e..321d0f1`
(25 TS files, 28 total).

Run the security profile from `plugins/sec-overlay/skills/sec-overlay/helpers`:
`uv run python -m sec_overlay.cli review --base <full-base-sha> --head <full-head-sha>
--root /Users/christopher/Documents/Development/_hy/mando --profile security`.
Leave `--concurrency`, `--timeout`, and `--max-git-procs` at their shipped defaults
(8, 600, 16) so the receipt evidences default behaviour.

Write `05-01-review-security-receipt.md` in the Phase 1 evidence format: the exact
copy-pasteable command, the directory it ran from, the exit code, the decisive output
tail rather than a full dump, and a version block produced by running `uv --version`,
`git --version`, and `python --version` at run time instead of restating documented
values. Record the sidecar path per D-09, the two full SHAs, the manifest `seal`
value, the reviewable and excluded file counts, the kept and dropped finding counts,
and the count of positioning declines.

Apply D-07 to every line before saving: keep commands, exit codes, seal states,
headline counts, gate verdicts, and SHAs; drop every target-repo path below the repo
root, every code snippet, and every finding body. The sidecar path itself is
permitted and required by D-09.

Create `05-DEFECTS.md` with the D-11 four-column ledger (Defect, Severity, Repro
Command, Disposition) using severities `blocker` and `non-blocker` and dispositions
`fixed-here` and `deferred`, each row followed by a sentence stating why that
disposition was chosen. Seed it with the D-05 mixing-criterion deviation as
`non-blocker` / `deferred`, plus any gap this run surfaced. A criterion that fails on
real output is a `deferred` row under D-12; do not re-run to make it disappear.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -m sec_overlay.cli review --base "$BASE_SHA" --head "$HEAD_SHA" --root /Users/christopher/Documents/Development/_hy/mando --profile security; echo "exit=$?"</automated>
    <automated>test -s /Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/artifacts/coverage_manifest.json && python3 -c "import glob,json;p=glob.glob('/Users/christopher/Documents/Development/_hy/mando/.sec-overlay/*/artifacts/coverage_manifest.json')[0];d=json.load(open(p));print('seal',d['seal']);print('base',d['base_sha']);print('head',d['head_sha'])"</automated>
    <automated>test -z "$(git -C /Users/christopher/Documents/Development/_hy/mando status --porcelain --untracked-files=no)" && echo "target tracked tree unchanged"</automated>
  </verify>
  <acceptance_criteria>
    - The security-profile `review` command exits 0, or a non-zero exit is recorded verbatim in `05-DEFECTS.md` with its severity and disposition.
    - `artifacts/coverage_manifest.json` in the target sidecar exists and its `seal` field is readable; its `base_sha` and `head_sha` equal the two full SHAs quoted in the receipt.
    - `git -C /Users/christopher/Documents/Development/_hy/mando status --porcelain --untracked-files=no` returns empty output.
    - `rg -c 'Exit code' .planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md` returns at least 1.
    - `rg -c '^\| .* \| (blocker|non-blocker) \| .* \| (fixed-here|deferred) \|' .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` returns at least 1.
  </acceptance_criteria>
  <done>A security-profile review has run end to end against a real diff in the target repo, its manifest seal state and headline counts are recorded in a sanitized receipt, and the defect ledger exists with at least the D-05 deviation row.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to the general profile and evidence the superset contract</name>
  <files>.planning/phases/05-end-to-end-verification-audit-review/05-01-review-general-receipt.md, .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md</files>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/review_findings.py` — `apply_profile`, `GENERAL_DEFECT_CLASSES`, the gate A-E ladder
    - `.planning/phases/03-rule-matching-review-modes/03-CONTEXT.md` — D-10, the profile-superset contract
    - `.planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md` — the SHA pair and format established in Task 1
  </read_first>
  <action>
Re-run the identical range under the general profile and compare the two kept-finding
sets, per D-06.

Run from the same directory with the same two full SHAs resolved in Task 1, changing
only `--profile security` to `--profile general`. Changing the SHA pair between the
two runs invalidates the comparison, so read the SHAs back from the Task 1 receipt
rather than re-resolving the refs.

Read both runs' kept findings from the sidecar's `review_ledger.json` — a separate
artifact from `findings.json`, because `models.py` is the frozen milestone contract
byte-mirrored by a Go port. Compute the security-kept fingerprint set and the
general-kept fingerprint set and assert the security set is a subset of the general
set. The expected mechanism is `apply_profile`: the security profile drops every
finding marked by gates A through E, while the general profile keeps gate A and gate B
findings whose class is in `GENERAL_DEFECT_CLASSES` and drops gates C, D, and E under
both profiles.

Write `05-01-review-general-receipt.md` in the same Phase 1 evidence format, adding
the subset comparison result: the two set sizes, the count of findings unique to the
general profile, and the count of any finding kept by security but dropped by general.
A non-zero count in that last figure is a superset violation. Per the flagged
assumption on E-12, record it as a `deferred` ledger row with the finding class and
gate involved, report the phase honestly as having a gap, and do not re-run.

Apply the same D-07 sanitization pass before saving. Append every gap this run
surfaced to `05-DEFECTS.md`.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -m sec_overlay.cli review --base "$BASE_SHA" --head "$HEAD_SHA" --root /Users/christopher/Documents/Development/_hy/mando --profile general; echo "exit=$?"</automated>
    <automated>rg -N '_hy/mando/' .planning/phases/05-end-to-end-verification-audit-review/05-01-review-security-receipt.md .planning/phases/05-end-to-end-verification-audit-review/05-01-review-general-receipt.md .planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md | rg -v '_hy/mando/\.sec-overlay' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The general-profile `review` command exits 0, or a non-zero exit is recorded verbatim in `05-DEFECTS.md`.
    - Both receipts quote the same `base_sha` and `head_sha` values.
    - `05-01-review-general-receipt.md` states the security-kept count, the general-kept count, and the count of findings kept by security but dropped by general.
    - The sanitization command above prints `0`. <!-- planner-discipline-allow: _hy/mando/ -->
    - `rg -c 'seal' .planning/phases/05-end-to-end-verification-audit-review/05-01-review-general-receipt.md` returns at least 1.
  </acceptance_criteria>
  <done>Both profiles have run on one identical SHA range, the superset relationship is measured and stated numerically in the receipt, and any violation is a ledger row rather than a re-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sanitization review before the first Phase 5 commit</name>
  <what-built>Two sanitized run receipts and the phase defect ledger, plus an automated sanitization gate that greps the three files for target-repo paths below the repo root and passes only on zero matches.</what-built>
  <how-to-verify>
    This is the first commit of Phase 5 evidence into this repository, and D-07 is
    one-way for git history. The automated gate catches path leaks; it cannot judge
    prose.

    1. Read `05-01-review-security-receipt.md`, `05-01-review-general-receipt.md`, and
       `05-DEFECTS.md` in full.
    2. Confirm each contains only commands, exit codes, seal states, headline counts,
       gate verdicts, and SHAs.
    3. Confirm no file path below the target repo root appears, other than the
       `.sec-overlay` sidecar path that D-09 requires.
    4. Confirm no source snippet and no finding body text appears — including inside
       repro commands in the ledger.
    5. Confirm the ledger contains an honest row for every gap observed, and no row
       claiming a re-run turned a failure into a pass.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the commit, or name the lines to strip.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target work repo → this marketplace repo | Third-party employer source crosses into a repository whose history is permanent |
| this repo's tooling → target work repo tree | Our pipeline writes a sidecar into a repository we do not own |
| shipped review pipeline → recorded evidence | Real pipeline output crosses into a claim of correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | receipt and ledger authoring in Tasks 1-2 | high | mitigate | D-07 sanitization pass before save; automated `rg` gate in Task 2 verify allowing only the `.sec-overlay` sidecar path; blocking human review in Task 3 before the first commit |
| T-05-02 | Tampering | `review` run against the target tree | high | mitigate | Task 1 precondition asserts a clean tracked tree; Task 1 verify re-asserts `git status --porcelain --untracked-files=no` is empty after the run; prohibition forbids staging or deleting anything in the target |
| T-05-03 | Repudiation | superset comparison in Task 2 | high | mitigate | E-12 flagged assumption plus D-12: a violation is a `deferred` ledger row with the finding class and gate named, never a re-run; both receipts must quote the same SHA pair so the comparison is reproducible |
| T-05-04 | Information Disclosure | repro commands inside `05-DEFECTS.md` | medium | mitigate | Task 3 checkpoint step 4 explicitly inspects repro commands, the most likely place a path or snippet survives sanitization |
| T-05-05 | Spoofing | SHA pair drift between the two profile runs | medium | mitigate | Task 2 reads the SHAs back from the Task 1 receipt rather than re-resolving refs; acceptance criterion asserts both receipts quote identical values |
| T-05-06 | Denial of Service | shipped `--timeout` and `--concurrency` defaults on a real diff | low | accept | Phase 4 already proved bounded-run and timeout behaviour; a hang here would be a run-blocker under D-10 and is caught by the exit-code check |
</threat_model>

<verification>
- Both `review` invocations complete and their exit codes are recorded verbatim.
- The target repo's tracked working tree is byte-identical before and after both runs.
- The coverage manifest in the sidecar carries a readable `seal` value and the pinned SHA pair.
- The sanitization gate reports zero target-repo paths below the repo root across all three produced files.
- `05-DEFECTS.md` has at least one row and every row carries a disposition and a rationale sentence.
</verification>

<success_criteria>
AUD-06 has real run evidence: a full `review` run in both profiles completed on a real
diff, the coverage manifest state is recorded, positioning declines are counted, the
profile-superset relationship is measured numerically, and every observed gap is a
ledger row rather than a hidden re-run.
</success_criteria>

<output>
Create `.planning/phases/05-end-to-end-verification-audit-review/05-01-SUMMARY.md` when done
</output>
