---
phase: 01-baseline-health-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md
  - README.md
  - CHANGELOG.md
autonomous: false
requirements: [VAL-01, VAL-02, VAL-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "01-VERIFICATION.md exists in the phase directory and is the only evidence artifact for this phase (D-05)"
    - "01-VERIFICATION.md carries a version block naming the installed ruff, ty, pytest, python, and claude CLI versions (D-07)"
    - "Every gate section records the exact command, the working directory, the exit code, and the decisive tail lines of real output (D-06)"
    - "`claude plugin validate .` is recorded twice — once at the repo root and once inside plugins/sec-overlay/ — as two separate receipts with two separate exit codes; neither invocation is merged into, substituted for, or inferred from the other (D-15)"
    - "The marketplace manifest's `plugins` array is non-empty, and every plugin entry it registers has its own validation receipt in 01-VERIFICATION.md; a registered plugin with no receipt fails the gate"
    - "01-VERIFICATION.md presents gate sections in the fixed order VAL-01 then VAL-02 then VAL-03 regardless of the order the gates were actually executed, and each section names the exact command run"
    - "Every non-zero exit code has a triage row naming its disposition: code defect, stale test, config, or environmental"
    - "The two documented environmental pytest failures (missing gitignored bench corpus) are recorded as environmental, not silenced by committing corpus or seed data"
  prohibitions:
    - statement: "Must not manufacture a green gate by weakening the gate itself — deleting or skipping tests, adding an unjustified noqa or type-ignore, widening the ruff/ty exclude lists, or committing gitignored corpus data — instead of fixing or honestly recording the defect"
      status: unverified
    - statement: "Must not record a gate result that is not backed by quoted output from a real run of the stated command in the stated directory during this phase"
      status: unverified
    - statement: "Must not bypass the repo's own governance hooks to land a change: no --no-verify, no wildcard staging, no direct commit to main"
      status: unverified
    - statement: "Must not edit the frozen JSON-contract modules; a fix that appears to require one halts and escalates to the maintainer instead of proceeding"
      status: unverified
  artifacts:
    - path: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      provides: "Baseline gate receipts, tool version block, and triage ledger for VAL-01/02/03"
      contains: "## VAL-01"
  key_links:
    - from: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      to: ".claude-plugin/marketplace.json"
      via: "one VAL-01 validation receipt per plugin entry registered in the manifest"
      pattern: "sec-overlay"
    - from: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      to: "plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml"
      via: "VAL-02 receipts run through uv against this project file (D-10, D-13)"
      pattern: "uv run"
---

<objective>
Capture the true health of the delivered baseline. Run the three gate families
(VAL-01 plugin validation, VAL-02 sec-overlay quality gates, VAL-03 prek hooks) and
record every result as a quoted receipt in a single evidence document, then triage
every failure into a fix ledger the maintainer approves before any fix lands.

Purpose: Phase 2 builds the diff pipeline on top of this package. A green, receipt-backed
baseline is its precondition. An assumed-green baseline is worse than a red one.
Output: `.planning/phases/01-baseline-health-verification/01-VERIFICATION.md` holding a
tool version block, three ordered gate sections with real receipts, and a triage ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-health-verification/01-CONTEXT.md
@.planning/phases/01-baseline-health-verification/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end gate receipt — version block plus the complete VAL-01 section</name>
  <files>.planning/phases/01-baseline-health-verification/01-VERIFICATION.md, README.md, CHANGELOG.md</files>
  <precondition>The `claude`, `uv`, and `prek` executables all resolve on PATH (`command -v claude uv prek` prints three paths).</precondition>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-CONTEXT.md (D-05 through D-09, D-15 define the evidence shape and the VAL-01 scope)
    - .planning/phases/01-baseline-health-verification/01-PATTERNS.md (section "01-VERIFICATION.md (evidence doc)" — receipt discipline)
    - .claude-plugin/marketplace.json (the `plugins` array names every entry that needs a receipt)
    - plugins/sec-overlay/.claude-plugin/plugin.json (the plugin manifest the second invocation validates)
    - CLAUDE.md (repo root — commit governance for the receipt commit)
    - scripts/hooks/pre-commit-check.sh (the staging rules this plan's commit must satisfy)
  </read_first>
  <action>
Prove the whole receipt pipeline on one gate before capturing the other two: run a real
command, capture a real exit code, write a real receipt, commit it.

Create `.planning/phases/01-baseline-health-verification/01-VERIFICATION.md` with, in this
order: an H1 title, a `## Tool Versions` block, then `## VAL-01 — Plugin Validation`.

The `## Tool Versions` block is a markdown table with header row `| Tool | Version |` and
exactly five data rows whose first cell is `ruff`, `ty`, `pytest`, `python`, and `claude`
(lowercase, one row each, that order). Fill every version from a real invocation, not from
memory (D-07): `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ruff --version`,
the same shape with `ty --version`, `pytest --version`, and `python --version`, and
`claude --version` at the repo root. Record the interpreter version only — do not add or
change a `requires-python` floor, that decision belongs to Phase 3 (D-11). Do not add
version pins to any file (D-09).

The `## VAL-01 — Plugin Validation` section carries two receipts, because D-15 requires two
invocations: `claude plugin validate .` at the repo root, and `claude plugin validate .`
executed inside `plugins/sec-overlay/`. Run the second without a `cd` in a compound command
— use a subshell or the CLI's own directory argument, whichever the installed `claude`
supports. Keep them as two independent receipts; do not infer one from the other.

Each receipt is a four-part block in this exact shape, so the evidence is greppable:

- a `- Command:` line holding the command in backticks
- a `- Directory:` line holding the directory it ran in, relative to the repo root
- a `- Exit code:` line holding the integer exit status
- an `- Output (tail):` line followed by a fenced block holding the decisive tail lines (D-06)

Trim the fenced output to the decisive lines and replace any absolute home-directory path
with `~` before writing it, so the document does not carry local filesystem layout.

Record what actually happened. A non-zero exit code is written down as a non-zero exit code
with its real output; it is not retried until green, not reworded, and not omitted.

Commit with `docs(01): capture VAL-01 baseline receipts`. The evidence document is a
repo-level file, so `scripts/hooks/pre-commit-check.sh` requires root `README.md` and root
`CHANGELOG.md` staged in the same commit — add a Common Changelog entry and the matching
README line, and stage the three paths explicitly. Never `git add -A`, never `--no-verify`.
  </action>
  <verify>
    <automated>V=.planning/phases/01-baseline-health-verification/01-VERIFICATION.md; test -f "$V" && [ "$(grep -v '^#' "$V" | grep -cE '^\| (ruff|ty|pytest|python|claude) ')" -eq 5 ] && [ "$(grep -v '^#' "$V" | grep -c '^- Exit code:')" -ge 2 ] && [ "$(grep -v '^#' "$V" | grep -c '^- Command:')" -ge 2 ] && [ "$(grep -v '^#' "$V" | grep -c '^- Directory:')" -ge 2 ] && git log -1 --name-only --format='' | grep -qx 'CHANGELOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/01-baseline-health-verification/01-VERIFICATION.md` exits 0
    - The `## Tool Versions` table has exactly five data rows, first cells `ruff`, `ty`, `pytest`, `python`, `claude`, each with a non-empty version string
    - `grep -c '^- Exit code:'` over the non-header lines returns at least 2
    - Two `- Directory:` values are present and differ: one is the repo root, one is `plugins/sec-overlay`
    - Every `- Output (tail):` line is immediately followed by a fenced block containing at least one line of real command output
    - `grep -c '/Users/' 01-VERIFICATION.md` returns 0 (absolute home paths replaced with `~`)
    - `git log -1 --name-only` for the receipt commit lists `README.md` and `CHANGELOG.md` alongside the evidence document
  </acceptance_criteria>
  <done>The maintainer can open 01-VERIFICATION.md and read the real, dated tool versions and both plugin-validation exit codes without running anything themselves.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the VAL-02 and VAL-03 gate sections in the same receipt shape</name>
  <files>.planning/phases/01-baseline-health-verification/01-VERIFICATION.md, README.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md (the receipt shape Task 1 established — match it exactly)
    - .planning/phases/01-baseline-health-verification/01-CONTEXT.md (D-10, D-12, D-13, D-14 fix the invocations and their scope)
    - plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml (ruff line-length, target-version, and the existing narrow excludes)
    - plugins/sec-overlay/CLAUDE.md (the maintainer's documented test/lint/type-check invocations and the known environmental failures)
    - .pre-commit-config.yaml (the two hook ids prek runs)
  </read_first>
  <action>
Append `## VAL-02 — sec-overlay Quality Gates` and then `## VAL-03 — prek Hooks` to
01-VERIFICATION.md. Section order in the document is fixed VAL-01, VAL-02, VAL-03
regardless of the order you execute them in — execution order is discretionary, document
order is not.

VAL-02 carries three receipts, all run against the helpers package only (D-13), all through
uv so the environment resolves from that directory's `pyproject.toml` (D-10). Use uv's
`--directory` option rather than a `cd` in a compound command, and pass `--locked` so a
stale `uv.lock` fails loudly instead of silently re-resolving dependencies:

- `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers pytest -q`
- `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ruff check sec_overlay/ bench/ tests/`
- `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ty check`

Redirect each command's combined output to a scratch file outside the repository, then lift
only the decisive tail lines into the receipt — pytest's summary line with its pass/fail
counts, ruff's result line, ty's result line. Delete the scratch files when the section is
written. A `--locked` failure is a real finding: record it as lockfile drift, not as a code
defect, and do not run `uv lock` to make it go away.

VAL-03 carries one receipt: `prek run --all-files` from the repo root, matching VAL-03's
"passes across the repo" wording (D-12). Both hook ids in `.pre-commit-config.yaml` must
appear in the captured output; if prek reports that hooks are not installed, that is itself
the VAL-03 finding — record it rather than installing and re-running as though it had
passed.

Then append `## Triage Ledger`: a markdown table with header `| Gate | Failure | Disposition | Rationale |`
holding one row per non-zero exit code and per individual test failure. Disposition is
exactly one of `code defect`, `stale test`, `config`, or `environmental`. Apply D-03 when
choosing between the first two: the test changes only when it asserts stale or wrong
behavior, otherwise the production code changes. The two failures the maintainer already
documents as environment-only — the missing gitignored bench corpus tripping
`test_bench.py::test_seed_corpus_is_valid` and `test_citations.py::test_all_mapped_ids_exist_in_seed`
— get disposition `environmental` with the missing corpus named as the rationale. Do not
resolve them by committing corpus or seed data.

If every gate exits 0, write the table header and a single row reading `| — | none | — | all gates green |`.
The ledger always exists; it is the input Task 3 puts in front of the maintainer.

Commit with `docs(01): capture VAL-02 and VAL-03 baseline receipts`, staging the evidence
document, root `README.md`, and root `CHANGELOG.md` explicitly.
  </action>
  <verify>
    <automated>V=.planning/phases/01-baseline-health-verification/01-VERIFICATION.md; [ "$(grep -cE '^## VAL-0[123] ' "$V")" -eq 3 ] && [ "$(grep -v '^#' "$V" | grep -c '^- Exit code:')" -ge 6 ] && grep -q '^## Triage Ledger' "$V" && grep -q '^| Gate | Failure | Disposition | Rationale |' "$V" && [ "$(grep -n '^## VAL-01' "$V" | cut -d: -f1)" -lt "$(grep -n '^## VAL-02' "$V" | cut -d: -f1)" ] && [ "$(grep -n '^## VAL-02' "$V" | cut -d: -f1)" -lt "$(grep -n '^## VAL-03' "$V" | cut -d: -f1)" ]</automated>
  </verify>
  <acceptance_criteria>
    - All three gate headings are present and appear in file order VAL-01, VAL-02, VAL-03
    - At least six `- Exit code:` receipts exist (2 from VAL-01, 3 from VAL-02, 1 from VAL-03)
    - Each VAL-02 receipt's `- Command:` line contains `uv run` and `--directory plugins/sec-overlay/skills/sec-overlay/helpers`
    - The VAL-03 receipt's captured output names both hook ids from `.pre-commit-config.yaml`
    - `## Triage Ledger` exists with the four-column header and at least one row
    - Every ledger `Disposition` cell holds exactly one of `code defect`, `stale test`, `config`, `environmental`, or the literal `—` on the all-green row
    - `git status --porcelain` reports no leftover scratch capture files inside the repository
    - No test file, `pyproject.toml` exclude list, or hook config was edited in this task — `git diff --stat` for the commit lists only the evidence document, `README.md`, and `CHANGELOG.md`
  </acceptance_criteria>
  <done>All three gate families have real receipts in the evidence document, and every failure has a disposition the maintainer can act on.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve the triage ledger and confirm the frozen-contract boundary</name>
  <files>.planning/phases/01-baseline-health-verification/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md (the triage ledger being approved)
    - .planning/phases/01-baseline-health-verification/01-CONTEXT.md (D-01 through D-04 — the failure policy being applied)
  </read_first>
  <decision>Given the concrete triage ledger, which remediation route does Phase 1 take?</decision>
  <context>
D-02 already locked the rule that the frozen JSON-contract modules are never edited, but it
was written before any gate had run — it could not know which files the failures would name.
This checkpoint is the first moment the concrete fix set exists. It confirms that fix set
against the one-way boundary rather than re-asking the locked rule.

The boundary is one-way because those two modules are byte-mirrored by a Go port; changing
one breaks a published JSON contract that other systems already read, and the undo is a
coordinated migration across both implementations, not a revert.

Present the triage ledger verbatim, then state which files each fix would touch.
  </context>
  <options>
    <option id="proceed-as-triaged">
      <name>Proceed as triaged</name>
      <pros>No listed fix touches a frozen module. Plan 02 executes the ledger under normal governance and Phase 1 completes.</pros>
      <cons>Commits real code changes to the delivered baseline; each one needs a version bump and a changelog entry.</cons>
    </option>
    <option id="escalate-frozen">
      <name>Escalate — a fix needs a frozen-contract edit</name>
      <pros>Stops before a one-way door. The contract question reaches the maintainer with the failing receipt attached.</pros>
      <cons>Phase 1 cannot complete until the contract decision is made; Phase 2 stays blocked.</cons>
    </option>
    <option id="no-fix-needed">
      <name>No fix needed — green or environmental only</name>
      <pros>Nothing is changed. Plan 02 records "no fix required" and Phase 1 goes straight to sealing.</pros>
      <cons>The two documented environmental failures stay red on a fresh clone until the corpus is regenerated.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The triage ledger was presented to the maintainer verbatim, with the touched-file list for each proposed fix
    - The maintainer's selection is recorded in 01-VERIFICATION.md under a `## Remediation Route` heading naming the chosen option id
    - If `escalate-frozen` is selected, no remediation commit is made and the phase halts
  </acceptance_criteria>
  <resume-signal>Select: proceed-as-triaged, escalate-frozen, or no-fix-needed</resume-signal>
</task>

</tasks>

<flagged_assumptions>
Two edge-probe rows could not be classified into a checkable acceptance criterion and are
carried forward as explicit assumptions, not silently dropped:

| Requirement | Probe | Status | Assumption being made |
|-------------|-------|--------|-----------------------|
| VAL-02 | unclassified — review manually | unresolved | Assumed that "quality gates pass" means the three invocations in D-10/D-13 exit 0, and that a test failure documented by the maintainer as environmental (missing gitignored corpus) does not count against the gate. No source artifact states the boundary between "gate failure" and "environment gap" beyond the maintainer's notes. |
| VAL-03 | unclassified — review manually | unresolved | Assumed that `prek run --all-files` exiting 0 is the whole of VAL-03, and that "hooks are installed" is proven by prek executing both configured hook ids rather than by a separate installation check. |

Both assumptions are recorded here for the maintainer to confirm or correct at the Task 3
checkpoint. Neither is auto-dismissed.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external tool stdout → evidence document | pytest/ruff/ty/prek/claude output is copied into a document that is later trusted as proof |
| agent → repository working tree | receipt commits cross into tracked files under governance hooks |
| PyPI / uv resolver → helpers virtualenv | `uv run` resolves the dev dependency group before executing a gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | gate invocation in Tasks 1-2 | high | mitigate | Receipts record the exact command and directory; Task 2's acceptance criteria assert the commit diff contains only the evidence document and root docs, so no test, exclude list, or hook config can be edited while capturing a baseline |
| T-01-02 | Repudiation | 01-VERIFICATION.md | high | mitigate | Every gate section requires a `- Command:`, `- Directory:`, `- Exit code:`, and a fenced real-output block; the automated verify counts them, so a claimed pass with no receipt fails the task |
| T-01-05 | Tampering | uv dependency resolution | medium | mitigate | All three VAL-02 invocations pass `--locked`, so a drifted lockfile fails loudly instead of silently re-resolving; a `--locked` failure is triaged as drift and never resolved by re-locking inside this task |
| T-01-06 | Information Disclosure | fenced output blocks | low | accept | Output is trimmed to decisive tail lines (D-06) and absolute home paths are rewritten to `~` before writing; residual disclosure risk in a local planning document is accepted |
| T-01-SC | Tampering | package-manager installs | medium | mitigate | This plan installs no packages — `uv.lock` is committed and every gate runs against the locked resolution. Any `uv add` or `uv lock --upgrade` halts and escalates instead of proceeding, so no package-legitimacy audit is bypassed |
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Kind | Created by |
|----------|------|-----------|
| `.planning/phases/01-baseline-health-verification/01-VERIFICATION.md` | new evidence document — tool version block, three ordered gate sections, triage ledger, remediation route | this plan (Tasks 1-3) |
| `## Tool Versions` block inside 01-VERIFICATION.md | dated record of installed ruff, ty, pytest, python, claude CLI versions (D-07) | Task 1 |
| `## Triage Ledger` table inside 01-VERIFICATION.md | per-failure disposition set consumed by Plan 02 | Task 2 |
| `## Remediation Route` heading inside 01-VERIFICATION.md | the maintainer's checkpoint selection | Task 3 |
| Root `README.md` and `CHANGELOG.md` entries | governance-required companions to each receipt commit | Tasks 1-2 |

No source module, test, configuration file, or plugin manifest is modified by this plan.
</artifacts_this_phase_produces>

<verification>
- 01-VERIFICATION.md exists and holds a five-row tool version table
- Three gate sections exist in document order VAL-01, VAL-02, VAL-03
- At least six exit-code receipts exist, each paired with a command, a directory, and real output
- A triage ledger table exists with a disposition for every failure
- A remediation route is recorded from the checkpoint
- No source, test, config, or manifest file was modified by this plan
</verification>

<success_criteria>
The maintainer can read one document and know, from quoted command output, exactly which of
the three gate families is green today, which is not, why, and what will be done about it.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-health-verification/01-01-SUMMARY.md` when done
</output>
