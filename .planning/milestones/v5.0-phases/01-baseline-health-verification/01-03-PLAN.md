---
phase: 01-baseline-health-verification
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "01-VERIFICATION.md carries a final receipt for all six gate invocations, captured after the last fix commit, each showing exit code 0"
    - "The baseline receipts and the triage ledger remain visible in the document — the final green does not overwrite the record of what was red"
    - "A fix ledger table records gate, failure, fix summary, and commit SHA for every fix that landed (D-08)"
    - "Every SHA in the fix ledger resolves to a real commit on the current branch"
    - "The two frozen JSON-contract modules are byte-identical to their state at phase start, proven by a diff against the phase-start SHA (D-02)"
    - "Every fix-ledger commit that touched a sec-overlay shipping file also carried a plugin version bump and a changelog entry (D-04)"
    - "The tool version block still matches the versions the final run used (D-07, D-09)"
    - "Failures recorded as environmental are still recorded as environmental, with their root cause named and no data committed to hide them"
  prohibitions:
    - statement: "Must not record a gate result that is not backed by quoted output from a real run of the stated command in the stated directory during this phase"
      status: unverified
    - statement: "Must not manufacture a green gate by weakening the gate itself — deleting or skipping tests, adding an unjustified suppression, widening an exclude list, or committing gitignored corpus data"
      status: unverified
    - statement: "Must not delete or rewrite the baseline receipts and triage ledger when adding the final green receipts; the red-to-green record stays intact"
      status: unverified
    - statement: "Must not bypass the repo's own governance hooks to land the sealing commit: no --no-verify, no wildcard staging, no direct commit to main"
      status: unverified
  artifacts:
    - path: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      provides: "Final green receipts, the fix ledger, and the frozen-contract integrity proof"
      contains: "## Fix Ledger"
  key_links:
    - from: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      to: "git history"
      via: "every fix ledger SHA resolves via git cat-file on the current branch"
      pattern: "## Fix Ledger"
    - from: ".planning/phases/01-baseline-health-verification/01-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "one final green receipt per VAL-01/02/03 success criterion"
      pattern: "## Final Verification"
---

<objective>
Seal the phase. Re-run all six gate invocations after the last fix, record the final green
receipts alongside the baseline reds, and prove the fixes obeyed the phase's constraints —
frozen contract untouched, every version bump and changelog entry present, every ledger SHA
real.

Purpose: The phase claim is "the maintainer can trust the delivered baseline". A trustworthy
claim needs the final receipt *and* the record of what it took to get there.
Output: 01-VERIFICATION.md completed with a final verification section, a fix ledger, and an
integrity proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-health-verification/01-CONTEXT.md
@.planning/phases/01-baseline-health-verification/01-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run all six gate invocations and record the final green receipts</name>
  <files>.planning/phases/01-baseline-health-verification/01-VERIFICATION.md, README.md, CHANGELOG.md</files>
  <precondition>No uncommitted changes remain in the working tree (`git status --porcelain` is empty), so the receipts describe committed state rather than a dirty tree.</precondition>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md (the receipt shape to match and the baseline sections to preserve)
    - .planning/phases/01-baseline-health-verification/01-CONTEXT.md (D-06, D-07, D-10, D-12, D-13, D-15 fix the six invocations and the receipt detail)
    - .planning/ROADMAP.md (the three Phase 1 success criteria these receipts must satisfy)
  </read_first>
  <action>
Append a `## Final Verification` section to 01-VERIFICATION.md holding a fresh receipt for
each of the six invocations, captured after the last fix commit:

- `claude plugin validate .` at the repo root
- `claude plugin validate .` inside `plugins/sec-overlay/`
- `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers pytest -q`
- `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ruff check sec_overlay/ bench/ tests/`
- `uv run --locked --directory plugins/sec-overlay/skills/sec-overlay/helpers ty check`
- `prek run --all-files` from the repo root

Each receipt keeps the four-part shape the earlier sections established: a `- Command:` line,
a `- Directory:` line, a `- Exit code:` line, and an `- Output (tail):` line followed by a
fenced block of the real decisive tail lines, with absolute home paths rewritten to `~`.

Append, do not overwrite. The baseline sections and the triage ledger stay exactly where
they are. A document that shows only the final green is a weaker artifact than one that
shows the red, the reasoning, and the green — the whole point of the phase is the second
kind.

Re-read the tool versions and confirm the `## Tool Versions` block still matches what this
run used. If a version moved between the baseline run and this one, update the block and add
a one-line note saying it moved; do not add a pin (D-09).

If any invocation is still non-zero, the phase is not done. Record the receipt honestly and
stop rather than sealing — an environmental failure recorded in the triage ledger is the one
exception, and it must appear in the pytest receipt with its `environmental` disposition
restated inline.

Commit as `docs(01): record final gate verification`. The evidence document is a repo-level
file, so stage root `README.md` and root `CHANGELOG.md` in the same commit. Explicit paths
only, never `--no-verify`.
  </action>
  <verify>
    <automated>V=.planning/phases/01-baseline-health-verification/01-VERIFICATION.md; grep -q '^## Final Verification' "$V" && [ "$(sed -n '/^## Final Verification/,$p' "$V" | grep -c '^- Exit code: 0$')" -ge 6 ] && grep -q '^## Triage Ledger' "$V" && [ "$(grep -cE '^## VAL-0[123] ' "$V")" -eq 3 ] && [ "$(grep -c '/Users/' "$V")" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `## Final Verification` exists and contains at least six receipts
    - Every receipt in that section shows `- Exit code: 0`
    - The six `- Command:` values cover both plugin validations, all three helpers commands, and the repo-wide prek run
    - The baseline `## VAL-01`, `## VAL-02`, `## VAL-03` sections and the `## Triage Ledger` are still present and unmodified
    - The `## Tool Versions` table still has its five rows and matches the versions of this run
    - `grep -c '/Users/' 01-VERIFICATION.md` returns 0
    - `git status --porcelain` was empty before the receipts were captured
  </acceptance_criteria>
  <done>Six green receipts sit in the evidence document, captured after the last fix, with the baseline red still visible above them.</done>
</task>

<task type="auto">
  <name>Task 2: Write the fix ledger and prove the phase constraints held</name>
  <files>.planning/phases/01-baseline-health-verification/01-VERIFICATION.md, README.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/01-baseline-health-verification/01-VERIFICATION.md (the triage ledger rows that need matching ledger entries)
    - .planning/phases/01-baseline-health-verification/01-CONTEXT.md (D-02, D-04, D-08 define the ledger columns and the constraints to prove)
    - plugins/sec-overlay/CHANGELOG.md (confirm one entry per shipping-file fix commit)
    - plugins/sec-overlay/.claude-plugin/plugin.json (confirm the version advanced once per shipping-file fix commit)
  </read_first>
  <action>
Append `## Fix Ledger` to 01-VERIFICATION.md: a markdown table with header
`| Gate | Failure | Fix summary | Commit SHA |` holding one row per fix that landed (D-08).
Gate is `VAL-01`, `VAL-02`, or `VAL-03`. Failure names the concrete symptom — a test node id,
a ruff or ty rule code, a validator message. Fix summary is one line. Commit SHA is the short
SHA, and it must resolve on the current branch. If no fix landed, write the header and one
row reading `| — | none | all gates green at baseline | — |`.

Then append `## Constraint Proof` with three checks, each quoting the command and its output:

First, frozen-contract integrity. Diff the two modules named in D-02 against the phase-start
SHA and show that the diff is empty. Quote the command and the empty result — an assertion
without the receipt is exactly what this phase exists to reject.
<!-- planner-discipline-allow: models.py -->
<!-- planner-discipline-allow: evidence.py -->

Second, governance compliance. For each shipping-file fix commit in the ledger, show
`git show --name-only` output confirming the plugin manifest and the plugin changelog were
staged in that same commit (D-04).

Third, ledger integrity. Show that every SHA in the fix ledger resolves — a `git cat-file -t`
per SHA returning `commit`.

If any of the three checks fails, do not adjust the check to make it pass. Record the failure
and stop; a failing constraint proof means a fix broke a phase rule and needs the maintainer.

Finally, add a one-paragraph `## Phase Outcome` stating in plain terms whether the delivered
baseline is trustworthy today, naming any residual environmental failure and its root cause
so a future reader is not surprised by a red test on a fresh clone.

Commit as `docs(01): add fix ledger and constraint proof`, staging the evidence document,
root `README.md`, and root `CHANGELOG.md` explicitly.
  </action>
  <verify>
    <automated>V=.planning/phases/01-baseline-health-verification/01-VERIFICATION.md; grep -q '^## Fix Ledger' "$V" && grep -q '^| Gate | Failure | Fix summary | Commit SHA |' "$V" && grep -q '^## Constraint Proof' "$V" && grep -q '^## Phase Outcome' "$V" && for s in $(sed -n '/^## Fix Ledger/,/^## Constraint Proof/p' "$V" | grep -oE '\b[0-9a-f]{7,40}\b'); do git cat-file -t "$s" 2>/dev/null | grep -qx commit || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `## Fix Ledger` exists with the four-column header and at least one row
    - Every short SHA appearing in the fix ledger returns `commit` from `git cat-file -t`
    - Every triage-ledger row dispositioned `code defect`, `stale test`, or `config` has a matching fix-ledger row; rows dispositioned `environmental` have none
    - `## Constraint Proof` contains a quoted empty diff of the two frozen contract modules against the phase-start SHA
    - `## Constraint Proof` shows, per shipping-file fix commit, that the plugin manifest and plugin changelog were in that commit
    - `## Phase Outcome` names any residual environmental failure and its root cause
    - `git log --format=%s` for this plan's commits shows no `--no-verify` bypass and no commit on main
  </acceptance_criteria>
  <done>The evidence document answers, with receipts, three questions: is it green, what did it take, and did the fixes stay inside the rules.</done>
</task>

</tasks>

<flagged_assumptions>
Carried forward unresolved from Plans 01 and 02; neither is auto-dismissed:

| Requirement | Probe | Status | Assumption being made |
|-------------|-------|--------|-----------------------|
| VAL-02 | unclassified — review manually | unresolved | Assumed a maintainer-documented environmental failure (missing gitignored corpus) does not block the gate, while every other failure does. The `## Phase Outcome` paragraph surfaces this to the reader rather than burying it. |
| VAL-03 | unclassified — review manually | unresolved | Assumed `prek run --all-files` exiting 0 is the whole of VAL-03, with "hooks are installed" proven by prek executing both configured hook ids. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → evidence document | ledger SHAs and version-bump claims are copied into a document later trusted as proof |
| external tool stdout → evidence document | final gate output is quoted as the phase's completion claim |
| agent → phase seal | this plan decides whether downstream phases may proceed on this baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Repudiation | fix ledger | high | mitigate | The automated verify resolves every ledger SHA through `git cat-file -t` and requires `commit`, so a fabricated or mistyped SHA fails the task rather than sitting in the record |
| T-01-07 | Tampering | evidence history | high | mitigate | Task 1 appends rather than overwrites, and its acceptance criteria assert the baseline gate sections and triage ledger are still present, so the red-to-green record cannot be quietly reduced to a green-only claim |
| T-01-03 | Tampering | frozen JSON-contract modules | critical | mitigate | The constraint proof quotes an empty diff of both modules against the phase-start SHA; a non-empty diff fails the proof and halts the seal instead of being adjusted away |
| T-01-04 | Elevation of Privilege | governance compliance claim | high | mitigate | The proof section shows `git show --name-only` per shipping-file fix commit, so a missing version bump or changelog entry is visible rather than asserted (D-04) |
| T-01-06 | Information Disclosure | fenced output blocks | low | accept | Output stays trimmed to decisive tail lines with home paths rewritten to `~`; the automated verify asserts zero absolute home paths remain. Residual disclosure risk in a local planning document is accepted |
| T-01-SC | Tampering | package-manager installs | medium | mitigate | This plan installs no packages and runs every gate with `--locked`; any install or lock upgrade halts and escalates for a package-legitimacy review |
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Kind | Created by |
|----------|------|-----------|
| `## Final Verification` section in 01-VERIFICATION.md | six post-fix green receipts, one per gate invocation | Task 1 |
| `## Fix Ledger` table in 01-VERIFICATION.md | gate / failure / fix summary / commit SHA, per D-08 | Task 2 |
| `## Constraint Proof` section in 01-VERIFICATION.md | quoted empty frozen-contract diff, per-commit governance proof, ledger SHA resolution | Task 2 |
| `## Phase Outcome` paragraph in 01-VERIFICATION.md | plain-language trust statement plus any residual environmental failure | Task 2 |
| Root `README.md` and `CHANGELOG.md` entries | governance-required companions to the sealing commits | Tasks 1-2 |

No source module, test, configuration file, or plugin manifest is modified by this plan.
</artifacts_this_phase_produces>

<verification>
- Six final receipts exist, all exit code 0, captured after the last fix commit
- Baseline receipts and the triage ledger survive intact in the same document
- A fix ledger exists with a resolvable SHA per fix
- The frozen contract modules diff empty against the phase-start SHA, with the receipt quoted
- Each shipping-file fix commit is shown to carry its version bump and changelog entry
- A phase outcome paragraph names any residual environmental failure
</verification>

<success_criteria>
A reader who was not present can open 01-VERIFICATION.md and verify, without running
anything, that all three gate families pass today, what was broken, what was changed, and
that nothing crossed the frozen-contract boundary.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-health-verification/01-03-SUMMARY.md` when done
</output>
