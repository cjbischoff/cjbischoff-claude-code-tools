---
phase: 06-remediation-and-governed-release
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - .planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md
  - .planning/phases/06-remediation-and-governed-release/06-DEFECTS.md
  - .planning/STATE.md
  - README.md
  - CHANGELOG.md
autonomous: false
requirements: [REL-01, REL-02, REL-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-06: the run is a real SKILL.md-driven per-file reviewer dispatch against the mando target, executed for both profiles, and the subset check is security-kept ⊆ general-kept computed on those reviewer results — not a mocked runner, not a whole-set shortcut, and not a synthetic fixture."
    - "D-07: the range is Phase 5's exact `base..head` SHA range on mando, re-read from the Phase 5 receipts at execution time rather than taken from this plan, so the run is directly comparable to the tracer run; a discrepancy between the receipts and this plan's expected values resolves in favor of the receipts and becomes a ledger row."
    - "REL-01: a real per-file reviewer dispatch runs against the target range, and the receipt shows per-file reviewer returns in place of the whole-set skip count Phase 5 recorded — that count flip is the proof the tracer non-dispatch defect is closed."
    - "REL-01: the dispatched run uses a fresh Phase 6 workspace supplied by the flag Plan 01 added, and the Phase 5 sidecar is byte-unchanged afterwards, proving the resume-identity guard was satisfied by an override rather than weakened."
    - "REL-01: the E-12 subset verdict is computed over two consumes of one identical reviewer output set, so the only variable between them is the profile gate and not model nondeterminism."
    - "REL-01: the E-12 receipt states the verdict and whether it was vacuous, with both kept-set sizes, so a reader cannot mistake an empty comparison for a held relation."
    - "REL-01 empty case: if either kept set is empty, the receipt records the verdict as vacuous, states that the live run is therefore not the primary evidence, and closes E-12 on Plan 04's deterministic backstop plus that honest note — per D-08."
    - "REL-01 ordering: the two consumes read the same reviewer output files, so the verdict is independent of which profile was consumed first — the receipt records the order used."
    - "REL-01 adjacency: every one of the ten Phase 5 ledger rows appears in the Phase 6 ledger with a terminal state, and no row is left in the deferred state it arrived in."
    - "REL-02 empty case: this plan's commits stage only planning documents and root docs — zero plugin shipping files — so no plugin version bump occurs, and the suite still passes, demonstrating the bump rule's lower bound."
    - "REL-02: the governance receipt lists, for each of the four shipping PRs, its branch, its commit types, its version transitions, and its CodeRabbit outcome, so the REL-02 claim is auditable from one place."
    - "REL-03 ordering: the dependency table is still asserted empty on the merged milestone branch, so the result does not depend on the order the four PRs merged in."
    - "The receipts contain no path below the target repository root, no code excerpt, and no finding body — only commands, exit codes, seal states, headline counts, verdicts, and SHAs."
  artifacts:
    - ".planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md — sanitized run, E-12, and governance receipts"
    - ".planning/phases/06-remediation-and-governed-release/06-DEFECTS.md — terminal disposition for all ten Phase 5 rows"
    - ".planning/STATE.md — phase 6 closed"
  key_links:
    - "One prepare and one dispatch feeding two consumes is what makes the subset verdict evidence; two independent dispatches would differ by model nondeterminism and prove nothing about the gate."
    - "The fresh workspace must come from the override flag, not from deleting the Phase 5 sidecar — the sidecar's survival is itself the proof the resume guard was not weakened."
    - "The vacuity flag on the E-12 verdict is load-bearing: without it this receipt reproduces the exact Phase 5 defect it exists to close."
    - "The sanitization boundary is enforced before the receipt is committed, not after — git history cannot be unpublished."
  prohibitions:
    - "No edit to helpers/sec_overlay/models.py."
    - "No edit to helpers/sec_overlay/evidence.py."
    - "No change to any `fingerprint()` output value."
    - "No new entry under `[project] dependencies` in helpers/pyproject.toml."
    - "No change to any file under `plugins/` in this plan — it is a documentation and evidence plan; any code defect found here is recorded in the ledger, not fixed inline."
    - "No absolute or relative path below the target repository's root in any committed receipt."
    - "No code excerpt, diff hunk, or finding body text in any committed receipt."
    - "No full artifact copied out of the target's sidecar into this repo."
    - "No write of any kind into the Phase 5 workspace."
    - "No `git add -A`, `git add .`, or `git commit -a`."
    - "No `--no-verify` on any git command."
    - "No commit on `main`."
    - "No `Co-Authored-By` trailer on any commit."
    - "No `rm -rf` — use `trash`."
---

<objective>
Produce the evidence this milestone has been claiming and could not show: a real per-file reviewer dispatch, a profile-subset verdict computed over one identical reviewer output set with its vacuity stated either way, and a governance receipt covering the four shipping PRs. Then give every Phase 5 defect a terminal disposition and close the phase.

Purpose: Phase 5 ended with nine deferred defects, a dispatch that never dispatched, and a subset check that compared nothing to nothing. Plans 01 through 04 fixed the code and installed the guards. This plan is where the claims get demonstrated rather than asserted, and where the ledger stops being a list of open questions.

Output: sanitized receipts, a closed defect ledger, and a closed phase. This plan writes no code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-remediation-and-governed-release/06-CONTEXT.md
@.planning/phases/06-remediation-and-governed-release/06-01-SUMMARY.md
@.planning/phases/06-remediation-and-governed-release/06-02-SUMMARY.md
@.planning/phases/06-remediation-and-governed-release/06-03-SUMMARY.md
@.planning/phases/06-remediation-and-governed-release/06-04-SUMMARY.md
@.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md
@CLAUDE.md
</context>

## Governance rail

Branch `docs/phase-06-receipts-and-defect-ledger`, forked from `docs/milestone-v5-diff-review` and merged back into it (D-14). Every commit here stages only `.planning/` documents plus the root `README.md` and root `CHANGELOG.md` — nothing under `plugins/`. That means no plugin version bump: this is the REL-02 lower bound, and the truth above records it deliberately rather than as an omission. Commits are `docs` type. Run `prek run` before each commit and let the hooks state the staging requirement rather than guessing at it. Explicit paths only, no `--no-verify`, no `Co-Authored-By` trailer.

## Target and range

Target repository: the external `mando` checkout, at whatever local path the operator has it (D-06). Per D-07 the range is Phase 5's exact `base..head`, and the executor **re-reads it from the Phase 5 receipts** rather than taking it from this plan — reusing the range is what makes the run directly comparable to the tracer run, so the authoritative copy is the one Phase 5 wrote. The values expected there are base `5f477d8c140c5b85f6c307a42d7afe96541efbfb` and head `d06ce30d328e41b9f258d3cb19964a57d0facd37`; if what the receipts say differs, the receipts win and the discrepancy goes in the ledger. Both SHAs are safe to record — a commit hash is not a path. The local checkout path is not.

## PR grouping (D-13, with the discretion it grants)

D-13 fixes the thematic shape and leaves the exact grouping to discretion. Two departures from its suggested list, both stated here so a reviewer does not read them as drift:

- The deps-template fix rides with the doc corrections (Plan 03) rather than with the pipeline wiring (Plan 02). It is a rendering fix in `report.py` with no relation to the phase table; grouping it with the other output-correctness fixes keeps both PRs single-concern.
- The frozen-contract and profile-subset guards get their own PR (Plan 04). D-15 and D-08 are not in D-13's suggested list, and folding test-only guards into a code PR would mix a claim with its own evidence.

That yields five PRs, four of them shipping-file PRs and this one planning-only.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the receipt sanitization boundary before anything is committed</name>
  <reversibility rating="one-way">A receipt committed to this repository's git history cannot be unpublished. Over-disclosure is permanent even if a later commit removes the file, so the boundary is agreed before the first write, not audited after it.</reversibility>
  <decision>What exactly may cross from the target's audit sidecar into this repository's committed receipts.</decision>
  <context>
D-09 fixes the intent: sanitized receipts only. The dispatched run produces findings about a third-party codebase, held in that target's own sidecar. This repository is a public plugin marketplace. Anything written here is permanent, so the boundary is confirmed before the run's output is transcribed rather than reviewed afterwards.

The three candidate boundaries below differ only in whether counts may be broken down. The run itself is identical under all three; only the transcription changes.
  </context>
  <options>
    <option id="minimal">
      <name>Headline counts only</name>
      <pros>Smallest disclosure surface. No per-file or per-severity structure that could be correlated back to the target's layout.</pros>
      <cons>A single total makes the count flip that proves the dispatch defect closed harder to read, since the Phase 5 baseline was itself a single number.</cons>
    </option>
    <option id="d09-as-written">
      <name>D-09 as written — commands, exit codes, seal states, headline counts, subset verdict, SHAs</name>
      <pros>Exactly what the locked decision specifies. Sufficient to show the count flip, the seal states, and the subset verdict with both kept-set sizes. No paths, no code, no finding bodies.</pros>
      <cons>Per-category counts sit at the edge of "headline"; the executor must decide category-level breakdown case by case.</cons>
    </option>
    <option id="plus-severity">
      <name>D-09 plus a per-severity breakdown</name>
      <pros>Makes the profile-gate effect legible at severity granularity, which strengthens the E-12 receipt.</pros>
      <cons>A severity distribution over a named public repository at a named SHA range is a partial disclosure of that repository's security posture. Goes beyond what D-09 authorizes.</cons>
    </option>
  </options>
  <resume-signal>Select: minimal, d09-as-written, or plus-severity. Selecting anything other than d09-as-written is a change to a locked decision and must be stated as such in the phase summary.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the dispatched review, compute the E-12 verdict, write sanitized receipts</name>
  <files>.planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md</files>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/SKILL.md` — the review-mode orchestration contract: the prepare, dispatch, and consume steps, and how the per-file reviewer subagent is invoked. This is the path Phase 5 never actually took, so read how a real dispatch is meant to be driven rather than inferring it from the CLI.
    - `plugins/sec-overlay/skills/sec-overlay/agents/review-file.md` — the per-file reviewer prompt, to know what one reviewer return looks like and therefore what a per-file return count counts.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py`, the `review` subparser and `run_review` — the current flag set including the `--workspace` override Plan 01 added, the profile flag, and the reviewer run configuration flags left to discretion.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/review_coverage.py` — `CoverageManifest.seal()` and the seal states the receipt reports.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/review_findings.py` — `apply_profile()`, so the subset verdict is computed over the same kept-set notion the code uses.
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` — the tracer non-dispatch row and the E-12 vacuous row, for the baseline numbers the receipt is compared against.
    - `.planning/phases/06-remediation-and-governed-release/06-01-SUMMARY.md` and `06-02-SUMMARY.md` — the landed `--workspace` behavior and the 24-phase table, so the run is driven against what actually shipped.
  </read_first>
  <action>
Sequence per D-12: the override flag has already landed, so the run uses it rather than working around its absence.

First, record the Phase 5 sidecar's pre-run state — enough to prove it was not written to. A digest over its state file, or its modification time, is sufficient; the path itself is not recorded, only the fact and the digest.

D-07: re-read Phase 5's exact `base..head` SHA range from the Phase 5 receipts — not from this plan — and run prepare once against that range, with a fresh Phase 6 workspace supplied through the override flag. Reusing that exact range is what makes this run directly comparable to the tracer run, so the receipts are authoritative and any discrepancy with this plan's expected values becomes a ledger row rather than a silent reconciliation.

Then dispatch once. D-06: this is the step Phase 5 skipped — drive the real SKILL.md-driven per-file reviewer subagent path against the mando target, for both profiles, and check security-kept ⊆ general-kept on those reviewer results. Not a mocked runner, not a whole-set shortcut, not a synthetic fixture. Reviewer run configuration — model, concurrency, timeout — is at discretion; choose values that complete without timing out on this range and record the exact values chosen, because a receipt that omits them is not reproducible.

Then consume twice over that one identical reviewer output set. Consume the first profile in place. For the second, copy the reviewer output files into a second workspace and consume there with the other profile. Copying the output rather than re-dispatching is the whole point: it holds reviewer output constant so the only difference between the two kept sets is the profile gate. Two dispatches would differ by model nondeterminism and would prove nothing about the gate. Record the order the two profiles were consumed in.

Compute the subset verdict inline from the two kept sets — a set difference plus two sizes. Record the verdict and, separately, whether the comparison was vacuous. If either kept set is empty, record the verdict as vacuous and say plainly that the live run is not the primary evidence in that case. Per D-08, E-12 still closes: the deterministic backstop Plan 04 installed is the permanent regression guard, and a zero-finding run closes the item on that test plus this honest note. What is prohibited is the Phase 5 failure mode — reporting a vacuous comparison as though it were a held relation. Recording it as vacuous and naming the test as the evidence is the sanctioned outcome, not a shortfall.

Then verify the sidecar is unchanged against the pre-run digest. A changed sidecar means the override did not isolate the run and is a defect for the ledger, not something to work around.

Write `06-RECEIPTS.md` with three sections: the run receipt, the E-12 receipt, and a placeholder heading for the governance receipt Task 3 fills. Follow the Phase 1 evidence format the discretion note names — exact command, exit code, decisive tail lines, version block — with the target path elided from every command and every tail line. A decisive tail line that cannot be sanitized is summarized as a count rather than quoted; the format is a template, not an override of D-09.

Per D-10 the receipt must carry the three things that prove dispatch happened: the review-source counts showing per-file reviewer returns rather than the whole-set skip, the findings ledger headline counts, and the sealed manifest state. Add the reviewer run configuration, both kept-set sizes, the subset verdict, the vacuity flag, and the two SHAs. Include nothing else. Before staging, re-read the file line by line against the prohibitions: no path below the target root, no code excerpt, no finding body.

If the run surfaces a new defect, record it in the receipt and carry it into Task 3's ledger. Do not fix code in this plan — the four shipping PRs are merged and a fifth would restart the governance rail for a defect that has not been triaged.

Commit as `docs(06-05):` staging the receipt, root `README.md`, and root `CHANGELOG.md`. No plugin version bump — nothing under `plugins/` is staged.
  </action>
  <verify>
    <automated>cd /Users/christopher/Documents/Development/_me/cjbischoff-claude-code-tools && rg -c 'review_source_skipped' .planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md && rg -n 'vacuous' .planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md && git diff --stat HEAD -- plugins/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - D-06: the receipt reports a per-file reviewer return count greater than zero from the real SKILL.md-driven dispatch against mando, states the Phase 5 whole-set skip count beside it as the baseline, and covers both profiles with the security-kept ⊆ general-kept check computed on those reviewer results.
    - The receipt states the subset verdict and the vacuity flag as two separate facts, with both kept-set sizes.
    - The receipt names the reviewer model, concurrency, and timeout actually used.
    - D-07: the receipt records both SHAs of Phase 5's exact `base..head` range, re-read from the Phase 5 receipts rather than copied from this plan, alongside all exit codes, all seal states, the findings-ledger headline counts, and the sealed manifest state — the three D-10 dispatch proofs.
    - Every command and tail line in the receipt has the target path elided; any tail line that could not be sanitized appears as a count instead of a quote.
    - If the verdict is vacuous, the receipt names Plan 04's deterministic backstop as the evidence closing E-12 and does not present the vacuous comparison as a held relation.
    - The receipt records the order the two profiles were consumed in.
    - The receipt states that the second consume read copied reviewer output rather than a second dispatch.
    - The Phase 5 sidecar digest is identical before and after the run, and the receipt says so.
    - `git diff --stat HEAD -- plugins/` is empty for this task's commit.
    - No line of the receipt contains a path below the target repository root, a code excerpt, or a finding body.
    - One `docs(06-05):` commit with no plugin version bump, and `prek run` passing.
  </acceptance_criteria>
  <done>A real dispatch ran into an isolated workspace, the subset verdict is recorded with its vacuity stated, the Phase 5 sidecar is untouched, and the receipt discloses only what Task 1 authorized.</done>
</task>

<task type="auto">
  <name>Task 3: Give every Phase 5 defect a terminal disposition and close the phase</name>
  <files>.planning/phases/06-remediation-and-governed-release/06-DEFECTS.md, .planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md, .planning/STATE.md, README.md, CHANGELOG.md</files>
  <read_first>
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` in full — all ten rows, their current states, and the reproduction each records. Every row needs a terminal state here.
    - `.planning/phases/06-remediation-and-governed-release/06-01-SUMMARY.md` through `06-04-SUMMARY.md` — what each plan actually landed, including the recorded CodeRabbit outcomes, the version transitions, and the doc surfaces Plan 03 enumerated. The ledger cites these rather than re-deriving them.
    - `.planning/phases/06-remediation-and-governed-release/06-CONTEXT.md` — D-05 and D-11, which fix how a row that cannot be fixed is dispositioned versus one that is fixed.
    - `.planning/REQUIREMENTS.md` lines 134-143 — the exact REL-01, REL-02, and REL-03 wording the closure claim is measured against.
    - `.planning/STATE.md` — the current position and decision log, and the shape a phase-close entry takes.
    - The root `README.md` and root `CHANGELOG.md` — the sections a repo-level change updates.
  </read_first>
  <action>
Write `06-DEFECTS.md` covering all ten Phase 5 rows. Each row gets a terminal state — fixed, dispositioned, or newly-found-and-carried — plus the plan and commit that closed it, or the written reason it cannot be closed. No row stays in the deferred state it arrived in; that is the whole content of REL-01's first clause.

Two rows are dispositions rather than fixes, per D-05 and D-11. The mixing-criterion row cannot be satisfied because the target directory has only five commits in its entire history, so the criterion is unsatisfiable rather than unmet — record the observation and the count, and state that the criterion should be revised rather than retried. Any row the Task 2 run reopened or newly surfaced is carried with its reproduction, and explicitly not fixed here, with the reason: the four shipping PRs are merged and an untriaged fifth would restart the governance rail.

Then fill the governance receipt section in `06-RECEIPTS.md`. For each of the four shipping PRs, record its branch name, the commit types in order, the version transitions, and the CodeRabbit outcome as observed. Add this plan's own commits as the fifth row, showing zero shipping files staged and therefore no bump — that row is the REL-02 lower bound and is deliberate, so label it as such rather than leaving a reader to wonder if a bump was missed.

Then re-assert REL-03 on the merged milestone branch: run the dependency-table assertion Plan 04 installed, against the merged state, and record the result. Running it after all four merges is what makes the claim independent of merge order; running it only inside Plan 04's branch would not.

State the closure claim against the four phase success criteria one at a time, and for each, cite the receipt or ledger row that carries it. If a criterion is met only in part, say which part and do not round it up — a phase that closes with one criterion honestly marked partial is worth more than one that closes clean by assertion. A vacuous E-12 verdict is not such a case: D-08 closes E-12 on the deterministic backstop plus an honest note, so that criterion is met by the test and the receipt states which evidence carries it.

Update `.planning/STATE.md` to close Phase 6. Update the root `README.md` and root `CHANGELOG.md` per the repo-level rule.

Commit as `docs(06-05):`, staging only `.planning/` documents and the two root docs. Then open the PR with `gh pr create --base docs/milestone-v5-diff-review`, never against `main`. The body states only what the diff does, in plain factual language, and uses none of the adjectives repo governance bans in a PR description. Wait for CodeRabbit per the state Plan 01 recorded, then merge and delete the branch.

The milestone-to-main merge is out of scope for this plan — D-14 makes it a single separate step at milestone close.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `06-DEFECTS.md` contains one row per Phase 5 ledger row — ten rows minimum — and every row's state is terminal; no row reads as deferred.
    - Each fixed row cites the plan number and the commit that closed it.
    - Each dispositioned row states the written reason it cannot be fixed, and the mixing-criterion row records the observed commit count.
    - The governance receipt has five rows: four shipping PRs with branch, commit types, version transitions, and CodeRabbit outcome, plus this plan's zero-shipping-file row labelled as the deliberate REL-02 lower bound.
    - The dependency-table assertion was re-run on the merged milestone branch and its result is recorded, establishing REL-03 independently of merge order.
    - Each of the four phase success criteria is addressed individually with a citation, and any partially-met criterion is labelled partial rather than rounded up.
    - `.planning/STATE.md` shows Phase 6 closed.
    - Root `README.md` and root `CHANGELOG.md` are updated, and `prek run` passes.
    - `git diff --stat HEAD~1 -- plugins/` is empty for this task's commit — no plugin file staged, no version bump.
    - `gh pr view <n> --json baseRefName` reports `docs/milestone-v5-diff-review`.
    - The PR is merged and the branch deleted; the milestone-to-main merge is not performed.
  </acceptance_criteria>
  <done>All ten Phase 5 rows carry a terminal state, the governance and REL-03 receipts are complete, the closure claim is stated per criterion without rounding, and the phase is closed.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | Location |
|---|---|---|
| run receipt | markdown section in the Phase 1 evidence format: sanitized commands, exit codes, sanitized decisive tail lines, version block, seal states, sealed manifest state, findings-ledger headline counts, per-file reviewer return count against the Phase 5 baseline, reviewer run configuration, both SHAs | `.planning/phases/06-remediation-and-governed-release/06-RECEIPTS.md` |
| E-12 receipt | markdown section: both kept-set sizes, subset verdict, vacuity flag, consume order, statement that the second consume read copied output, and which evidence closes E-12 under D-08 | same file |
| governance receipt | markdown table: five rows covering the four shipping PRs plus this plan's zero-bump row | same file |
| REL-03 re-assertion result | recorded outcome of the dependency-table test on the merged milestone branch | same file |
| Phase 6 defect ledger | new file, one terminal row per Phase 5 row | `.planning/phases/06-remediation-and-governed-release/06-DEFECTS.md` |
| Phase 6 close entry | STATE.md update | `.planning/STATE.md` |
| `docs/phase-06-receipts-and-defect-ledger` | git branch | git |

No code. No file under `plugins/` is modified. No plugin version bump. Two commits, both `docs` type, both staging only planning and root documents.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Target sidecar to this repository | audit output about a third-party codebase crosses into a public plugin marketplace repository, permanently, via git history. |
| Reviewer subagent to consume step | per-file reviewer returns are model-generated text that the consume step parses and gates; the returns are untrusted input to that parser. |
| Phase 5 workspace to Phase 6 run | a resume against an existing workspace is guarded by identity; the override flag is the sanctioned way past it and must isolate rather than weaken. |
| Evidence to closure claim | the receipt is what a reader trusts instead of re-running the phase; an overstated receipt is worse than a missing one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | committed receipts | high | mitigate | Findings about a third-party repository would be permanently published in a public marketplace repo. The boundary is fixed by a blocking decision checkpoint before any write, the prohibitions enumerate what may not appear, and the receipt is re-read against them before staging. |
| T-06-05-02 | Information Disclosure | target checkout path | high | mitigate | A local path below the target root discloses the operator's filesystem layout and the target's internal structure. Commands are recorded with the target path elided; only commit SHAs are recorded, which are not paths. |
| T-06-05-03 | Repudiation | E-12 verdict | high | mitigate | A vacuous verdict reported as a pass reproduces the exact Phase 5 defect. Verdict and vacuity are recorded as two separate facts with both set sizes; a vacuous result is explicitly not presented as a held relation, and per D-08 closure is attributed to Plan 04's deterministic backstop instead. |
| T-06-05-04 | Repudiation | dispatch proof | high | mitigate | A run that silently fell back to the whole-set path would look like a successful run. The per-file reviewer return count is recorded beside the Phase 5 whole-set skip count, so the count flip is the proof rather than a claim of success. |
| T-06-05-05 | Tampering | Phase 5 workspace | high | mitigate | Writing into the Phase 5 sidecar would destroy the prior phase's evidence and mask whether the override actually isolated the run. A digest is taken before the run and compared after, and writing to that workspace is prohibited. |
| T-06-05-06 | Elevation of Privilege | resume-identity guard | high | mitigate | Reaching a second profile by weakening the guard rather than by overriding the workspace would remove a real protection. The guard is untouched; the sidecar's survival is the evidence, and Plan 01 already pinned the guard with a test. |
| T-06-05-07 | Tampering | reviewer returns as parser input | medium | accept | Model-generated reviewer text is untrusted input to the consume step. The consume step's own seal, position, reflection, and receipt gates are the existing controls; this plan exercises them and records their states rather than adding new parsing. A gate failure here becomes a ledger row. |
| T-06-05-08 | Repudiation | closure claim | medium | mitigate | Rounding a partially-met criterion up to met would make the phase close on an assertion. Each of the four criteria is addressed individually with a citation, and partial is labelled partial. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and modifies no packaging file. No install task exists, so the legitimacy gate has no input. REL-03 is re-asserted here by running the existing test, not by adding one. |
</threat_model>

<flagged_assumptions>
- Unresolved: whether the dispatched run will produce a non-vacuous E-12 comparison. Planning cannot decide this; only the run can. Either outcome closes E-12 per D-08 — a non-vacuous live result is the primary evidence, and a vacuous one closes on Plan 04's deterministic backstop plus an honest receipt note. The prohibited outcome is neither of those: it is a vacuous comparison reported as a held relation, which is the Phase 5 defect this plan exists to close.
- Unresolved: the exact SHA range. D-07 makes the Phase 5 receipts authoritative and requires the executor to re-read them. The values recorded in this plan are the expected ones; a discrepancy is a ledger row, not something to reconcile silently.
- Unresolved: the reviewer run configuration — model, concurrency, timeout — is at discretion per CONTEXT. The values are chosen at execution time against this range and recorded, because an unrecorded configuration makes the receipt unreproducible.
- Unresolved: whether the Task 2 run surfaces new defects. If it does they are carried in the ledger with their reproduction and explicitly not fixed, since the four shipping PRs are merged and an untriaged fifth would restart the governance rail.
- Judgement recorded: the milestone-to-main merge is deliberately out of scope. D-14 makes it one separate step at milestone close, so a plan that performed it would close the milestone as a side effect of closing a phase.
- Closed here: the REL-02 empty case carried as a backstop truth in Plans 01 and 04. This plan's commits stage zero shipping files, so no bump occurs, and the governance receipt labels that row as the deliberate lower bound rather than an omission.
</flagged_assumptions>

<verification>
- Sanitization: `rg` the committed receipt for the target's local path prefix, for fenced code blocks, and for finding-body markers — all must return nothing.
- Dispatch proof: the receipt's per-file reviewer return count is greater than zero and the Phase 5 whole-set skip count is stated beside it.
- Isolation: the Phase 5 sidecar digest matches before and after.
- E-12: verdict and vacuity flag are both present, with both kept-set sizes.
- Ledger completeness: every Phase 5 row appears with a terminal state.
- REL-03: the dependency-table test passes on the merged milestone branch.
- No code touched: `git diff --stat docs/milestone-v5-diff-review...HEAD -- plugins/` is empty for this plan's branch.
- Suite still green: `cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/ -q`.
- Hooks: `prek run` passes on each commit.
</verification>

<success_criteria>
- A real per-file reviewer dispatch ran against the target range into an isolated Phase 6 workspace, and the receipt shows the count flip that proves it.
- The Phase 5 sidecar is byte-unchanged, proving the resume guard was satisfied by override and not weakened.
- The E-12 verdict is recorded with its vacuity stated, computed over one identical reviewer output set consumed twice, and the receipt names which evidence closes the item under D-08.
- All ten Phase 5 defect rows carry a terminal state.
- The governance receipt covers four shipping PRs plus this plan's deliberate zero-bump row, and REL-03 is re-asserted on the merged milestone branch.
- Every committed line respects the boundary confirmed in Task 1.
- Phase 6 is closed in STATE.md; the milestone-to-main merge is left for milestone close.
</success_criteria>

<output>
Create `.planning/phases/06-remediation-and-governed-release/06-05-SUMMARY.md` when done. State the boundary option selected in Task 1, the E-12 verdict and whether it was vacuous, and any defect the run newly surfaced.
</output>