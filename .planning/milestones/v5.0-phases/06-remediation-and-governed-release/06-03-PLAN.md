---
phase: 06-remediation-and-governed-release
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/report.py
  - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/README.md
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_report.py
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_docs_invariants.py
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md
  - plugins/sec-overlay/skills/sec-overlay/agents/redteam.md
  - plugins/sec-overlay/skills/sec-overlay/agents/README.md
  - plugins/sec-overlay/CLAUDE.md
  - plugins/sec-overlay/.claude-plugin/plugin.json
  - plugins/sec-overlay/CHANGELOG.md
autonomous: true
requirements: [REL-01, REL-02]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REL-01: a deps-class finding whose package identifier carries a leading scope renders its real package name on the Fix line, never an empty pair of backticks."
    - "REL-01: an unscoped package with a version suffix still renders the bare name, so the fix for the scoped case does not regress the common case (adjacency)."
    - "REL-01: a package identifier with no version suffix renders unchanged, and an absent identifier renders the existing unknown-package placeholder rather than an empty string (empty case)."
    - "REL-01: when an identifier contains more than one separator, the version boundary is taken as the last one, so scope and name survive intact (ordering)."
    - "REL-01: `agents/redteam.md` describes the two mechanical conditions `wants_runtime` actually evaluates, and states that the status condition alone forces inclusion regardless of the disposition field."
    - "REL-01: the anti-manipulation and tool-trust constant blocks in `agents/redteam.md` are byte-identical before and after the rewrite."
    - "REL-01: no live doc surface under `plugins/sec-overlay/` describes the vendored semgrep ruleset as a version-controlled sub-repository, given no such tracking file exists in this repo's history."
    - "REL-01: `helpers/tests/README.md` attributes the cwd-scoping bug's late discovery to the mocked git runner in the surrounding fixtures, not to those tests bypassing the CLI entry point."
    - "REL-02: each commit in this plan carries its own version bump and CHANGELOG entry in the same commit (adjacency), and the version sequence is strictly increasing with no reuse or skip (ordering)."
    - { statement: "REL-01 ordering for doc corrections: the order the four doc defects are fixed in does not change the final set of corrections", verification: backstop }
  artifacts:
    - "plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/report.py — corrected package-name extraction on the deps Fix line"
    - "plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_report.py — five new cases covering scoped, unscoped, versionless, absent, and multi-separator identifiers"
    - "plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_docs_invariants.py — two code-derived doc guards"
    - "plugins/sec-overlay/skills/sec-overlay/agents/redteam.md — prose matching the two-way mechanical split"
    - ".planning/phases/06-remediation-and-governed-release/06-03-SUMMARY.md"
  key_links:
    - "The package-name extraction must split on the last separator, not the first — splitting on the first is precisely the bug, because a scoped identifier begins with one."
    - "The empty-result fallback must return the original identifier, so a versionless scoped name does not become an empty string by a second route."
    - "The redteam prompt guard derives its expected strings from the predicate's own two trigger values, so the guard cannot drift from the code it describes."
  prohibitions:
    - "No edit to helpers/sec_overlay/models.py."
    - "No edit to helpers/sec_overlay/evidence.py."
    - "No change to any `fingerprint()` output value."
    - "No new entry under `[project] dependencies` in helpers/pyproject.toml."
    - "No change to `redteam.py` — D-02 reconciles this gap in prose only, and the status-forces-inclusion behavior is the intended safe default."
    - "No change to any anti-manipulation or tool-trust constant block in an agent prompt."
    - "No edit to a historical CHANGELOG entry — a past entry records what was believed at the time and is not corrected retroactively."
    - "No `git add -A`, `git add .`, or `git commit -a`."
    - "No `--no-verify` on any git command."
    - "No commit on `main`."
    - "No `Co-Authored-By` trailer on any commit."
    - "No `rm -rf` — use `trash`."
---

<objective>
Close the four remaining fixable rows from the Phase 5 ledger that are neither CLI nor pipeline work: the deps-finding template rendering a blank package name (D-04), the red-team prompt describing a three-way split the code never implemented (D-02), the false claim that the vendored semgrep ruleset is a tracked sub-repository (D-04), and the wrong explanation of why the cwd-scoping bug survived the test suite (D-04).

Purpose: each of these is a shipped document or template asserting something untrue. Dead documentation misleads the next maintainer and the next agent, so the fix is to make the prose match the mechanics — not to add a caveat next to the wrong claim.

Output: a merged PR containing one template fix with five test cases, two code-derived doc guards, and three corrected doc surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-remediation-and-governed-release/06-CONTEXT.md
@.planning/phases/06-remediation-and-governed-release/06-PATTERNS.md
@.planning/phases/06-remediation-and-governed-release/06-01-SUMMARY.md
@.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md
@CLAUDE.md
</context>

## Governance rail

This is the doc-corrections PR in D-13's thematic grouping. It also carries the deps-template fix, which D-13's suggested list placed with the pipeline wiring: that fix is a rendering correction in `report.py` with no relation to the phase table, so grouping it with the other output-correctness fixes keeps both PRs single-concern. D-13 leaves exact grouping to discretion; this is that discretion exercised, recorded so a reviewer does not read it as drift.

Branch `fix/deps-template-and-doc-corrections`, forked from `docs/milestone-v5-diff-review` and merged back into it (D-14). Increments follow D-16: every commit here is `test`, `fix`, or `docs` type, so every bump is patch. Every commit staging a shipping file also stages the bumped `plugin.json` and a new `plugins/sec-overlay/CHANGELOG.md` entry, plus the immediate folder's `README.md` for each staged file. Read the current version out of `plugin.json` before each commit; if the value differs from what an earlier plan projected, the increment rule governs, not the projection. All commits here are `test`, `fix`, or `docs` type, so every bump is a patch bump. Test-first for the code change. Explicit paths only, no `--no-verify`, no `Co-Authored-By` trailer. Follow the CodeRabbit outcome Plan 01 recorded rather than re-testing it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the real package name on a deps finding's Fix line (D-04)</name>
  <files>plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_report.py, plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md, plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/report.py, plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/README.md, plugins/sec-overlay/.claude-plugin/plugin.json, plugins/sec-overlay/CHANGELOG.md</files>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/report.py` lines 70-105 — the deps-class detail block: how the package identifier is read off the finding, the unknown-package fallback string, and the Fix line that interpolates a truncated form of that identifier.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_report.py` — the existing per-finding detail-render tests, their finding fixtures, and how they assert on rendered markdown text.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/sca.py` (or whichever module populates a deps finding's package identifier) — the real shapes that field can hold, so the test cases are drawn from reality rather than invented.
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` — the deps-template row, for the reported symptom.
  </read_first>
  <behavior>
    - Identifier `@scope/name@1.2.3` renders the Fix line naming `@scope/name`.
    - Identifier `name@1.2.3` renders the Fix line naming `name`.
    - Identifier `@scope/name` with no version renders the Fix line naming `@scope/name`.
    - An absent or whitespace-only identifier renders the existing unknown-package placeholder, unchanged.
    - An identifier carrying more than one separator resolves at the last one, so everything left of the version survives.
    - In all five cases the rendered Fix line contains no empty pair of backticks.
  </behavior>
  <action>
Write the failing tests first in `test_report.py`, one per case above, named for the behavior: a scoped-with-version case, an unscoped-with-version case, a scoped-versionless case, an absent-identifier case, and a multi-separator case. Add a sixth assertion shared across all five, checking the rendered Fix line never contains an empty backtick pair — that is the symptom the defect ledger actually reports, so it belongs in the suite directly rather than only implied by the five name assertions. Run them, capture RED, and commit in a `test(06-03):` commit with a patch bump and a CHANGELOG entry.

Then fix `report.py`. The root cause is that the identifier is truncated at its *first* separator, and a scoped identifier begins with that separator, so the slice left of it is the empty string. Change the truncation to take everything left of the *last* separator, and fall back to the untruncated identifier when that yields an empty result — which covers the scoped-versionless case by a single expression rather than a second branch. This is one line. Do not introduce a parser, a helper function, a regex, or a package-identifier dataclass for it; the whole correction is which separator the split targets plus a falsy fallback.

Change nothing else on that line or in the surrounding block. In particular, leave the unknown-package fallback string exactly as it is — it is already correct and the tests pin it.

Run the full `test_report.py`, then the whole suite, then `ruff` and `ty`. Commit GREEN as `fix(06-03):` with a patch bump and CHANGELOG entry.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_report.py tests/test_sca.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` reports zero failures across the whole suite.
    - All five new cases pass, and the shared empty-backtick-pair assertion is present in each.
    - `git diff` for `report.py` shows a one-line change inside the deps detail block and nothing else.
    - No new function, class, or regex was added to `report.py`.
    - `git log --oneline` shows one `test(06-03):` and one `fix(06-03):` commit for this task, each with `plugin.json` and `CHANGELOG.md` staged.
    - `uv run ruff check .` and `uv run ty check` report zero findings.
  </acceptance_criteria>
  <done>Every deps-class finding names its real package on the Fix line, with the scoped, unscoped, versionless, absent, and multi-separator cases each pinned by a test.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the red-team prompt to the two-way mechanical split (D-02)</name>
  <files>plugins/sec-overlay/skills/sec-overlay/agents/redteam.md, plugins/sec-overlay/skills/sec-overlay/agents/README.md, plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_docs_invariants.py, plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md, plugins/sec-overlay/.claude-plugin/plugin.json, plugins/sec-overlay/CHANGELOG.md</files>
  <read_first>
    - `plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/redteam.py` lines 30-50 — the `wants_runtime` predicate. It is a two-term disjunction: one term compares `runtime_disposition` against a single string value, the other compares `status` against a single `FindingStatus` member. Read both trigger values exactly; the prose must describe these and nothing else.
    - `plugins/sec-overlay/skills/sec-overlay/agents/redteam.md` lines 1-60 — the current three-category description, which invents a "neither" case routed through `open_questions`, and the surrounding structure the rewrite must preserve.
    - `plugins/sec-overlay/skills/sec-overlay/references/prompt-constants.md` — the constant blocks `redteam.md` imports, which must stay byte-identical.
    - Two sibling prompts under `plugins/sec-overlay/skills/sec-overlay/agents/` — for the prose conventions a rewrite must match.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_docs_invariants.py` — the existing style for a test that couples a doc's text to a value read out of the code.
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` — the redteam prose row, including the live observation that a finding marked with the exempting disposition still entered the runtime bucket because its status forced inclusion.
  </read_first>
  <action>
Rewrite the mechanical-split section of `agents/redteam.md` so it describes what the predicate does: a finding enters the runtime bucket when either of two conditions holds, and the status condition alone is sufficient. State plainly that setting the disposition field to the exempting value does not remove a finding whose status already carries the runtime-testing member, so there is no third category and no finding can be routed out of the bucket by disposition alone. Say why that is deliberate — inclusion is the safe default for a security tool, per D-02 — so a future reader does not "fix" the code to match the old prose.

Keep the rest of the file intact. The anti-manipulation and tool-trust constant blocks stay byte-identical; only the split description changes. Do not touch `redteam.py`: D-02 is explicit that the behavior stays and the prose moves.

Then add a guard in `test_docs_invariants.py`. Read the predicate's two trigger values out of the code — the disposition string and the status member's value — and assert each appears in the prompt file's text. Deriving both from the code is what makes the guard durable: a future rename in `redteam.py` fails the test instead of silently re-opening the same drift. Do not assert on the absence of the old three-way wording; a negative prose assertion is brittle and would break on any legitimate rewording.

Commit as `docs(06-03):` with a patch bump and a CHANGELOG entry. `agents/README.md` is staged because it is the immediate folder's README; add a line there only if it summarises the prompt's contract, otherwise stage it unchanged is not permitted — make a real, minimal entry.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_docs_invariants.py tests/test_prompts.py tests/test_references_caps.py tests/test_redteam.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` reports zero failures.
    - The new doc-invariant test reads both trigger values from `sec_overlay.redteam` or `sec_overlay.models` at runtime, with no hardcoded copy of either value in the test body.
    - `git diff docs/milestone-v5-diff-review...HEAD -- plugins/sec-overlay/skills/sec-overlay/references/prompt-constants.md` is empty.
    - `git diff docs/milestone-v5-diff-review...HEAD -- plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/redteam.py` is empty.
    - The rewritten section names exactly two conditions and states that the status condition alone forces inclusion.
    - The rewritten section states why inclusion is the intended default.
    - One `docs(06-03):` commit with `plugin.json` and `CHANGELOG.md` staged.
  </acceptance_criteria>
  <done>The prompt describes the predicate the module actually evaluates, a code-derived test holds it there, and `redteam.py` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two false doc claims, then ship the PR (D-04)</name>
  <files>plugins/sec-overlay/CLAUDE.md, plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md, plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_docs_invariants.py, plugins/sec-overlay/.claude-plugin/plugin.json, plugins/sec-overlay/CHANGELOG.md</files>
  <read_first>
    - `plugins/sec-overlay/CLAUDE.md` — the setup section describing how the vendored semgrep ruleset is obtained, which asserts it is a version-controlled sub-repository and tells the reader to use a recursive clone.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md` around line 828 — the cwd-scoping-bug section. Its final sentence already gives the true reason (the other tests inject their own runner); the false part is the earlier framing that they bypass the CLI entry point.
    - `plugins/sec-overlay/skills/sec-overlay/helpers/tests/conftest.py` and two of the mocked-runner review tests — so the corrected explanation describes the fixtures that actually exist.
    - `.gitignore` and the repo root for how the ruleset directory is actually populated and ignored — the corrected prose must describe the real mechanism, not merely delete the wrong one.
    - `.planning/phases/05-end-to-end-verification-audit-review/05-DEFECTS.md` — the submodule-claim row and the WR-02 row.
    - `.planning/phases/06-remediation-and-governed-release/06-01-SUMMARY.md` — the recorded CodeRabbit outcome that governs this PR's wait.
  </read_first>
  <action>
First, find every live surface carrying the sub-repository claim, not only the one the ledger names. Search the tracked docs under `plugins/sec-overlay/` for the setup instruction; the ledger row names the plugin manual, but the same assertion is repeated on several other doc surfaces, and correcting one while leaving the rest would fix the symptom and leave the defect. Correct each live surface to describe the real mechanism: a plain ignored directory populated by a shallow clone, with no tracking file anywhere in this repo's history. Leave historical CHANGELOG entries alone — a past entry records what was believed at the time and is not rewritten.

Second, correct the cwd-bug explanation in the tests README. Replace the claim that the sibling tests missed the bug by bypassing the CLI entry point with the true cause: they inject their own runner, so no real subprocess ever executed against a wrong working directory. Keep the section's existing shape and its already-correct closing sentence.

Third, add a doc guard in `test_docs_invariants.py`: assert that when no tracking file for a sub-repository exists at the repo root, no tracked doc under the plugin directory instructs the reader to perform a recursive-clone or sub-repository-init step for the ruleset. Derive the doc file list by walking the plugin tree rather than hardcoding paths, so a new doc cannot reintroduce the claim unguarded. Exclude the changelog from that walk, for the historical-record reason above.

Commit as `docs(06-03):` with a patch bump and a CHANGELOG entry, staging each corrected doc plus its immediate folder README.

Then ship the PR. Open it with `gh pr create --base docs/milestone-v5-diff-review`, never against `main`. The body states only what the diff does, in plain factual language, and uses none of the adjectives repo governance bans in a PR description. Wait for CodeRabbit per the state Plan 01 recorded; address any finding before merging. Merge into `docs/milestone-v5-diff-review` and delete the branch.
  </action>
  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_docs_invariants.py -q && cd /Users/christopher/Documents/Development/_me/cjbischoff-claude-code-tools && claude plugin validate .</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_docs_invariants.py -q` passes, including the new ruleset-setup guard.
    - The new guard builds its file list by walking the plugin tree, with no hardcoded doc path list, and excludes the changelog.
    - Every live doc surface that previously described the ruleset as a sub-repository now describes the ignored-directory mechanism; the task receipt lists each file corrected and the count.
    - `git diff docs/milestone-v5-diff-review...HEAD -- plugins/sec-overlay/CHANGELOG.md` adds new entries only and modifies no existing entry line.
    - The tests README's cwd-bug section attributes the miss to the injected runner and no longer to bypassing the entry point.
    - `claude plugin validate .` passes.
    - `gh pr view <n> --json baseRefName` reports `docs/milestone-v5-diff-review`.
    - The PR is merged, the branch is deleted, and the CodeRabbit outcome is written in the plan summary.
    - `uv run pytest tests/ -q` reports zero failures.
  </acceptance_criteria>
  <done>No live doc asserts a sub-repository that does not exist, the cwd-bug explanation names the real cause, a tree-walking guard prevents both from returning, and the PR is merged under governance.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Kind | Location |
|---|---|---|
| corrected package-name truncation | one-line change on the deps Fix line, splitting at the last separator with a falsy fallback | `helpers/sec_overlay/report.py` |
| deps Fix-line scoped-package case | test function | `helpers/tests/test_report.py` |
| deps Fix-line unscoped-package case | test function | `helpers/tests/test_report.py` |
| deps Fix-line versionless case | test function | `helpers/tests/test_report.py` |
| deps Fix-line absent-identifier case | test function | `helpers/tests/test_report.py` |
| deps Fix-line multi-separator case | test function | `helpers/tests/test_report.py` |
| red-team prompt trigger-value guard | test function reading both `wants_runtime` trigger values from the code | `helpers/tests/test_docs_invariants.py` |
| ruleset-setup claim guard | test function walking the plugin doc tree | `helpers/tests/test_docs_invariants.py` |
| two-way mechanical-split section | rewritten prose block | `skills/sec-overlay/agents/redteam.md` |
| corrected ruleset-setup prose | edited section on every live doc surface carrying the claim | `plugins/sec-overlay/CLAUDE.md` and siblings found by the search |
| corrected cwd-bug explanation | edited section | `helpers/tests/README.md` |
| `fix/deps-template-and-doc-corrections` | git branch | git |

No new file. No new runtime dependency. No new module, function, class, or regex in `report.py`. Four commits, four patch bumps.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finding data to rendered report | a package identifier read off a finding is interpolated into markdown a human acts on. |
| Agent prompt to model behavior | `agents/redteam.md` is the instruction surface an LLM executes; wrong prose changes what the security pass does. |
| Documentation to maintainer action | setup docs instruct a human on how to obtain the ruleset a scan depends on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Spoofing | deps Fix line | medium | mitigate | A blank package name lets a reader act on the wrong dependency or dismiss the finding as unrenderable. The five cases plus the shared empty-backtick assertion pin the real name in every shape the identifier takes. |
| T-06-03-02 | Tampering | package-identifier truncation | medium | mitigate | The fix reads the identifier and splits it; it never uses that value as a path, a shell argument, or an import, so no injection sink is introduced. The change is a string slice, asserted by test. |
| T-06-03-03 | Elevation of Privilege | red-team prompt prose | high | mitigate | Prose claiming a finding can be routed out of the runtime bucket by disposition alone could lead a future change to actually implement that exemption, dropping runtime-dependent security findings. The rewrite states the status condition is sufficient and says why, and a code-derived test holds the prose to the predicate. |
| T-06-03-04 | Tampering | agent prompt constant blocks | high | mitigate | The anti-manipulation and tool-trust blocks are the prompt's own defence against instruction injection. They stay byte-identical, asserted by an empty diff on the constants file and a scoped review of the prompt diff. |
| T-06-03-05 | Spoofing | ruleset-setup docs | medium | mitigate | A maintainer following a recursive-clone instruction that cannot work may proceed with an absent or stale ruleset and read a thin scan as a clean one. Every live surface is corrected and a tree-walking guard prevents reintroduction. |
| T-06-03-06 | Repudiation | test-suite explanation | low | mitigate | An explanation that misattributes why a real bug survived the suite leads the next author to trust the wrong fixture shape. Corrected to name the injected runner. |
| T-06-03-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. `dependencies` stays empty, asserted in Plan 04; no install task exists, so the legitimacy gate has no input. |
</threat_model>

<flagged_assumptions>
- Unresolved: the full set of doc surfaces carrying the sub-repository claim. D-04 names the plugin manual explicitly, and the same assertion is known to appear on several sibling surfaces. Task 3 requires a search rather than a fixed list, and requires the receipt to enumerate what was found. Correcting every live surface is root-cause work on the named defect, not new scope; the tree-walking guard is what makes the claim stay closed.
- Unresolved: whether `agents/README.md` currently summarises the red-team prompt's contract. If it does not, Task 2's minimal entry there is a one-line addition rather than an edit; the governance hook requires the file staged either way.
- Backstop only: the REL-01 ordering truth for doc corrections — that fixing them in a different order yields the same final set — is asserted structurally rather than exercised, since the four corrections touch disjoint files and cannot interact.
</flagged_assumptions>

<verification>
- Full suite: `cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/ -q` — zero failures.
- Lint and types: `uv run ruff check .` and `uv run ty check` — zero findings.
- Behavior untouched where it must be: `git diff docs/milestone-v5-diff-review...HEAD -- .../redteam.py .../prompt-constants.md` is empty.
- Frozen contract untouched: `git diff docs/milestone-v5-diff-review...HEAD --stat -- .../models.py .../evidence.py` is empty.
- Dependencies untouched: `git diff docs/milestone-v5-diff-review...HEAD -- plugins/sec-overlay/skills/sec-overlay/helpers/pyproject.toml` is empty.
- History preserved: no existing CHANGELOG entry line is modified.
- Plugin validity: `claude plugin validate .` passes.
</verification>

<success_criteria>
- The deps Fix line names the real package for scoped, unscoped, versionless, absent, and multi-separator identifiers, each pinned by a test, with no empty backtick pair in any case.
- `agents/redteam.md` describes the two-condition predicate and the deliberate status-forces-inclusion default; `redteam.py` and the constant blocks are byte-identical.
- No live doc surface asserts a sub-repository that has never existed in this repo.
- The tests README names the injected runner as the reason the cwd bug survived the suite.
- Two code-derived doc guards are in the suite.
- Four governed commits, four patch bumps, one merged PR on the milestone branch.
</success_criteria>

<output>
Create `.planning/phases/06-remediation-and-governed-release/06-03-SUMMARY.md` when done. List every doc surface corrected for the ruleset claim, with the count, so the Plan 05 defect ledger can cite it.
</output>
