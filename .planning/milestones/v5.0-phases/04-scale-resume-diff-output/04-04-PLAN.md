---
phase: 04-scale-resume-diff-output
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on: [04-01, 04-02, 04-03]
autonomous: true
requirements: [OUT-01, SCALE-02, SCALE-03]

files_modified:
  - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_cli.py
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_review_live.py
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_review_tracer.py
  - plugins/sec-overlay/skills/sec-overlay/SKILL.md
  - plugins/sec-overlay/skills/sec-overlay/helpers/README.md
  - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/README.md
  - plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md
  - plugins/sec-overlay/CHANGELOG.md
  - plugins/sec-overlay/.claude-plugin/plugin.json

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "OUT-01: after any run_review() call that reaches a seal, the coverage_manifest block embedded in artifacts/review_comments.json carries a non-null seal that equals the seal in artifacts/coverage_manifest.json — for a complete run and for a partial run."
    - "SCALE-03: `cli review --model <id>` is accepted by argparse, main() forwards it to run_review(model=...), and a resumed run through main() with a changed --model exits 2 with a stderr message naming the model change."
    - "SCALE-02: when a review unit's git fetch hangs, run_review returns without waiting for that fetch to finish and the CLI process exits bounded by --timeout, with the hung git child killed rather than left running."
  artifacts:
    - plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py
    - plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_cli.py
    - plugins/sec-overlay/skills/sec-overlay/SKILL.md
  key_links:
    - "manifest.seal() must run before write_review_comments(ws, comments, manifest.to_dict()) on every path that seals"
    - "argparse --model -> args.model -> run_review(model=) -> CoverageManifest(model=) and check_resume_identity(model=)"
    - "executor try/finally shutdown(wait=False) + per-call subprocess timeout -> bounded process wall clock"
---

<objective>
Close the three failed truths in `04-VERIFICATION.md` (status `gaps_found`, score 2/5):
OUT-01 (embedded manifest seal is always null), SCALE-03 (`--model` has no CLI
surface, so resume identity is dead code in production), SCALE-02 (`--timeout` does
not bound wall-clock time because the executor context manager waits for the hung
worker).

Purpose: the phase's three failed success criteria become observably true, so
re-verification checks exactly what failed. No new capability is added.
Output: a corrected `cli.py`, seven new tests, an updated `--model` CLI contract in
`SKILL.md`, and the governance files each fix commit must carry.

Non-goals — these are recorded warnings in `04-VERIFICATION.md` and stay open:
WR-01 (`_LOCALE_STEM` misses `pt-br`/`pt_BR`/`fil`), WR-02 (SARIF fingerprint omits
line), IN-01 (`--concurrency` is agent-enforced only). Do not touch them.
WR-03 (unbounded line-count prefetch) is closed incidentally by Task 3's per-call
subprocess timeout; do not add separate work for it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scale-resume-diff-output/04-VERIFICATION.md
@.planning/phases/04-scale-resume-diff-output/04-REVIEW.md
@.planning/phases/04-scale-resume-diff-output/04-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol created or changed by this plan, for source-grounding.

**Created**

| Symbol | Location |
|--------|----------|
| `--model` argparse argument on the `review` subparser | `cli.py` review parser block |
| `from functools import partial` import | `cli.py` imports |
| local `seal` variable in `run_review`'s output block | `cli.py` |
| `timeout` keyword parameter on `_fetch_review_unit_files` | `cli.py` |
| `test_review_comments_embedded_manifest_seal_matches_on_disk_after_complete_run` | `tests/test_cli.py` |
| `test_review_comments_embedded_manifest_seal_is_partial_after_partial_run` | `tests/test_cli.py` |
| `test_review_accepts_model_flag_and_forwards_it_to_run_review` | `tests/test_cli.py` |
| `test_review_resume_with_changed_model_exits_2_via_main_entrypoint` | `tests/test_cli.py` |
| `test_review_returns_before_hung_unit_fetch_completes` | `tests/test_cli.py` |
| `test_review_abandoned_unit_fetch_stops_at_the_unit_deadline` | `tests/test_cli.py` |
| `test_review_production_git_calls_carry_subprocess_timeout` | `tests/test_cli.py` |

**Changed**

| Symbol | Change |
|--------|--------|
| `run_review` output block (`cli.py` ~500-540) | seal computed before `write_review_comments`; single write per path |
| `main()` review dispatch (`cli.py` ~670-682) | gains `model=args.model` |
| `run_review` runner resolution (`cli.py` ~314) | production default becomes `partial(subprocess.run, timeout=timeout)` |
| `run_review` unit-fetch block (`cli.py` ~397-411) | explicit executor instance + `try`/`finally` `shutdown(wait=False)` |
| `_fetch_review_unit_files` | computes a monotonic deadline at entry, stops fetching remaining members past it |
| `_make_review_runner` inner `runner` (`tests/test_cli.py:79`) | signature accepts `**kwargs` |
| `_fake_run_for`, `failing_diff` (`tests/test_review_live.py`) | signatures accept `**kwargs` |
| `_fake_run`, `_make_fake_run` (`tests/test_review_tracer.py`) | signatures accept `**kwargs` |
| `SKILL.md` review invocation block and Step 3 Consume | document `--model` and the identical-value convention |
| `tests/README.md` fake-runner convention section | records the `**kwargs` requirement |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seal the coverage manifest before embedding it in review_comments.json (OUT-01)</name>

  <files>
    plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_cli.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/README.md,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md,
    plugins/sec-overlay/CHANGELOG.md,
    plugins/sec-overlay/.claude-plugin/plugin.json
  </files>

  <read_first>
    - `04-VERIFICATION.md` gap OUT-01 — the `missing:` list is the authoritative fix outline.
    - `04-REVIEW.md` CR-01 — carries the drafted replacement for the `cli.py` output block.
    - `cli.py` lines ~500-540 — the defect: `write_review_comments(ws, comments, manifest.to_dict())`
      runs, then `if not selection.reviewable: return 0`, then `if manifest.seal() == "complete": return 0`.
    - `cli.py` lines ~432-462 — the `if prepare:` branch returns 0 before any seal, so a
      `--prepare` run persists no sealed manifest. Leave that branch alone.
    - `review_coverage.py` — `seal()` and `to_dict()`; `seal()` raises on an empty manifest
      (decision T-02-05), which is why the zero-reviewable path must keep its pre-seal write.
    - `review_comments.py` — `write_review_comments` embeds its `manifest_dict` argument under
      the `coverage_manifest` key in `artifacts/review_comments.json`.
    - `tests/test_cli.py:165` `_failing_parse_hunks` — the existing way to force a partial seal.
    - `tests/test_cli.py:79` `_make_review_runner` and `tests/test_cli.py:389` — the end-to-end
      `run_review` harness these tests copy.
  </read_first>

  <behavior>
    - Test 1 (complete run): a `run_review` call over one reviewable file finishes every file, so
      the on-disk manifest seals `complete`; the `coverage_manifest` block inside
      `review_comments.json` has that same non-null seal value.
    - Test 2 (partial run): with `parse_hunks` failing for one of two files the on-disk manifest
      seals `partial` and the embedded block reports `partial`, not null.
    - Both tests read the embedded value and the on-disk `artifacts/coverage_manifest.json` and
      assert equality, so the two artifacts can never disagree again.
    - Regression guard: the zero-reviewable early return still writes a comments file (its
      manifest is empty and cannot be sealed), so `selection.reviewable` empty keeps exit 0.
  </behavior>

  <action>
    Write both tests first and confirm they fail against the current ordering (the embedded seal
    reads null). Commit the red state, then reorder.

    In `run_review`, build `comments` as today. Keep the zero-reviewable early return ahead of the
    seal: when `selection.reviewable` is empty, write the comments file with the unsealed
    `manifest.to_dict()` and return 0 — sealing an empty manifest raises by design (T-02-05).
    For every other path, assign the result of `manifest.seal()` to a local, then call
    `write_review_comments(ws, comments, manifest.to_dict())` exactly once with the post-seal dict,
    then branch on the local: return 0 when the seal is complete, otherwise print the unfinished
    files as today and return 3. Do not write the comments file twice on any path, and do not add
    a second `manifest.seal()` call — `seal()` mutates state and the return code must read the
    same value the artifact embeds.

    Governance for this commit: patch-bump `plugins/sec-overlay/.claude-plugin/plugin.json`, add a
    `### Fixed` entry to `plugins/sec-overlay/CHANGELOG.md` naming the null embedded seal, and stage
    the immediate-folder READMEs for every touched folder. Stage explicit paths only.
  </action>

  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_cli.py -k "embedded_manifest_seal" -q</automated>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_cli.py tests/test_review_comments.py tests/test_review_live.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `test_review_comments_embedded_manifest_seal_matches_on_disk_after_complete_run` passes and
      asserts the embedded seal is not None.
    - `test_review_comments_embedded_manifest_seal_is_partial_after_partial_run` passes and asserts
      the embedded seal equals `partial`.
    - Both new tests fail when the write is moved back ahead of the seal (verified by running them
      against the pre-fix ordering before committing the fix).
    - `grep -c "write_review_comments(" plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py`
      returns 2 (the zero-reviewable path and the sealed path) — never 3.
    - `tests/test_cli.py` and `tests/test_review_comments.py` and `tests/test_review_live.py` pass
      with no new failures against the recorded baseline.
  </acceptance_criteria>

  <done>
    A real `run_review` run produces a `review_comments.json` whose embedded `coverage_manifest.seal`
    is non-null and equals the on-disk manifest's seal, for both a complete and a partial run.
  </done>

  <reversibility rating="reversible">
    Ordering change inside one function, reverted by one commit revert; no artifact schema change.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give --model a CLI surface and wire it through main() (SCALE-03)</name>

  <files>
    plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_cli.py,
    plugins/sec-overlay/skills/sec-overlay/SKILL.md,
    plugins/sec-overlay/skills/sec-overlay/helpers/README.md,
    plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/README.md,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md,
    plugins/sec-overlay/CHANGELOG.md,
    plugins/sec-overlay/.claude-plugin/plugin.json
  </files>

  <read_first>
    - `04-VERIFICATION.md` gap SCALE-03 — the `missing:` list, including the recorded failure
      `error: unrecognized arguments: --model opus`.
    - `04-REVIEW.md` CR-02 — the drafted argparse addition and the `main()` call-site change.
    - `cli.py` lines ~576-611 — the `review` subparser: `--base`, `--head`, `--root`, `--profile`,
      `--rule`, `--exclude`, `--prepare`, `--concurrency`, `--timeout`, `--max-git-procs`.
    - `cli.py` lines ~660-690 — `main()`'s review dispatch, which forwards every flag above and no
      `model=`.
    - `cli.py` line ~391 and lines ~320-328 — `CoverageManifest(..., model=model, profile=profile)`
      and the `check_resume_identity(prior_manifest, model=model, profile=profile)` gate that
      returns 2 on `ResumeIdentityError`.
    - `review_coverage.py` — `check_resume_identity` raises
      `resume rejected: model changed from ... to ...`; `to_dict`/`load` round-trip `model`.
    - `cli.py` lines ~432-462 — `--prepare` returns before any seal, so a prepare run persists no
      manifest; the resume test needs a full consume run first.
    - `tests/test_cli.py:389` `test_review_default_bounds_are_8_600_and_16` — the spy-on-`run_review`
      plus `monkeypatch.setattr(subprocess, "run", ...)` plus `cli.main([...])` idiom to copy.
    - `SKILL.md` review invocation block (~L74-95) and Step 3 Consume (~L133) — where the flag and
      the identical-value convention get documented.
  </read_first>

  <behavior>
    - Test 1 (forwarding): `cli.main(["review", "--base", ..., "--model", "opus", ...])` with
      `run_review` spied on records `model == "opus"` in the captured kwargs, and argparse raises
      nothing.
    - Test 2 (resume rejection through argparse): a first `cli.main([...])` consume run over one
      reviewable file persists a manifest carrying its model; a second `cli.main([...])` over the
      same base/head with a different `--model` returns 2, and the captured stderr names the model
      change.
    - Default stays `None`, so every existing invocation without `--model` keeps today's behavior.
  </behavior>

  <action>
    Write both tests first and confirm test 1 fails with an argparse unrecognized-argument error.
    Commit the red state, then wire the flag.

    Add a `--model` argument to the `review` subparser with default `None` and help text saying the
    value is an opaque identity string recorded in the coverage manifest and compared on resume.
    Append `model=args.model` to the `run_review(...)` call in `main()`'s review branch. Change
    nothing in `run_review`, `CoverageManifest`, or `check_resume_identity` — they already accept
    and compare the value; only the CLI surface is missing.

    For the resume test, drive both runs through `cli.main` rather than `run_review` so the argparse
    layer is exercised. The first run must be a full consume run, not `--prepare`, because the
    prepare branch returns before the manifest is sealed and persisted. Assert the exit code is 2
    and that the captured stderr mentions the model field.

    Document `--model` in `SKILL.md`: add it to the review invocation block and state in Step 3
    Consume that the same identity string must be passed on every resumed run, mirroring the
    existing identical-`--root` convention sentence.

    Governance for this commit: patch-bump `plugin.json`, add a `### Fixed` entry to the plugin
    `CHANGELOG.md` naming the missing CLI flag, and stage the immediate-folder READMEs for every
    touched folder. Stage explicit paths only.
  </action>

  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_cli.py -k "model" -q</automated>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run python -m sec_overlay.cli review --help 2>&1 | grep -- "--model"</automated>
    <automated>grep -n "model=args.model" plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -m sec_overlay.cli review --help` lists `--model`; the argparse error recorded in the
      verification report no longer reproduces.
    - `test_review_accepts_model_flag_and_forwards_it_to_run_review` passes and asserts the spied
      `run_review` received `model="opus"`.
    - `test_review_resume_with_changed_model_exits_2_via_main_entrypoint` passes, asserts exit code
      2, and asserts the stderr text names the model change.
    - Both new tests fail when `model=args.model` is removed from `main()`.
    - `SKILL.md` documents `--model` in the review invocation block and states the identical-value
      resume convention.
    - Existing `tests/test_cli.py`, `tests/test_review_live.py`, `tests/test_review_coverage.py`
      pass with no new failures.
  </acceptance_criteria>

  <done>
    `--model` is a real CLI argument, `main()` forwards it, and a resumed run with a changed model
    exits 2 through the argparse entry point.
  </done>

  <reversibility rating="reversible">
    Additive flag with a `None` default; removal restores prior behavior exactly.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound review wall-clock time on a hung unit fetch (SCALE-02)</name>

  <files>
    plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_cli.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_review_live.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/test_review_tracer.py,
    plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/README.md,
    plugins/sec-overlay/skills/sec-overlay/helpers/tests/README.md,
    plugins/sec-overlay/CHANGELOG.md,
    plugins/sec-overlay/.claude-plugin/plugin.json
  </files>

  <read_first>
    - `04-VERIFICATION.md` gap SCALE-02 — the `missing:` list and the recorded evidence
      `1 passed in 4.20s` for a test that declares `timeout=1`.
    - `04-REVIEW.md` CR-03 — the drafted `try`/`finally` replacement, plus its own note that the
      leaked thread and its git child still need a subprocess-level timeout for a complete fix.
    - `cli.py` lines ~397-411 — the unit-fetch block: the executor is used as a context manager, so
      its exit waits for the abandoned worker; `future.result(timeout=timeout)` already handles the
      failure bookkeeping through `TIMEOUT_NOTE`.
    - `cli.py` line ~63 — `TIMEOUT_NOTE = "review unit exceeded --timeout"`.
    - `cli.py` lines ~136-155 `_fetch_review_unit_files` — the per-unit dict comprehension that
      keeps fetching remaining members after the deadline has passed.
    - `cli.py` lines ~110-133 `_fetch_file_review_inputs` — returns the exception instead of
      raising, so a killed git call already funnels into the existing failure path.
    - `cli.py` lines ~304-314 — bound validation, then `import subprocess` and
      `r = runner or subprocess.run`; `r` is the production runner handed to `RepoMemory.for_target`,
      `resolve_ref_sha`, the line-count prefetch, and the unit fetch.
    - `cli.py` lines ~87-107 `_bounded_map` — a separate context-managed executor that completes
      normally; leave it as it is.
    - `tests/test_cli.py:415` `test_review_fetches_files_concurrently_bounded_by_max_git_procs` —
      the existing `time.monotonic()` wall-clock idiom to copy.
    - `tests/test_cli.py:494` `test_review_unit_timeout_fails_every_member_with_timeout_note` — the
      three-locale-sibling unit and the 1.2s sleeping runner; this is the 4.20s test.
    - Fake runner signatures that must accept the new kwarg: `tests/test_cli.py:79`
      (`_make_review_runner`, monkeypatched at 263, 331, 404), `tests/test_review_live.py`
      (`_fake_run_for`, `failing_diff`; ~14 monkeypatch sites), `tests/test_review_tracer.py`
      (`_fake_run`, `_make_fake_run`; 5 sites), and the convention documented in
      `tests/README.md` lines ~61 and ~698.
  </read_first>

  <behavior>
    - Test 1 (return before the fetch finishes): one three-member unit whose runner sleeps well past
      the declared timeout; measured wall time around `run_review` stays under the time the unit's
      full fetch work would take, and the exit code and `TIMEOUT_NOTE` bookkeeping match the
      existing timeout test.
    - Test 2 (worker stops at the deadline): the sleeping runner counts its invocations; after the
      deadline the abandoned worker does not fetch the unit's remaining members, so the recorded
      call count stays below the count a full unit fetch would make.
    - Test 3 (production runner carries a kill): with no injected runner, the object `run_review`
      hands to git calls passes a `timeout` equal to the declared `--timeout` down to
      `subprocess.run`, so a hung child is killed rather than orphaned.
    - Existing behavior preserved: `TIMEOUT_NOTE` stays the recorded note for every member of a
      timed-out unit, the manifest still seals `partial`, exit code stays 3, and
      `_bounded_map` result ordering is unchanged.
  </behavior>

  <action>
    Write all three tests first. Confirm test 1 fails on wall clock against the current code (the
    recorded reproduction is 4.20s for a declared timeout of 1) and that test 3 fails because the
    production runner is bare. Commit the red state, then fix.

    Replace the context-managed executor in the unit-fetch block with an explicit
    `ThreadPoolExecutor(max_workers=min(max_git_procs, len(units)))` instance bound to a local,
    wrapped in `try` / `finally`, where the `finally` calls `ex.shutdown(wait=False)`. Keep the
    submit loop, the `future.result(timeout=timeout)` collection, and the existing
    `TIMEOUT_NOTE` failure mapping byte-for-byte — only the lifetime management changes.

    `shutdown(wait=False)` alone does not bound the process: `concurrent.futures.thread` registers
    an interpreter-exit hook that joins every worker thread, so a worker still blocked inside git
    would hold the process open past the timeout. Close that by killing the child instead of
    abandoning it: import `partial` from `functools` and make the production runner default
    `partial(subprocess.run, timeout=timeout)` so every git call the review path makes inherits a
    kill deadline. Set that per-call value equal to the declared `--timeout`, not a fraction of it,
    so the consuming thread's clock — which starts earlier, at submit time — always expires first
    and the recorded note stays deterministic. An injected `runner` is untouched, so tests keep full
    control.

    Also stop the abandoned worker from doing pointless work: give `_fetch_review_unit_files` a
    `timeout` parameter, compute a monotonic deadline at function entry (not at submit time, since a
    queued unit's wait would otherwise consume its own budget), and once the deadline has passed
    record the remaining members as timed out through the same exception-return path
    `_fetch_file_review_inputs` already uses instead of fetching them.

    The production runner change reaches every fake that gets monkeypatched onto `subprocess.run`,
    so add `**kwargs` to the shared fake runner signatures in `tests/test_cli.py`,
    `tests/test_review_live.py`, and `tests/test_review_tracer.py`, and record that requirement in
    the fake-runner convention section of `tests/README.md`. Do not change what those fakes return.

    Leave `_bounded_map` and its context manager alone; its executor always drains, and its git
    calls now inherit the same kill deadline through the shared runner.

    Governance for this commit: patch-bump `plugin.json`, add a `### Fixed` entry to the plugin
    `CHANGELOG.md` naming the unbounded wall clock, and stage the immediate-folder READMEs for
    every touched folder. Stage explicit paths only.
  </action>

  <verify>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_cli.py -k "timeout or hung or deadline" -q --durations=10</automated>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/test_cli.py tests/test_review_live.py tests/test_review_tracer.py -q</automated>
    <automated>grep -n "shutdown(wait=False)" plugins/sec-overlay/skills/sec-overlay/helpers/sec_overlay/cli.py</automated>
    <automated>cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `test_review_returns_before_hung_unit_fetch_completes` passes and asserts measured wall time is
      below the unit's full fetch work time; it fails against the pre-fix code (reproduction: 4.20s
      for a declared timeout of 1).
    - `test_review_abandoned_unit_fetch_stops_at_the_unit_deadline` passes and asserts the recorded
      runner call count is lower than a full unit fetch would produce.
    - `test_review_production_git_calls_carry_subprocess_timeout` passes and asserts the timeout
      value handed to `subprocess.run` equals the declared `--timeout`.
    - `--durations=10` for the timeout selection shows no test exceeding roughly twice its declared
      timeout.
    - `test_review_unit_timeout_fails_every_member_with_timeout_note` still passes unchanged:
      exit code 3, every member recorded with `TIMEOUT_NOTE`, manifest seal `partial`.
    - `tests/test_review_live.py` and `tests/test_review_tracer.py` pass — no fake runner raises a
      `TypeError` from the new keyword argument.
    - Full-suite run matches the recorded baseline: the only failures are the two pre-existing
      environmental ones (`test_bench.py::test_seed_corpus_is_valid`,
      `test_preflight.py::test_report_finds_vendored_rules_regardless_of_cwd`).
  </acceptance_criteria>

  <done>
    A hung unit fetch no longer holds `run_review` or the CLI process past `--timeout`, the hung git
    child is killed, and the abandoned worker stops fetching at the unit deadline.
  </done>

  <reversibility rating="costly">
    The production-runner change alters the shared fake-runner contract documented in
    `tests/README.md` and touches three test files, so reverting means re-editing all of them. Still
    fully reversible; no artifact or CLI contract changes.
  </reversibility>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking threshold: high. Any `high` threat left unmitigated blocks the phase.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git subprocess -> review harness | A repository or a stalled git process supplies unbounded latency and untrusted file text |
| review harness -> artifact consumer | `artifacts/review_comments.json` is read by a downstream commenter that decides whether findings are complete |
| CLI argv -> run_review | `--model`, `--timeout` and friends are operator-supplied identity and bound values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Repudiation | `run_review` output block, `review_comments.json` | high | mitigate | Task 1: seal before write, so a partial review can never be consumed as complete; both artifacts assert-equal in tests |
| T-04-14 | Spoofing | `--model` identity on resume | medium | mitigate | Task 2: `--model` reaches `check_resume_identity`, so findings cannot be silently attributed to a model that never reviewed them; exit 2 on mismatch |
| T-04-15 | Denial of Service | unit-fetch executor and git children | high | mitigate | Task 3: `try`/`finally` `shutdown(wait=False)`, per-call subprocess kill deadline, worker-side unit deadline |
| T-04-16 | Denial of Service | abandoned worker thread for the timed-out unit | low | accept | Bounded to one unit's remaining members within a single short-lived CLI process; the git child is killed by the per-call timeout, so nothing accumulates across runs |
| T-04-17 | Tampering | operator passes a mismatched `--timeout` per run | low | accept | `--timeout` is an operator bound, already range-validated; a larger value is a deliberate operator choice, not an attacker input |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No install tasks in this plan. The core stays stdlib-only (`functools`, `concurrent.futures`, `subprocess`) with an empty runtime dependency list per REL-03, so no package-legitimacy gate applies |
</threat_model>

<verification>
Run from `plugins/sec-overlay/skills/sec-overlay/helpers`:

1. `uv run pytest tests/ -q` — full suite; only the two recorded environmental failures may fail.
2. `uv run pytest tests/test_cli.py -q --durations=10` — no test exceeds roughly twice its declared timeout.
3. `uv run python -m sec_overlay.cli review --help` — lists `--model`.
4. `grep -n "shutdown(wait=False)" sec_overlay/cli.py` — the executor lifetime fix is present.
5. `grep -c "write_review_comments(" sec_overlay/cli.py` — returns 2.
6. `git log --oneline -6` — six commits (red then green per gap), each Conventional Commits, each fix commit carrying `plugin.json` and `CHANGELOG.md`.
</verification>

<success_criteria>
- All three `must_haves.truths` observable, each proved by a test that fails against the pre-fix code.
- Seven new tests added, all passing.
- `plugins/sec-overlay/.claude-plugin/plugin.json` patch-bumped once per fix commit, each with a matching `CHANGELOG.md` entry.
- `SKILL.md` documents `--model` and the identical-value resume convention.
- Full-suite result equals the recorded baseline; no new failures.
- WR-01, WR-02, IN-01 untouched.
</success_criteria>

<output>
Create `.planning/phases/04-scale-resume-diff-output/04-04-SUMMARY.md` when done.
</output>
