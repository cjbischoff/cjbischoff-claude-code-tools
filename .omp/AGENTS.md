# omp-native context — marketplace governance

This file is auto-discovered at priority 100 (native) and shadows every other
context file at repository-root depth. omp always reads the marketplace rules
regardless of other tool configs (Cursor, Copilot, Claude config).

@../CLAUDE.md
