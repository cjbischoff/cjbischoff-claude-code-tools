# CLAUDE.md — {{plugin-name}} plugin maintainer manual

This file guides a maintainer working on the {{plugin-name}} plugin source. It never loads into a
user's session when the plugin is installed (docs-verified against the Claude Code plugin
runtime).

Governance — branching, commits, changelogs, version bumps — lives in the root `CLAUDE.md`;
nothing here overrides it.

---
## Developing

{{commands to lint, test, and run the plugin's skill during development}}

---
## Documentation

{{which folders carry their own README.md and what each one must cover}}
