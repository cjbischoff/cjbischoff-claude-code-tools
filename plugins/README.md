# plugins/

Holds one directory per Claude Code plugin distributed by this marketplace.

**Naming convention:** the directory name equals the plugin `name` in its `.claude-plugin/plugin.json` and in the marketplace manifest.

**Writers:** Claude Code sessions in this workspace, on a branch, with user review before merge.

## Contents

| Directory | Purpose |
|-----------|---------|
| `sec-overlay/` | Agentic security-audit harness; SAST + multi-agent investigation. Python core is the `sec_overlay` package under `skills/sec-overlay/helpers/`; run instructions resolve paths from `${CLAUDE_PLUGIN_ROOT}`. Test suite green after the rename; plugin manifest validates. The report and red-team renderers tolerate agent-authored `expected_signal` values in any shape (object, bare string, or null) via a shared helper. `skills/sec-overlay/references/prompt-constants.md` now carries 12 verbatim blocks, including `DIAGRAM_STYLE`, `FIELD_OWNERSHIP`, and `QUALIFIER_PROOF`. The `Finding` dataclass and finding schema now carry an `open_questions` field (list of dicts; defaults to `[]`). The phase gate now flags comment-only `file:line` citations as a scrutiny note via `is_comment_line()`. `verify.py` rejects a `deps`-class patch that only bumps to a placeholder version (e.g. `vX.Y.Z`) as `not-fixed`, and `verify_findings` no longer overrides an explicit `validate-fix` not-fixed verdict with a conflicting re-scan result. Context ingestion (`context.py`) now discovers IaC/deployment-config files as a `deployment_config` kind carrying a `deployed_in` env tag, and `Context.diagram` holds a claimed-control status mermaid block rendered into `CONTEXT.md`. The red-team renderer now emits a "Questions to ask" section from `Finding.open_questions` across all discriminated buckets, and the agent prompts import `FIELD_OWNERSHIP`/`DIAGRAM_STYLE`/`QUALIFIER_PROOF` and carry the diagram/deployment-config guidance. Docs synced: `references/README.md`'s schema table now documents `open_questions`, and the skill `CLAUDE.md` block count/list (§6 and §8) matches the current 12 `prompt-constants.md` blocks. Stale Go-rewrite prose removed from the live docs; the finding serialization/schema contract is enforced by `test_contracts.py` and `test_finding_schema.py`. Skill CLAUDE.md refocused on repo mechanics: git/governance, testing requirement, and the prek folder-README hook. Overview README and helpers/README test counts corrected to 575 tests / 2 env-only failures; diagrams and worked example verified against the pipeline. Added six per-folder READMEs (`agents/classes/`, `references/asvs/`, `references/codeguard/`, `references/hunting/`, `helpers/sec_overlay/`, `helpers/tests/`). The overview README's architecture diagram uses the subgraph id `OVERLAY` (label unchanged). Added `cost.aggregate_by_model` and a "Run economics" section (tokens by phase/model, USD estimate) to `report.to_markdown`/`write_report`; test count now 577. The `Finding` dataclass and finding schema now also carry `cluster_id` and `affected_sites` fields (additive, nullable); test count now 579. Added `sec_overlay.selfscore`, a per-run finding-status score persisted to `CampaignState.budget["self_score"]`; test count now 580. SKILL.md now documents token proxy fallback for ambiguous harness token reporting and the per-run self-score call in the C2 postflight phase. Added `sec_overlay.cluster`, a deterministic pass that groups ≥3 same-class, same-sink `raw` findings into one systemic cluster (`cluster_id` + primary `affected_sites`), run immediately after dedupe and before the critic/gate ladder; test count now 582. |
