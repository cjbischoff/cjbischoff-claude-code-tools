# cjbischoff-claude-code-tools

Claude Code plugin marketplace — personal plugins for Christopher Bischoff.

## Installation

```
/plugin marketplace add cjbischoff/cjbischoff-claude-code-tools
/plugin install sec-overlay@cjbischoff-claude-code-tools
```

## Plugins

- **sec-overlay**: agentic security-audit harness (static analysis, tool-receipt gate).

## Development

```bash
claude plugin validate .      # validate plugin + marketplace manifests
prek run                      # run governance hooks
cd plugins/sec-overlay/skills/sec-overlay/helpers && uv run pytest -q   # Python core tests
```

### OpenWiki

First init is local (do not run `--init` in CI). From the repo root, with an Anthropic key in the environment:

```bash
export OPENWIKI_PROVIDER=anthropic
export OPENWIKI_MODEL_ID=claude-sonnet-5
export OPENWIKI_TELEMETRY_DISABLED=1
export DO_NOT_TRACK=1
openwiki code --init --print
```

Keep `openwiki/INSTRUCTIONS.md` and `.openwikiignore`. The generated wiki under `openwiki/` is tracked; start at `openwiki/quickstart.md`. Later refreshes: `openwiki code --update --print`, or the `OpenWiki Update` workflow (set `ANTHROPIC_API_KEY` as a repository secret; weekly Monday 08:00 UTC plus manual dispatch).

An update run reads `.openwiki-history.md` to learn what changed since the last run, because `.openwikiignore` blocks the agent from running `git log`. CI regenerates it automatically; before a local `--update`, run it yourself:

```bash
./scripts/openwiki-history-digest.sh
```

## Directory Guide

Each folder below has its own README.md describing what it holds, its naming convention, and who writes to it. A commit that changes a tracked file inside a folder that has a README.md must update that folder's README.md in the same commit; inside `plugins/`, this applies per plugin. The only exemption is a commit that stages exclusively a plugin's own `CHANGELOG.md`; any other staged file under that plugin still requires the immediate-folder README.md (see [CLAUDE.md](CLAUDE.md) for the exact routing).

| Folder | Purpose |
|--------|---------|
| `plugins/` | One directory per distributed plugin |
| `scripts/` | Repo-level tooling (git hook scripts) |
| `docs/` | Design specs and planning documents |

## Artifact inventory

| Path | Purpose |
|------|---------|
| `.claude-plugin/marketplace.json` | Marketplace manifest; lists all plugins |
| `.planning/` | GSD planning setup: PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, synthesized intel from the 50 ingested design docs, the ingest conflict report, and the onboarding summary. Current milestone: v5.0 Hybrid Diff-Review Architecture (32 requirements, 6 phases). Phase 1 context, its pattern map, its three execution plans, its baseline-gate evidence document (`01-VERIFICATION.md`, covering VAL-01/02/03, a triage ledger, the approved `proceed-as-triaged` remediation route, the Plan 2 fix/no-fix verdicts, Plan 3's final green receipts, its fix ledger, and its constraint proof), Plan 1, Plan 2, and Plan 3's summaries (`01-01-SUMMARY.md`, `01-02-SUMMARY.md`, `01-03-SUMMARY.md`), the phase code-review report (`01-REVIEW.md`, status clean: 0 critical, 0 warning, 2 info), and the independent verification report (`01-VERIFICATION-REPORT.md`, status passed: 7/7 truths, 1 recorded maintainer override for the two environmental pytest failures) are captured in `.planning/phases/01-baseline-health-verification/`. Phase 1 is complete and verified (3/3 plans, verification passed with 1 recorded override); STATE.md, ROADMAP.md, and PROJECT.md point at Phase 2 (Diff Pipeline & Positioning) next; Phase 2's context, discussion log, phase research, draft validation strategy, pattern map, and five verified execution plans (`02-CONTEXT.md`, `02-DISCUSSION-LOG.md`, `02-RESEARCH.md`, `02-VALIDATION.md`, `02-PATTERNS.md`, `02-01-PLAN.md` through `02-05-PLAN.md`; plan checker passed, 15/15 context decisions covered, 7/7 requirement IDs mapped) are captured in `.planning/phases/02-diff-pipeline-positioning/`. Plan 1 (the review-mode tracer), Plan 2 (the allowlist, exclude-glob, and exclusion-reason enum for `file_select.py`), Plan 3 (the `CoverageManifest` state machine and unified-diff hunk parser), Plan 4 (the never-guess four-rung positioning ladder plus the position-review markdown section and JSON ledger), and Plan 5 (the review-mode position gate's three-way split, the dropped-findings report section and ledger, and the seal-to-exit-code mapping) are complete and summarized (`02-01-SUMMARY.md`, `02-02-SUMMARY.md`, `02-03-SUMMARY.md`, `02-04-SUMMARY.md`, `02-05-SUMMARY.md`), and the phase code-review report (`02-REVIEW.md`, 3 critical, 2 warning findings) is recorded, and the verification report (`02-VERIFICATION.md`, status gaps_found, 6/9 must-haves) is recorded; the review's 5 findings were then fixed (`02-REVIEW-FIX.md`, status all_fixed), and the security audit is signed off (`02-SECURITY.md`, 24 threats closed, threats_open 0), and the validation strategy is filled and approved (`02-VALIDATION.md`, status validated, nyquist_compliant true, 0 gaps, 304 phase tests green); STATE.md and ROADMAP.md show all 8 Phase 2 plans done, and REQUIREMENTS.md marks DIFF-01 through DIFF-04 and POS-01 through POS-03 Complete; the milestone audit (`v5.0-MILESTONE-AUDIT.md`, second 2026-08-19 refresh at 3/6 phases complete, status gaps_found: 10/32 requirements satisfied, 7 partial, the `review` workspace-isolation sidecar blocker still open, Phase 2's three verification gaps confirmed fixed post-verification pending a phase re-verification; Nyquist validation now compliant for all three executed phases after the retroactive `01-VALIDATION.md`) is recorded at the `.planning/` root; Phase 3's context, discussion log, phase research, draft validation strategy, pattern map, and six checker-approved execution plans (`03-CONTEXT.md`, `03-DISCUSSION-LOG.md`, `03-RESEARCH.md`, `03-VALIDATION.md`, `03-PATTERNS.md`, `03-01-PLAN.md` through `03-06-PLAN.md`; 8/8 requirement IDs mapped, 16/16 context decisions covered, six sequential waves, one blocking decision checkpoint in Plan 4) are captured in `.planning/phases/03-rule-matching-review-modes/`; the plan check's three blockers are closed — Plan 6 adds the per-file review-agent dispatch that feeds real findings into `review_position_gate`, `03-VALIDATION.md` carries a filled per-task verification map (status draft), and `03-RESEARCH.md`'s open questions are resolved inline — and STATE.md records Phase 3 as "Ready to execute" (6 plans) with the six-wave order annotated in ROADMAP.md; Plan 1 (the review-mode tracer proving one Python file end to end through `rule_glob.resolve_rule_doc` and `reflection.apply_verdict`, plus its closeout: the D-01 Python-floor comment, the `rules/rule_docs/README.md` directory guide, and the `helpers/README.md` module-map update) is complete and summarized (`03-01-SUMMARY.md`); STATE.md and ROADMAP.md show Plan 1 of 6 done for Phase 3, and REQUIREMENTS.md marks RULE-01, RULE-05, and REV-02 Complete; `03-01-SUMMARY.md` carries a passed self-check confirming its created files and its four recorded commits (`cb2f7d7`, `a7c04ee`, `e058974`, `6b75973`) resolve in the repo; Plan 2 (four-layer per-path rule resolution with first-match-wins fallthrough, the whole-layer first-non-empty `--rule`/`--exclude` filter selection, `merge_system_rule` header concatenation, and the RULE-03 hard-reject rule-file safety gate — symlink resolution, extension allowlist, repo-root containment, and a 512 KB cap enforced on the read itself) is complete and summarized (`03-02-SUMMARY.md`); STATE.md and ROADMAP.md show Plan 2 of 6 done for Phase 3, and REQUIREMENTS.md marks RULE-02, RULE-03, and RULE-04 Complete; `03-02-SUMMARY.md` carries a passed self-check confirming its created files and its six recorded task commits (`4d7a4cd`, `1409dc1`, `0b142b9`, `c714b1b`, `21df638`, `061a2c4`) resolve in the repo; Plan 3 (seven per-language rule docs — go, java, php, rust, ts/js/tsx/jsx, kotlin, swift — plus a data-driven parametrized conformance test over `BUILTIN_PATH_RULE_MAP` and a rewritten language-agnostic `default.md`) is complete and summarized (`03-03-SUMMARY.md`); STATE.md and ROADMAP.md show Plan 3 of 6 done for Phase 3, and REQUIREMENTS.md confirms RULE-05 Complete; `03-03-SUMMARY.md` carries a passed self-check confirming its created files and its two recorded commits (`59fa3eb`, `bc1b021`) resolve in the repo; Plan 4 (the `security`/`general` review profiles: a new `review_findings.py` module wrapping findings in `ReviewFinding` to keep `models.py`/`evidence.py` frozen per D-11, `apply_profile`'s class-allowlist bypass of gates A/B for five general-defect classes, and a committed dual-run baseline fixture proving the security profile is byte-identical while the general profile is a strict superset) is complete and summarized (`03-04-SUMMARY.md`); STATE.md and ROADMAP.md show Plan 4 of 6 done for Phase 3, and REQUIREMENTS.md marks REV-01 Complete; `03-04-SUMMARY.md` carries a passed self-check confirming its created files and its four recorded commits (`245d9e7`, `8a982ce`, `e45d76a`, `6235453`) resolve in the repo; Plan 5 (the retract-only reflection filter: prompt, response validation, and the mechanical protected-subject veto, the never-silent ledger's markdown-rendering half, and the D-12 receipt-gate disposition ladder splitting general-defect findings without a Tier-1 receipt into `unconfirmed`/`needs-deployment-testing`) is complete and summarized (`03-05-SUMMARY.md`); STATE.md and ROADMAP.md show Plan 5 of 6 done for Phase 3, and REQUIREMENTS.md marks REV-03 Complete; `03-05-SUMMARY.md` carries a passed self-check confirming its created files and its three recorded commits (`ff9293f`, `f45f0c6`, `dcab42c`) resolve in the repo; Plan 6, the final plan of Phase 3 (`review_agent.py`'s per-file prompt render and response parser stamping every finding with `REVIEW_AGENT_CLAIM` so it can never self-confirm, the ported `agents/review-file.md` prompt and the SKILL.md prepare-dispatch-consume dispatch loop, and `run_review`'s wiring of a real finding source — `diffscope.file_text_at_ref`-derived evidence, `--prepare`, and `recorded_return_source` — through the position, profile, reflection, and receipt gates in spec order, proven by a live `--profile security`/`--profile general` split on one fixture diff) is complete and summarized (`03-06-SUMMARY.md`); STATE.md and ROADMAP.md show Phase 3 complete at 6 of 6 plans done, and REQUIREMENTS.md confirms RULE-01, REV-01, and REV-02 Complete; `03-06-SUMMARY.md` carries a passed self-check confirming its created files and its three recorded commits (`0436562`, `a9b9698`, `9f86ddc`) resolve in the repo; the Phase 3 code-review report (`03-REVIEW.md`, status issues_found: 1 critical, 3 warning, 1 info across 29 files at standard depth) is recorded; the Phase 3 verification report (`03-VERIFICATION.md`, status gaps_found: 4/5 must-haves, two gaps — the discarded reflection kept-list and the unwired disposition ladder — plus one human-verification item) is recorded; the gap-closure plan `03-07-PLAN.md` (wave 7, gap_closure, requirements REV-02/REV-03, 3 tasks) closes both gaps and converts the pending human-verification item into an automated multi-file isolation test, leaving plans `03-01` through `03-06` untouched; the plan checker passed `03-07-PLAN.md` (gap coverage, REV-01 regression safety, 16/16 context decisions still covered), and STATE.md records Phase 3 back in executing state with 15 total plans and `03-07` ready to execute; `03-07` (the gap-closure plan) is complete and summarized (`03-07-SUMMARY.md`) — Task 1 rewires `run_review`'s reflection loop to read and rebind `apply_profile`'s kept output so a retraction actually removes its finding from the ledger (`fb5aca7`), Task 2 routes `apply_profile`'s kept-finding disposition through the D-12 ladder (`45dafc1`), and Task 3 adds a composed end-to-end proof plus a clean phase-gate receipt (`5cf439e`); STATE.md and ROADMAP.md show Phase 3 complete at 7 of 7 plans done, and REQUIREMENTS.md confirms REV-02 and REV-03 Complete; the regenerated Phase 3 code-review report (`03-REVIEW.md`, status issues_found: 0 critical, 2 warning, 1 info across 39 files at standard depth, prior findings CR-01 and WR-01 confirmed fixed) is recorded; the Phase 3 re-verification report (`03-VERIFICATION.md`, status passed: 5/5 must-haves, all 8 requirement IDs traced) is recorded, and ROADMAP.md and STATE.md mark Phase 3 complete (2026-08-19) and point at Phase 4 (Scale, Resume & Diff Output) next; PROJECT.md moves REV-01/REV-04/REV-05 (Phase 3) and REV-02/REV-03 (Phase 2) from Active to Validated; the Phase 3 security audit is signed off (`03-SECURITY.md`, status verified: 27 threats closed, threats_open 0, 3 accepted risks); the Phase 3 validation strategy is validated (`03-VALIDATION.md`, status validated, nyquist_compliant true, 1 gap resolved, 191 phase tests green); the Phase 1 validation strategy is reconstructed retroactively and validated (`01-VALIDATION.md`, status validated, nyquist_compliant true, 1 gap found and resolved — tests added for the `stage_validate.py` non-dict rejection adapters); Phase 04.1 (Close gap: DIFF-04 — review sidecar workspace isolation, inserted after Phase 4) carries its phase research (`04.1-RESEARCH.md`, confidence HIGH: the fix routes `run_review`'s workspace through `RepoMemory.for_target` like `scan`/`audit`) and a draft validation strategy (`04.1-VALIDATION.md`, status draft) in `.planning/phases/04.1-close-gap-diff-04-review-sidecar-workspace-isolation/`; the phase now also carries its single execution plan (`04.1-01-PLAN.md`, wave 1, requirement DIFF-04, 3 tasks: a tracer that re-roots `run_review` onto the sidecar and pins it with `test_review_defaults_to_repo_memory`, an expansion task that re-roots roughly 28 assertions across four test files, and a docs-and-governance task landing everything as one commit), and ROADMAP.md records the phase goal, DIFF-04 as its requirement, and the plan checklist; the phase pattern map (`04.1-PATTERNS.md`, 6/6 analogs — the fix mirrors the `scan`/`audit` `RepoMemory.for_target` branches in `cli.py`) is recorded, the plan checker passed 10/10 dimensions, and STATE.md marks Phase 04.1 "Ready to execute" (1 plan); `04.1-01` is complete and summarized (`04.1-01-SUMMARY.md`) — `run_review` now resolves its workspace through `RepoMemory.for_target(root, runner=r)` instead of constructing `Workspace(args.root)` directly, matching `scan`/`audit`, pinned by a new regression test and roughly 28 re-rooted assertions across four test files, with docs and the version bump landed in one governed commit (`f76432a`); STATE.md and ROADMAP.md show Phase 04.1 complete at 1 of 1 plan done, and REQUIREMENTS.md confirms DIFF-04 Complete; the Phase 04.1 code-review report (`04.1-REVIEW.md`, status clean: 0 critical, 0 warning, 0 info across 14 files at standard depth) is recorded, and the phase verification report (`04.1-VERIFICATION.md`, status passed: 6/6 must-haves, DIFF-04 traced) marks Phase 04.1 complete (2026-08-19) with STATE.md and ROADMAP.md pointing at Phase 5 (End-to-End Verification) next; PROJECT.md moves DIFF-04 to Validated; the Phase 04.1 security audit is signed off (`04.1-SECURITY.md`, status verified: 5 threats, 5 closed, threats_open 0, 3 accepted risks); the Phase 04.1 validation strategy is filled and approved (`04.1-VALIDATION.md`, status validated, nyquist_compliant true, 0 gaps, 78 phase tests green, full suite at the recorded baseline); the milestone audit is refreshed a third time (`v5.0-MILESTONE-AUDIT.md`, 2026-08-19, status gaps_found at 4/7 phases executed: 12/32 requirements satisfied, 6 partial pending a Phase 2 re-verification, 14 pending in unstarted Phases 4–6, DIFF-04 sidecar blocker closed by Phase 04.1, integration check 6/6 links wired with zero gaps, Nyquist compliant across all four executed phases); the Phase 2 UAT is complete (`02-UAT.md`, status complete: 10/10 passed — 6 deliverables auto-covered by passing tests, 4 verified by hand against a live `sec-overlay review` run, 0 issues); the Phase 2 verification is regenerated after the review fixes (`02-VERIFICATION.md`, status passed: 9/9 must-haves, the three previously-failed items CR-01/CR-02/CR-03 live-reproduced as fixed), ROADMAP.md marks Phase 2 complete (2026-08-19), and STATE.md keeps its position at Phase 5; Phase 4 (Scale, Resume & Diff Output) carries its phase research (`04-RESEARCH.md`, confidence HIGH: bounded concurrency and timeout handling reuse the existing `prefilter.py` `ThreadPoolExecutor` pattern and `CoverageManifest`'s `fail()`/`seal()` transitions, resume identity-pinning extends `CoverageManifest` with model/profile fields, and the diff-anchored comment payload and SARIF `partialFingerprints` are new sibling artifacts derived from `Finding.file`/`cls`/`evidence`) covering SCALE-01/02/03 and OUT-01/02, and a draft validation strategy (`04-VALIDATION.md`, status draft) in `.planning/phases/04-scale-resume-diff-output/`; the phase now also carries its three execution plans (`04-01-PLAN.md` wave 1 — the bundle-grouping tracer, the diff-anchored comment payload, and content-only SARIF fingerprints for SCALE-01/OUT-01/OUT-02; `04-02-PLAN.md` wave 2 — the three bounded flags, the order-preserving git worker pool, and timeout-to-partial sealing for SCALE-02; `04-03-PLAN.md` wave 3, non-autonomous — a blocking decision checkpoint on where resume identity is persisted, then the pre-write identity refusal and SHA-pinned resumed reads for SCALE-03), each with a STRIDE threat model at ASVS level 1, all three plans validating clean on frontmatter and structure, plus an API-coverage declaration (`COVERAGE.md`: no external API integration, stdlib-only core per ADR-2026-08-04 and REL-03), and ROADMAP.md records the plan checklist and a 0/3 progress row; the plan checker passed all three Phase 4 plans (5/5 requirement IDs mapped, all 8 edge-probe items authored, dependency graph acyclic), and STATE.md marks Phase 4 "Ready to execute" (3 plans) with the three-wave order annotated in ROADMAP.md; `04-01` is complete and summarized (`04-01-SUMMARY.md`) — `bundle.group_bundles` gains real grouping semantics (impl/test pairs, locale/config siblings, single-file fallback), `review_agent.parse_review_response`'s widened `bundle_paths` focus rule is threaded end to end through `cli.run_review`, and dedicated test coverage locks the `review_comments.json` and SARIF `partialFingerprints` contracts (both already correct from the tracer plan); STATE.md and ROADMAP.md show Plan 1 of 3 done for Phase 4, and REQUIREMENTS.md marks SCALE-01, OUT-01, and OUT-02 Complete; `04-02` is complete and summarized (`04-02-SUMMARY.md`) — `review` gains three validated `--concurrency`/`--timeout`/`--max-git-procs` flags (defaults 8/600/16, rejecting rather than clamping out-of-range input), the two per-file git-fetch loops run through a `ThreadPoolExecutor` bounded by `--max-git-procs` while preserving serial file order, and a per-`ReviewUnit` `--timeout` fails every member file of a unit that misses its deadline so the coverage manifest seals partial instead of hanging or raising; STATE.md and ROADMAP.md show Plan 2 of 3 done for Phase 4, and REQUIREMENTS.md marks SCALE-02 Complete; `04-03` is complete and summarized (`04-03-SUMMARY.md`) — a Task 1 decision checkpoint resolved option-a (identity lives on `CoverageManifest` itself, `MANIFEST_VERSION` 1 to 2), Task 2 adds `check_resume_identity`/`ResumeIdentityError` refusing a mismatched resume before the manifest's first write (proven by a byte-hash-unchanged test), and Task 3 pins a resumed run's `base_sha`/`head_sha` to the prior manifest's sealed values, round-tripped through `resolve_ref_sha` so a rewritten or collected SHA fails loudly instead of reading an empty diff; STATE.md and ROADMAP.md show Phase 4 complete at 3 of 3 plans done, and REQUIREMENTS.md marks SCALE-03 Complete; the Phase 4 code-review report (`04-REVIEW.md`, status issues_found: 3 critical, 3 warning, 1 info across 24 files at standard depth) is recorded; the Phase 4 verification report (`04-VERIFICATION.md`, status gaps_found: 2/5 must-haves, three gaps — the pre-seal manifest embed, the missing `--model` CLI surface, and the unbounded executor shutdown past `--timeout`) is recorded; the Phase 4 gap-closure plan (`04-04-PLAN.md`, wave 4, three TDD tasks claiming OUT-01, SCALE-02, and SCALE-03) is planned and ROADMAP.md shows Phase 4 at 3 of 4 plans executed; `04-04` is complete and summarized (`04-04-SUMMARY.md`) — `review_comments.json`'s embedded coverage-manifest seal now always matches the on-disk manifest for a complete or partial run, `review --model <id>` is a real argparse flag forwarded to `run_review` and enforced on resume, and a hung review-unit git fetch no longer holds `run_review` past `--timeout` (the executor stops waiting on the abandoned worker, the worker itself stops fetching past its own deadline, and the underlying git child is killed via a per-call `subprocess.run(timeout=...)`); STATE.md and ROADMAP.md show Phase 4 complete at 4 of 4 plans done, and REQUIREMENTS.md confirms OUT-01, SCALE-02, and SCALE-03 Complete; the Phase 4 code-review report is regenerated after the gap closure (`04-REVIEW.md`, status issues_found: 0 critical, 2 warning, 1 info across 20 files at standard depth; the three prior critical findings are confirmed fixed); the Phase 4 re-verification report (`04-VERIFICATION.md`, status passed: 5/5 must-haves, all three prior gaps re-confirmed fixed independently, full suite at the recorded baseline) is recorded, and ROADMAP.md and STATE.md mark Phase 4 complete (2026-08-20); PROJECT.md moves REV-06 (with SCALE-01/02/03 and OUT-01/02) from Active to Validated, and STATE.md points at Phase 5 (End-to-End Verification) next — Phase 04.1 was already complete; Phase 5's context and discussion log (`05-CONTEXT.md`, `05-DISCUSSION-LOG.md`: audit target is a pinned local work repo, review runs both profiles on one real diff chosen by criteria, evidence is sanitized receipts with the sidecar read live by the verifier, and Phase 5 fixes run-blockers only with `05-DEFECTS.md` feeding Phase 6) are captured in `.planning/phases/05-end-to-end-verification-audit-review/`, and STATE.md records the Phase 5 context session; Phase 5's research (`05-RESEARCH.md`, confidence HIGH: the six AUD success criteria are already mechanically enforced in shipped driver/gate code, no natural D-05-compliant diff yet exists in mando's history, and the environment is ready except a vendored semgrep-rules content gap with a verified fix) is captured in the same phase directory, alongside the phase's draft validation strategy (`05-VALIDATION.md`, status draft), its pattern map (`05-PATTERNS.md`), its API coverage declaration (`COVERAGE.md`, no external API integration), and its four execution plans (`05-01-PLAN.md` through `05-04-PLAN.md`: a review tracer in both profiles, the full audit run behind a semgrep-ruleset fix, then finding-integrity and artifact-coverage readbacks; 6/6 requirement IDs and 12/12 context decisions mapped, run in four sequential waves); the plan checker passed all four plans (0 blockers, 2 non-blocking warnings), and STATE.md marks Phase 5 "Ready to execute" (4 plans) with the four-wave order annotated in ROADMAP.md; `05-01`'s tracer Task 1 and Task 2 are complete, sanitized per D-07 (`05-01-review-security-receipt.md`, `05-01-review-general-receipt.md`): both profiles ran end to end on one real diff in mando (14 of 15 changed files reviewable, one `.mdc` file excluded as not-allowlisted), each sealing complete with 0 live findings — by design, since the CLI-only invocation never dispatches the per-file reviewer subagent — and the vacuous security-kept-⊆-general-kept subset check passed; a real cwd-scoping bug in `run_review`'s production git runner was found and fixed in `plugins/sec-overlay` (`841c5d8`), and `05-DEFECTS.md` records five D-11 ledger rows (the fixed cwd bug, and four deferred: the D-05 mixing-criterion gap, the un-exercised reviewer dispatch, `review`'s missing `--workspace` flag, and the E-12 vacuous superset-contract flag); `05-01` is complete and summarized (`05-01-SUMMARY.md`) with a passed self-check, STATE.md and ROADMAP.md show Plan 1 of 4 done for Phase 5, and REQUIREMENTS.md marks AUD-06 Complete; STATE.md's Current Position now reads Plan 2 of 4 with three new decisions recorded, and ROADMAP.md's Phase 5 progress row shows 1/4 plans, In Progress; `05-02`'s Task 1 closed the vendored semgrep-ruleset gap as a governed run-blocker fix, and Task 2 drove the full audit end to end on mando's pinned head through all 22 `phases.py` stages plus the out-of-table `redteam`/`postflight` steps (the first pass halted at `artifact-gate` for a missing red-team plan, closed by running `redteam`/`redteam-adversary`/`postflight` as the plugin's own maintainer manual documents), with the working-tree fence holding throughout; Task 3 is complete and sanitized per D-07 (`05-02-audit-run-receipt.md`): the pinned SHA, all 24 recorded stages, the 21 per-phase receipts, and the fence-clean result are recorded, and `05-DEFECTS.md` gains four more dispositioned rows (9 total) — the redteam/postflight documentation-vs-`PHASE_TABLE` gap, a `redteam.py` prose-vs-mechanics gap on the runtime-disposition override, and a cosmetic deps-finding-template rendering defect, all deferred to Phase 6; `05-02` is complete and summarized (`05-02-SUMMARY.md`) with a passed self-check confirming both recorded commits (`278d80e`, `8a981ca`) resolve in the repo, and a self-caught documentation-accuracy note that `05-DEFECTS.md` actually gained three new rows in Task 3 (six to nine), not four — the "9 total" figure and every automated `<verify>` check were unaffected; STATE.md and ROADMAP.md show Plan 2 of 4 done for Phase 5, and REQUIREMENTS.md marks AUD-01 Complete; `05-03`'s sanitized finding-integrity receipt (`05-03-finding-integrity-receipt.md`) reads Plan 02's live sidecar and proves AUD-02 (the one confirmed finding passes `evidence.confirms_alone()`, 0 ladder violations) and AUD-03 (all 3 `needs-deployment-testing` findings carry a positive `risk_score`, report headline count matches) on real output with zero violations, so `05-DEFECTS.md` gains no new rows; `05-03` is complete and summarized (`05-03-SUMMARY.md`) with a passed self-check confirming the receipt and its recorded commit (`2ede53e`) resolve in the repo; STATE.md and ROADMAP.md show Plan 3 of 4 done for Phase 5, and REQUIREMENTS.md marks AUD-02 and AUD-03 Complete; `05-04`'s sanitized artifact-and-coverage receipt (`05-04-artifact-coverage-receipt.md`) reads Plan 02's live sidecar and proves AUD-04 (both `arch-gate`/`tm-gate` verdicts `passed=true` with 0 errors, independently reproduced by re-running the four composed deterministic checks, and 10 real CVSS vectors all `CVSS:4.0/`) and AUD-05 (the report's explicit 515-file coverage denominator, a 0-error coverage-ledger validation, and 0 unlogged zero-finding attack-surface classes) on real output, plus a D-08 evidence map citing a sidecar path and field for all six AUD criteria; `05-DEFECTS.md` gains no new rows; `05-04` is complete and summarized (`05-04-SUMMARY.md`) with a passed self-check confirming its recorded commit (`8e44322`) resolves in the repo; STATE.md and ROADMAP.md show Phase 5 complete at 4 of 4 plans done and ready for verification, and REQUIREMENTS.md marks AUD-04 and AUD-05 Complete; the Phase 5 code-review report (`05-REVIEW.md`, status issues_found: 0 critical, 2 warnings, 0 info across the 10 files changed in Phase 5) records one unhandled-`FileNotFoundError` warning for an invalid `--root` in `cli.py` and one test-README accuracy warning, both non-blocking; the independent verification report (`05-VERIFICATION.md`, status human_needed: 17/18 must-haves verified, all six AUD criteria hold on real evidence) routes two judgment calls to `05-UAT.md` — the disposition of the two review warnings, and acceptance of the vacuous AUD-06 profile-superset pass; the Phase 5 security audit is signed off (`05-SECURITY.md`, status verified: 28 threats closed, threats_open 0, 2 accepted risks); the Phase 5 validation strategy (`05-VALIDATION.md`) is validated and Nyquist-compliant after a retroactive audit mapped all six AUD criteria to green automated tests (201 tests across 9 mapped suite files) plus 5 documented manual-only live-run verifications, with 0 gaps found; the milestone audit is refreshed a fourth time (`v5.0-MILESTONE-AUDIT.md`, 2026-08-21, status gaps_found at 6/7 phases executed: 28/32 requirements satisfied, AUD-06 partial pending two Phase 5 human sign-off items, REL-01/02/03 orphaned in the unstarted Phase 6, integration check 9/9 paths wired with all 3 E2E flows receipt-backed, Nyquist compliant for 5 of 6 executed phases with Phase 4's VALIDATION.md still in draft); the Phase 4 validation strategy is then filled and approved retroactively (`04-VALIDATION.md`, status validated, nyquist_compliant true, 0 gaps — SCALE-01/02/03 and OUT-01/02 all map to green tests shipped during execution, 158 tests across 8 phase files passing), closing the milestone audit's last Nyquist TODO; the milestone audit is refreshed a fifth time (`v5.0-MILESTONE-AUDIT.md`, 2026-08-21, status gaps_found: 28/32 requirements satisfied, an independent integration re-check confirming 12/12 cross-phase wiring points and 3/3 E2E flows, Nyquist compliant across all six executed phases; remaining gaps are AUD-06 partial pending the two Phase 5 human sign-off items in `05-UAT.md` and REL-01/02/03 in the unstarted Phase 6); the Phase 5 UAT is complete (`05-UAT.md`, status complete: 2/2 passed, 0 issues — WR-01/WR-02 deferred to Phase 6's REL-01 sweep as two new `05-DEFECTS.md` rows, and the vacuous AUD-06 profile-superset pass accepted with the substantive re-check tracked by the E-12 deferral), and `05-VERIFICATION.md` is canonicalized to passed; Phase 5 is marked complete (2026-08-21) in ROADMAP.md and STATE.md, PROJECT.md moves AUD-01 through AUD-06 to Validated, and STATE.md points at Phase 6 (Remediation and Governed Release), ready to plan; Phase 6's context and discussion log (`06-CONTEXT.md`, `06-DISCUSSION-LOG.md`) are captured in `.planning/phases/06-remediation-and-governed-release/`: all 10 deferred `05-DEFECTS.md` rows get fixes or written dispositions (PHASE_TABLE gains redteam/postflight wiring, `review` gains `--workspace`, WR-01 gets a clean exit-2, the deps template and three stale docs are corrected), E-12 closes via a real agent-dispatched review on the Phase 5 SHA range with a pytest subset backstop, fixes ship as thematic PRs off the milestone branch with per-commit semver, and a dedicated identity-guard test asserts the frozen contract; STATE.md records the context session with `06-CONTEXT.md` as the resume file; the phase now also carries its pattern map (`06-PATTERNS.md`, 8/8 analogs) and its five execution plans (`06-01-PLAN.md` through `06-05-PLAN.md`, five sequential waves: a tracer closing the WR-01 invalid-`--root` crash with a clean exit-2 plus `review --workspace` and the PR governance rail, the `redteam`/`postflight` `PHASE_TABLE` and `DETERMINISTIC_ACTIONS` wiring, the deps-finding template and stale-doc corrections, the frozen-contract identity and profile-subset guards, and a real agent-dispatched review closing E-12 with sanitized receipts, a terminal-state defect ledger, and the phase close), with 3/3 REL requirement IDs and 16/16 context decisions mapped and a STRIDE threat model at ASVS level 1 in every plan; Plan 05 was revised after the decision-coverage gate so its E-12 re-verification surfaces cite D-06 (the real per-file reviewer dispatch and the security-kept subset check) and D-07 (Phase 5's exact `base..head` SHA range, re-read from the Phase 5 receipts); the plan checker passed all five plans, STATE.md marks Phase 6 "Ready to execute" (5 plans), and ROADMAP.md annotates the five-wave sequential order; `06-01` is complete and summarized (`06-01-SUMMARY.md`) — WR-01's invalid-`--root` crash now exits 2 cleanly instead of raising, `review` gains a `--workspace` flag resolving through `workspace.load_paths` exactly like `audit`'s, and both ship as four governed commits (`plugin.json` 1.68.7 to 1.69.0) merged via PR #23 (`562b313`) off `docs/milestone-v5-diff-review`; the merge also confirms CodeRabbit's automatic review is disabled for a non-default base branch, requiring a manual `@coderabbitai review` comment to trigger a walkthrough — the answer Plans 02 through 04 depend on; two CodeRabbit nitpicks (an unasserted `--workspace` forwarding test, an unproven guard-before-git ordering) are logged to `deferred-items.md`; STATE.md and ROADMAP.md show Plan 1 of 5 done for Phase 6, and REQUIREMENTS.md marks REL-01 and REL-02 Complete; `06-02` is complete and summarized (`06-02-SUMMARY.md`) — `PHASE_TABLE` grows to 24 entries with a new `redteam` agent phase inserted between `selfscore` and `artifact-gate` (not after `artifact-review`, since `artifact_gate.run_artifact_gate` hard-requires `redteam-plan.md`) and a new `postflight` deterministic phase appended last, `driver.py` registers `postflight` in `DETERMINISTIC_ACTIONS` via `_act_postflight`, and the maintainer manual's three phase-order doc surfaces are corrected to match, shipping as four governed commits (`plugin.json` 1.69.1 to 1.69.4) via PR #24 (fast-forward merge, no merge commit) off `docs/milestone-v5-diff-review`; CodeRabbit's manual review trigger hit the reviewer's OSS rate limit rather than posting a walkthrough, and the user waived the wait for this PR; STATE.md and ROADMAP.md show Plan 2 of 5 done for Phase 6; `06-03` is complete and summarized (`06-03-SUMMARY.md`) — the deps Fix line now renders the real package name for scoped npm-style identifiers instead of an empty backtick pair, `agents/redteam.md` describes the real two-condition `wants_runtime()` OR predicate instead of an invented three-way split, and the false claim that the vendored semgrep ruleset is a tracked git submodule is corrected across 6 doc surfaces (8 mentions) alongside a wrong cwd-scoping-bug explanation, each closed defect held by a new code-derived doc guard in `test_docs_invariants.py`, shipping as four governed commits (`plugin.json` 1.69.4 to 1.69.8) via PR #25 (merge `8c3c800`) off `docs/milestone-v5-diff-review`; `@coderabbitai review` was triggered once and the repo owner merged without waiting further, per the phase's standing non-default-base-branch override; STATE.md and ROADMAP.md show Plan 3 of 5 done for Phase 6; `06-04` is complete and summarized (`06-04-SUMMARY.md`) — a new `test_frozen_contract.py` pins `models.py`/`evidence.py` to sha256 digests and `fingerprint()` to a golden 12-hex value across three field-order/completeness variants, closing REL-03 with a `tomllib` read of the real `pyproject.toml`'s empty dependency list, and four new `apply_profile()` probes (vacuous, single-element, boundary, permutation) appended to `test_review_profiles.py` turn Phase 5's E-12 vacuous-subset observation into a non-vacuous unit-level guarantee, all five detection proofs run and reverted before staging; shipping as two governed commits (`plugin.json` 1.69.8 to 1.69.10) via PR #26 (merge `498597a`) off `docs/milestone-v5-diff-review`; a courtesy `@coderabbitai review` was posted and the merge did not wait further, per the phase's standing waiver; STATE.md and ROADMAP.md show Plan 4 of 5 done for Phase 6, and REQUIREMENTS.md marks REL-03 Complete; `06-RECEIPTS.md` records Plan 5's real per-file review dispatch (14 fresh-context `sonnet` `review-file` subagents spawned by the orchestrator against one real diff, since the executor's own toolset carries no subagent-dispatch capability) and computes the E-12 subset verdict from two consume passes over the identical 14 recorded returns — security-kept 0, general-kept 5, `∅ ⊆ {5}` holds and is not vacuous, unlike Phase 5's both-empty comparison — with Phase 5's sidecar digest confirmed unchanged |
| `plugins/sec-overlay/.claude-plugin/plugin.json` | sec-overlay plugin manifest |
| `plugins/sec-overlay/README.md` | sec-overlay user-facing README: install, prerequisites, quick start |
| `plugins/sec-overlay/CHANGELOG.md` | sec-overlay Common Changelog |
| `plugins/sec-overlay/CLAUDE.md` | sec-overlay maintainer manual: development commands, folder-README rule, history |
| `plugins/sec-overlay/commands/` | Slash commands the plugin installs, including `/sec-overlay:audit`; a plugin-root `commands/` file is install payload, so a change there bumps the plugin version |
| `plugins/sec-overlay/skills/sec-overlay/SKILL.md` | Skill playbook: agentic security-audit harness; links to the skill's `CLAUDE.md` for prerequisites and operating rules |
| `plugins/sec-overlay/skills/sec-overlay/helpers/` | Python core (`sec_overlay` package) that runs tools and enforces gates |
| `plugins/sec-overlay/skills/sec-overlay/agents/` | LLM subagent prompts for the investigate/validate/patch phases |
| `docs/` | Design specs and implementation plans (see `docs/README.md`), including the sec-overlay defect-remediation design and its Plan A audit-driver, Plan B shared-vocabulary, Plan C coverage/accuracy, and Plan D report/telemetry/artifact-review implementation plans, and the architecture/threat-model standards design (C4/arc42 + derived DFD/STRIDE, CVSS v4.0, Mermaid caps, STE prose) with its Plans 1–3 (CVSS v4.0 migration, diagram/STE enforcement, phase rebuild), and the sec-overlay invocation design (one `/sec-overlay:audit` command, a `run.py` driver, scan-profile role inference feeding the correlation core) with its nine-task implementation plan |
| `docs/templates/plugin/` | New-plugin skeleton copied to `plugins/<name>/` and filled in per the root `CLAUDE.md` "New plugin" checklist |
| `.pre-commit-config.yaml` | prek hook config: doc-update guard + commit message check |
| `scripts/hooks/` | Hook scripts that enforce commit governance |
| `scripts/openwiki-history-digest.sh` | Writes the bounded `.openwiki-history.md` change digest an OpenWiki update run reads in place of `git log` |
| `CHANGELOG.md` | Common Changelog for repo-level changes; plugin changes live in `plugins/<name>/CHANGELOG.md` |
| `SECURITY.md` | How to report vulnerabilities (GitHub private reporting) |
| `.github/workflows/dependency-review.yml` | GitHub Dependency review on pull requests |
| `.github/dependabot.yml` | Weekly Dependabot updates for Actions and pip |
| `.github/codeql/codeql-config.yml` | CodeQL path exclusions (test fixtures, caches) |
| `.gitignore` | Keeps caches, venvs, local secrets, and the vendored semgrep-rules clone out of git |
| `.coderabbit.yaml` | CodeRabbit pull request review config: path rules, governance pre-merge checks, tool selection |
| `.cursor/rules/codeguard-1-hardcoded-credentials.mdc` | Always-on Cursor rule: never commit secrets, API keys, or credentials |
| `.openwikiignore` | Paths OpenWiki must not read during wiki init/update (separate from `.gitignore`) |
| `openwiki/INSTRUCTIONS.md` | User-authored wiki brief for init and CI `--update`; OpenWiki does not rewrite it |
| `.env.example` | Local OpenWiki provider, model, and telemetry-off settings (no secrets) |
| `.github/workflows/openwiki-update.yml` | Weekly/manual OpenWiki `--update` that opens a PR using Anthropic Sonnet 5 |
| `openwiki/` | Generated marketplace wiki (quickstart, marketplace, governance, sec-overlay, operations); do not hand-edit except `INSTRUCTIONS.md` |
| `AGENTS.md` | OpenWiki pointer block for coding agents; the generated `<!-- OPENWIKI:START -->` region only |

## Contributing

All changes go through feature branches with Conventional Commits. See [CLAUDE.md](CLAUDE.md) for detailed branching, commit, and code review processes. Root and plugin docs are split by audience: this README and the root `CHANGELOG.md` cover repo-level changes, while each plugin carries its own README, CHANGELOG, and maintainer CLAUDE.md under `plugins/<name>/`. Design specs and implementation plans for in-flight work live under `docs/superpowers/` (see [docs/README.md](docs/README.md)). CLAUDE.md's OpenWiki section covers when a generated page may be hand-edited.

## License

MIT
